---
schema: nbook.spec/v1
kind: behavior
status: planned
capability: ui.component-lab
owners:
  - ui
---
## 背景

NeuroBook 主应用有大量 Vue 组件，验证它们的行为目前依赖散落的 preview 页面。这些页面混杂两类内容：一类可以用固定输入完整表达，另一类必须调用真实后端、真实模型或真实项目才能成立。两类混在一处，无法判断一次观察到的结果是组件本身的行为还是外部状态的偶然结果。

Lab 是只存在于源码开发环境的组件检视入口。本规范**只定义在动手之前就必须确定的边界**：Lab 允许存在于哪里、绝对不能进入哪里、fixture 允许接触什么。Lab 的导航、检视面板、组件索引形状、场景交互与失败呈现**刻意不在此定义**，等原型建成并迁移过若干组件后，按真实形态补写；提前写只能是猜测。

组件自身的行为合同、能力标签与耦合约束由[`组件规范`](../../standards/code/components.md)定义，本规范不重复。

# NeuroBook Component Lab

## 目标与非目标

目标：

- 提供只存在于源码开发环境的组件检视入口，用固定输入观察组件行为与响应式边界。
- 把需要真实产品能力的场景挡在 Lab 之外，使 Lab 中的观察结果不受外部状态影响。
- 保证可分发产物不包含 Lab 代码、fixture 或开发路径信息。

非目标：

- Lab 不是产品功能，不面向最终用户，不是任何业务的正式入口。
- Lab 不替代正式界面验收、桌面冒烟测试、主题首帧验证或真实业务流程的窄屏验收。
- 本规范不定义 Lab 的界面形态与交互合同，不定义组件行为合同与能力标签，不定义组件索引的维护流程，不记录任何一次重构的进度。
- `planned` 状态不表示 Lab、fixture 或产物排除检查已经存在。

## 术语与参与者

- **源码开发环境**：从仓库源码启动的开发服务，可包含仅供开发验证的能力。
- **可分发产物**：构建输出的客户端、服务端或桌面安装包。
- **Lab**：源码开发环境专属的组件检视入口。
- **fixture**：固定输入与交互场景。重复运行结果相同，不含真实用户数据，不产生真实产品副作用。
- **确定性场景**：可以由 fixture 完整表达的场景，位置在 Lab。
- **真实行为场景**：必须在正式界面并使用真实产品边界验证的场景，不能用 fixture 替代。
- **产物门禁**：在构建流程中运行的检查，验证产物、路由与清单不含 Lab、fixture、开发路径或识别标记。
- **失败即拒绝**：失败时拒绝操作并报错，不静默降级为看似成功的结果。

## 输入与前置条件

Lab 入口只在源码开发环境下注册。构建、预览、安装包与运行时都不得通过环境变量或路由守卫隐藏一个实际已打包的 Lab；排除必须发生在构建图与路由生成阶段。

fixture 不得要求凭据、网络、真实 Project/Session、Provider/Model 或浏览器持久化状态。表达错误分支与时间相关分支时使用固定输入，不伪造业务逻辑的成功结果。

组件能否在 Lab 中验证，由[`组件规范`](../../standards/code/components.md)的能力标签推导，不由人工逐个决定：

- 不含 `persist:`、不含 `io:` 且不含 `state:shared-write` 的组件，可在 Lab 中验证。
- 含 `state:shared-read` 的组件，需要预置状态快照后才能验证。
- 含 `io:` 或 `state:shared-write` 的组件只能在正式界面验证，Lab 不挂载。

## 输出与可观察行为

- 同一个场景重复打开或重置后，初始输入与可观察状态一致，不依赖真实用户数据或网络时序。
- Lab 提供桌面、手机与平板三种响应式容器；手机固定为 `390×844`，平板固定为 `768×1024`。切换容器不改变 fixture 语义。
- 运行中的视口从宽屏进入 `<=700px` 时，左右侧栏自动收起，把空间让给画布；从窄屏恢复宽屏不自动展开，保留使用者最后一次侧栏状态。
- 侧栏状态切换提供短时转场；`prefers-reduced-motion: reduce` 时关闭转场，不改变状态结果。场景切换直接替换 fixture，不插入空白退场阶段。
- 元素检查器的悬停探针使用虚线矩形框；点击选中后只保留贴边标签，不常驻绘制整块元素边框。
- 不可在 Lab 中验证的组件可以被查询并显示原因，但不会被挂载为可交互场景，也不会以 fixture 形式给出替代结论。
- 可分发产物中访问 Lab 路径得到正常的不存在结果，且构建图、文本与清单中不含 Lab 模块、fixture、绝对源码路径或识别标记。

## 状态与转换

本能力不引入产品侧持久状态。场景加载、动作执行与响应式容器切换都只改变本地界面状态，不写入任何产品数据。

例外只有一处：Lab **自身的界面偏好**可以写本机浏览器存储，当前仅桌面壁纸一项。它是一张几 MB 的图片，进仓库要让每个终端用户跟着下载，不存又要每次重开都重选，两头都不成立。这一处必须满足三条：只占 Lab 自己的键、界面上提供清除入口、任何场景与 fixture 都不得读取它。场景的可重复性因此不受影响。

这些界面状态的具体转换合同属于 Lab 的界面形态，按本规范背景所述在原型建成后补写，当前不予规定。

## 副作用与数据

Lab 只读取版本控制内的组件声明与 fixture，并维护本地开发环境的界面状态。它不写用户配置、项目工作区、会话历史或业务数据库，也不访问真实网络服务。浏览器持久化只允许用于上一节所述的 Lab 自身界面偏好，不得用于场景、fixture 或任何产品数据。

fixture 使用脱敏的静态或内存数据，不调用真实 Provider/Model、真实接口，也不依赖跨场景共享的状态。

产物排除是构建阶段的边界：源码开发环境注册 Lab，可分发产物既不注册也不打包。验收产生的截图与日志写入系统临时根，不进入仓库或产物。

## 失败与恢复

- fixture 尝试访问真实接口、持久化状态、Provider/Model 或产品凭据时失败即拒绝，该场景视为不合格，不替换为看似成功的结果。
- 构建图、路由、文本或清单中出现 Lab、fixture、开发绝对路径或识别标记时，产物门禁失败。不得用运行时不存在或路由守卫隐藏来判定通过。
- 响应式容器下出现页面级横向滚动、操作被遮挡或焦点无法恢复时，该场景保持未验收，不降低视口要求。
- Lab 失败不改变产品数据。恢复方式是修正 fixture、组件声明或组件实现后重新加载，不创建第二个 Lab 入口。

## 边界与兼容

确定性场景与真实行为场景的边界是强约束。执行真实 Provider、Model、Session、Project 或业务写入的行为必须留在正式界面；Lab 只承载 fixture。**一个场景不能因为在 Lab 中更容易演示，就被改写成 fixture。**

组件的行为合同、能力标签与耦合约束由[`组件规范`](../../standards/code/components.md)拥有，Lab 不建立第二套组件分类。各产品领域拥有正式界面及其接口、项目、会话、共享状态与持久化语义，Lab 不取得任何产品数据的所有权。

Lab 消费的组件索引是派生产物，由扫描组件声明生成。Lab 不维护索引，不在索引中补写声明之外的信息，不保留第二份组件或场景清单。

Lab 落地本身不授权删除任何既有的 preview 页面。既有 preview 的清退条件、组件迁移进度与场景归属属于对应的重构工作，不属于本规范。

## 验收与 Smoke

1. Given 可分发产物的构建，When 检查路由、模块图、文本与清单，Then 不存在 Lab 模块、fixture、开发绝对路径或识别标记，访问 Lab 路径为不存在。
2. Given 一个 fixture 尝试访问真实接口、持久化状态或 Provider/Model，When 场景加载，Then 失败即拒绝并报错，不产生看似成功的结果。
3. Given 同一个场景，When 重复打开或执行重置，Then 初始输入与可观察状态一致，不依赖网络、真实用户数据或持久化状态。
4. Given 含 `io:` 或 `state:shared-write` 标签的组件，When 在 Lab 中查询，Then 显示只能在正式界面验证，且 Lab 不为其挂载任何 fixture。
5. Given 手机与平板容器，When 分别切换到 `390×844` 与 `768×1024`，Then 场景语义不变，核心操作可完成，无页面级横向滚动或控件相互遮挡。
6. Given Lab 中的任一场景执行完允许的动作，When 检查真实接口、项目、会话、Provider/Model 与浏览器持久化，Then 均无副作用；Lab 自身界面偏好的那一个键不计入，且清除该键不改变任何场景的表现。

## 证据

- 批准依据：开发者明确批准闭合 `t01-migration-design` 并创建本 `planned` Spec；随后明确要求不创建组件索引规范，把组件行为合同移入编码规范，并把 Lab 的界面形态推迟到原型建成之后再规定。
- 有意保留的缺口：Lab 的导航、检视面板、组件索引形状、场景交互与失败呈现尚未规定。它们在 Lab 原型建成、并有若干组件真实迁移通过之后补写；在此之前对这些方面的任何描述都是猜测，不写入本规范。
- 组件合同与能力标签：[`组件规范`](../../standards/code/components.md)。
- 既有 preview 页面与场景的现状基线：[preview-scenario-baseline.json](../../../.agents/works/w00003-neurobook-ui-foundation-migration/tasks/t01-migration-design/evidences/preview-scenario-baseline.json)。该文件是一次具体重构的基线，记录当前 14 个 preview 源与 31 个场景的标识与分类，不是本规范的合规结论；其中的场景来源使用 `app/pages/*preview.vue`，窄化为 `*.preview.vue` 会漏掉 `world-engine.workbench-preview.vue`。
- 重构进度与 preview 清退：[w00003 NeuroBook UI 底座迁移](../../../.agents/works/w00003-neurobook-ui-foundation-migration/README.md)。
- 本规范尚未实现。Lab、fixture、产物排除检查、浏览器与构建证据均不因本规范处于 `planned` 而存在或通过。
