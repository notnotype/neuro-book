---
schema: nbook.spec/v1
kind: behavior
status: planned
capability: ui.workbench-shell
owners:
  - ui
---

# Workbench 外壳接入

## 目标与非目标

**目标**：NeuroBook 主应用的外壳——标题栏、图标栏（活动栏）、主侧栏、右侧栏、编辑器区、面板与状态栏——按已批准的 Workbench 分区模型渲染：容器与视图由 descriptor 描述，图标栏按容器切换，侧栏可收起/展开，外壳几何经唯一尺寸边界提交并可按版本化快照恢复。阶段 1 的边界是**外壳接入但保留现有固定槽位**：现有入口一个不删、行为不减，新外壳与今天的固定槽位并存。

**非目标**：

- 不迁移 `files` / `characters` / `plot` 等消费者——逐个切换 descriptor 属后续阶段；
- 不删除任何固定入口、旧组件与旧状态字段；
- 不改变 Project、Session、文件、History、Job 的数据 authority；
- 不做编辑器分屏（多 Editor Group）、第三方可执行插件（L3）、跨窗口浮动与状态栏换位；
- 不建立第二套 resize 边界或第二套布局存储；
- 不覆盖 Workbench 的后续能力（注册表对外开放、内置插件注册路径、命令与只读描述快照桥接）——它们各自另有可独立验收的 Spec。

## 术语与参与者

- **Part**：工作台的大区域（标题栏、图标栏、主侧栏、右侧栏、编辑器区、面板、状态栏），拥有区域级几何与可见性。
- **图标栏（活动栏）**：图标入口条；图标项分两类——容器切换项与命令项。
- **视图容器**：一组视图的归属单位，也是图标栏上的可切换项，拥有容器级状态。
- **视图**：一项能力的描述与内容面板，由 descriptor 描述（稳定标识、标题 key、图标、`layout: scroll | fill`、可见性条件、所需 authority、容器归属、顺序与工厂键）；描述与内容分离，注册表不持组件。
- **现有固定槽位**：今天主页面写死的入口区域与入口实现（写死的图标栏列表、左右栏模板、编辑器区模板、面板与状态栏）。
- **主题宿主**：产品发布主题变量与 nb-ui 角色变量的 DOM 宿主；浮层经显式宿主声明接入（默认宿主的长期取舍由 [`nb-ui-overlay-portal-host.md`](../../proposals/nb-ui-overlay-portal-host.md) 跟踪，尚未拍板，不在本规范内）。
- **布局快照**：用户级布局状态（区域尺寸、可见性、容器与视图位置覆盖）的版本化表示。
- **宿主**：持有注册表、布局读写与主题的调用方（主应用外壳根）；视图只消费渲染模型、只发出结构化意图。
- **参与者**：用户（操作外壳、容器与视图）；外壳宿主（渲染、持久化、诊断）；descriptor 注册表（描述来源）；可序列化拆分树原语与唯一 resize 边界（几何）。

## 输入与前置条件

- 触发方式：打开或重新进入主应用页面、打开或切换 Project、窗口尺寸变化，以及用户操作（点击图标栏、收起侧栏、拖拽 sash、拖动视图、切换面板）。
- 输入：descriptor 注册表（视图与容器的标识、标题、图标、`layout`、可见性条件、所需 authority、归属与顺序、工厂键）；布局快照（用户级本地键，可缺失、可损坏、可来自旧版本）；主题（浅色 / 深色 / 自定义）与主题宿主的存在性；窗口尺寸（桌面宽度与窄屏 `390 × 844`）。
- 前置状态：外壳根已挂载主题宿主；Project 是否打开只影响视图动作的可用性，不阻塞外壳渲染。
- 权限：无。外壳自身不读写领域数据，不直接触碰 Project 文件、Session、History 或 Job。

## 输出与可观察行为

- 外壳渲染出与接入前**等价的布局**：区域的种类、顺序与相对位置不退化，不出现双重边框、双重标题条或双重标签条。
- 图标栏：容器切换项有选中态；点击非活动项切换该位置显示的容器并使其可见；命令项保持打开原有入口的行为。
- 侧栏与面板：主侧栏、右侧栏与面板可收起；收起后图标栏仍在，重新点击可展开；图标栏、编辑器区与状态栏不可收起。
- 视图容器按位置呈现：侧栏容器以堆叠视图行呈现，面板容器以标签条呈现；视图行显示图标、标题、`layout` 徽标与三态（正常 / 错误 / 动作不可用）。
- 编辑器区：单组标签条 + 内容区，不引入分屏。
- 状态栏：显示当前视图与容器计数，以及布局摘要。
- **现有入口全部可用**：文件树、角色、情节、设置、Agent 面板与侧栏、历史、World Engine 等今天的固定入口保持可打开、可切换、可关闭，行为与外壳接入前一致（阶段 1 不迁移它们的实现）。
- **主题跟随**：外壳与其浮层的颜色来自主题宿主发布的变量；在非 sepia 的浅色与深色主题下都不出现 `:root` 兜底底色。
- 布局恢复：外壳几何与可见性变化进入布局快照，重新进入时可恢复；恢复受尺寸边界夹取，与拖拽行为一致。

## 状态与转换

布局状态分四类，外加一类排除项：

| 层级 | 内容 | 作用域与键 |
|---|---|---|
| 布局 | 区域尺寸与可见性、容器位置与顺序、视图位置覆盖、面板位置与对齐 | 用户级：版本化布局快照与位置覆盖 |
| 编辑器会话 | 打开的标签与缓冲（不是布局） | 沿用既有项目级键，本期不动 |
| 视图自身 | 展开项、筛选等视图内 UI 状态 | 按视图声明的 `stateScope` 落层 |
| Session / 页面级 | 当前活动项、临时展开态、Dialog 开关 | 组件内状态，页面销毁即释放 |
| （排除）领域数据 | Project 文件、Session、Job、Trace | 各自 authority，不进任何布局快照 |

状态转换：

| 初始状态 | 事件 | 下一状态 | 拒绝条件 |
|---|---|---|---|
| 无快照 | 打开外壳 | 默认布局 | 无 |
| 已恢复布局 | 切换容器 / 收起展开 / 拖拽尺寸 / 跨容器移动视图 | 更新后的布局并写回快照 | 目标容器不存在、落到自身当前位置、拖动数据不是视图 → 无操作且快照不变 |
| 快照版本不符 | 打开外壳 | 默认布局 + 诊断 | 不做部分恢复 |
| 快照引用未知容器或视图 | 打开外壳 | 丢弃该条覆盖 + 诊断，其余照常恢复 | 不整树回默认 |
| 恢复尺寸越界 | 打开外壳 | 夹取到该叶的最小/最大值 + 诊断 | 不拒绝整个快照 |

- 活动视图身份的恢复沿用现有用户级行为，本期不迁移既有用户数据；新旧键并存期内任一时刻只有一个写者。
- 同一次拖拽手势期间的重复尺寸提交以最后一次为准；收起/展开与容器切换幂等。

## 副作用与数据

- 持久化：只写用户级布局键（布局快照与视图位置覆盖）；不写领域数据，不新增第二套尺寸持久化。
- 既有键：`novel.ide.local` / `novel.ide.session` 等现有键本期不迁移、不双写；整体退役时单独开一次迁移（一次性读旧写新 + 保留一个版本的只读兼容）。
- 尺寸提交：所有尺寸仍经现有唯一 resize 边界提交，原语只做夹取与传播；不新开第二套拖拽实现。
- 视图副作用：视图只发出结构化意图；写入、dirty、save/discard、Session、Job 与 Project 文件仍归原 authority。
- 网络与文件：外壳自身无网络请求、无领域文件写入。

## 失败与恢复

- 布局快照损坏、版本不符或根结构非法：回落默认布局并给出诊断，不阻塞外壳打开；诊断逐条可枚举，不静默吞掉。
- 快照引用未知容器、未知视图或非法尺寸：丢弃对应条目（或夹取尺寸）并逐条给出 issue，其余布局照常恢复。
- descriptor 或工厂失败：以**视图级 issue** 呈现该视图的错误与重试入口，不遮蔽其它视图、不拖垮外壳。
- 上下文不可用（Project 未打开、authority 缺失）：视图仍可见，动作不可执行并给出原因；可见性不等于权限。
- 目标切换后到达的迟到异步结果：标记为 superseded，不覆盖当前目标、不触发全局错误屏；目标切换本身失败表达为「当前目标无数据」，不沿用上一目标的数据。
- 主题宿主缺失或未挂载：不阻塞外壳渲染，但必须在诊断中可见，不得把兜底变量当作主题静默通过。

## 边界与兼容

- 归属：主应用 UI 层（owner `ui`）；不改变 nb-ui 公共组件契约，拆分树原语只被消费、不被本能力修改。
- 公开接口：本能力不承诺对外 API；descriptor 形状、布局键名与恢复规则属内部合同，升级走版本化快照。
- 主题与浮层接入沿用现有 nb-ui 通道（宿主上的角色变量桥接 + 浮层显式宿主声明），不新增第二套主题或浮层接入通道。
- 兼容：现有固定槽位、旧组件与旧主题 authority 在阶段 1 全部保留；删除发生在后续阶段，且必须逐项满足提案的删除门禁（入口闭环、行为等价证据、生命周期安全、owner 迁移、单 Editor Group 不变）。
- 既有验收不回退：nb-ui 组件与布局原语的合同测试、验证台的交互验收继续有效，不因外壳接入放宽。
- 窄屏：窄屏（`390 × 844`）与桌面共享同一模型；窄屏下不产生页面级横向滚动。

## 验收与 Smoke

1. Given 接入前的当前主页面，When 打开接入外壳后的页面，Then 区域种类、顺序与相对位置等价（标题栏 / 图标栏 / 主侧栏 / 编辑器区 / 右侧栏 / 面板 / 状态栏），无双重边框、双重标题条或双重标签条。
2. Given 现有固定入口清单（文件树、角色、情节、设置、Agent 面板与侧栏、历史、World Engine 等），When 逐项打开、切换、关闭，Then 行为与接入前一致，且没有入口被移除或降级为占位。
3. Given 浅色主题与深色主题（各含一次自定义主题），When 对照外壳与其中打开浮层的截图，Then 颜色来自主题宿主发布的变量，深色下无 sepia 兜底底色。
4. Given 图标栏与侧栏，When 点击非活动容器项、点击活动容器项、再点击展开，Then 容器切换与收起/展开生效：收起时图标栏仍在、编辑器区占满，展开后尺寸恢复到收起前（±2px 级）。
5. Given 尺寸拖拽，When 拖过该叶的最小/最大值，Then 尺寸停在夹取边界且不弹回；When 随后重新进入页面，Then 布局从快照恢复。
6. Given 快照被注入版本不符、未知容器引用与非法尺寸，When 打开外壳，Then 逐条给出诊断并按规则回落/丢弃，不出现空白外壳或未捕获异常。
7. Given 桌面宽度与 `390 × 844` 窄屏，When 完成核心操作（切换容器、收起展开、打开现有入口、切换主题），Then 操作可完成且无页面级横向滚动。
8. Given 原语与验证台的既有验收，When 运行 `bun run --cwd packages/nb-ui test`，Then 全部通过（最近基线 279）；When 在 Source Dev 打开 `/workbench-spike` 复核四条交互验收（观感、sash 可发现可拖、视图跨容器拖拽、活动栏与收起），Then 不回退（最近实测 34 条断言全部通过）。

Smoke 入口：Source Dev 打开主应用页面（桌面与 `390 × 844` 视口各一次）；`/workbench-spike` 复核原语交互；`bun run --cwd packages/nb-ui test` 复核组件与布局合同。

## 实现合同

尚未实现。

## 证据

- 批准依据（目标与阶段 1 口径）：[`workbench-view-host.md`](../../../packages/neuro-book/docs/proposals/workbench-view-host.md)——2026-09-13 开发者批准（`accepted`）；阶段 1 定义为「原语 + 测试矩阵 + 新 Lab 验证台；外壳接入但**保留现有槽位**，不删任何固定入口」，同文件「验证台验收（2026-09-13）」记录验证台四条验收已达成。
- 关联工作入口：[#192 建立类 VS Code 的 Workbench 与 View Host 抽象](https://github.com/notnotype/neuro-book/issues/192)（远端 Issue，本文不复制其正文）。
- 实现与验收证据待实现闭合后补录。
