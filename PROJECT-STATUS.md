# Project Status

## Summary

neuro-book 当前处于快速开发阶段。本轮产品主路径收敛到 **novel 写作模式 v1**：作者围绕 Markdown Studio、Project Workspace、Agent、World Engine 和 Scene-only Plot 工作台写作；旧 RAG subject 面板 / RP / simulation 默认模板先从普通入口隐藏，底层代码和历史资料保留。写作提示词主链已收口为 `灵感探索 → 项目/Lorebook → World Engine 初始化 → 08 剧情规划/状态推进 → 09 章节写作 → 写后回补/修订`。

## Product / Workspace Facts

- 部署代码已收口到独立包 `@notnotype/neuro-book-manager`，公开命令是 `neuro-book`；实例发现、离线检查、导入和Source接管使用独立Module，无参数入口按当前目录切换实例管理、损坏实例处理、Git checkout接管和新部署菜单，blessed TUI同时展示已注册与待导入/接管候选。用户级配置只保存偏好、默认实例、目录索引和有限搜索根，部署真相源仍是各Installation Root的`.deploy/installation.json`。
- 推荐顺序为 Windows Portable、GHCR、Product Bun、Source Dev/Source Product、Source Docker。Installation Manifest v5 固定记录 Source/Product/Manager/Manager Runtime/Application Runtime/Tools、容器 engine 与 State/Cache/Desktop/WebView typed locators；Release Manifest v5 用统一 build ID 连接 Source、五平台 Product、Windows Portable、GHCR digest 与 `stateMigration` 声明。Windows Portable zip 包含根源码、verified `.output`、managed Bun/rg/PortableGit/bash、版本化 Manager 和 `data/` State Root。
- `NEURO_BOOK_STATE_ROOT` 与 `NEURO_BOOK_CACHE_ROOT` 分别是用户状态与可重建缓存的路径真相源。Installed Windows 使用 `%LOCALAPPDATA%/NeuroBook/{data,cache,desktop}`，Windows Portable 使用 `data/` 与 `.cache/`；manifest 只保存 `installation-root` / `local-app-data` 相对 locator。Workspace Root、Boot Config、Product `.env`、SQLite 与日志从 State Root 解析，图片变体、llmlint cache、Bun install cache 和 Bash 完整输出从 Cache Root 解析。
- Product Runtime Image 已收敛为 Source-locked verified candidate：Builder 只生成候选，共享只读 Verifier 独立复算 manifest/ready/contract、canonical policy、owner inventory 与 payload digest；Manager 是受管 Installation Root 的唯一激活 owner，本地 checkout 由窄 Publisher 原子更新 `.output`。Manager start/admin/migration/recovery、Release archive、Portable、Container 与 Agent CLI 统一消费 Product Runtime Contract v4 和 verified execution identity；Product bootstrap 在解析命令前再次完整自验并删除 `NODE_PATH`。status/discovery 仍使用 4-9 ms 的只读控制面验证；已安装旧 Product 的 Manager 读取仍可显式兼容 v3，新候选、发布与 Product bootstrap 不接受 v3。
- Windows x64 当前 clean Product（source revision `4947c1e4ab7643c71d875bae4083a56412f06ee3`）为 3,237 个 payload 文件、133,931,826 bytes，`imageId=sha256:cfe3eb79b1087b86944e3240cbf4b52827f3434a862b0d6b7676001af920b196`，`sourceDigest=sha256:b52012b3427203e8976bbfcf1a4944731156240ea2421550781b656436409f25`，`dirty=false`。本地 Product archive 为 41,885,467 bytes，SHA-256 `990F0EF3ECD9F2142FA6B8BE0AE2CB204BE743E4391953A6AFCB7179B8F5DABA`；解压到仓库外后已通过完整 Windows verifier（含 v4 的 `world-engine-config`）、migration、Profile/Variable、SQLite、Sharp、Workspace CLI、HTTP/shutdown 和 State Root move/delete。它是基于该 clean revision 的本地验收资产，不是正式五平台 Release archive；Linux/macOS owner baseline、正式五平台 Candidate、真实 Docker/Podman、Portable 最终交叉身份、浏览器人工验收和 Tauri/Electron spike 仍未完成。
- 2026-08-03 发行前治理已收口到四个边界：Profile/Variable 共用显式 Source/Product Authoring Context 与完整模块图策略；Native/Container/Source Dev 通过 Verified Application Execution；Manager mutation 使用外置 heartbeat lease、锁内 Manifest 重读、Operation `applying` checkpoint 与可恢复 Product 切换；Release 只创建按 release ID 隔离的 Draft Candidate，全部 gate 后才公开并由独立 job 激活 OCI 正式别名。Manager `.49` 已通过 npm provenance、真实bunx与公开输入验证；`.48` 因 clean checkout 未登记 `shared/product-runtime-shutdown.ts` 而在发布前失败，两个 tag 均保留且不复用。clean Windows Source/Product/Portable归档、完整Product/Manager HTTP smoke和默认保留`data`/全量删除两种外置Host卸载均已完成。Release Coordinator已吸收GitHub Draft创建后的短暂列表一致性窗口，仍对重复或identity漂移fail closed。首个已dispatch的0.9 Draft被四个未登记平台policy的preflight正确拒绝；baseline workflow现对五平台执行同Source A/B measurement，审查并登记policy后才能重建候选。正式五平台Candidate Actions、真实Docker/rootless Podman、最终Portable Verifier和浏览器人工验收仍不能由本地证据替代。
- 鉴权属于 State Root `config.yaml` 的 Boot Config：服务器 Profile 默认开启，Windows Portable 默认关闭；`Create Admin.cmd` 通过 Manager 调用 Product 管理员脚本。管理员 API 统一走同一访问守卫；关闭时管理员接口也无鉴权。配置中心只读展示当前进程状态，不提供热写。
- GHCR 安装不在宿主机 clone 源码；Manager 生成 `.deploy/docker-compose.generated.yml` 并拉取 Release Manifest 固定 digest 的镜像。镜像 `/app` 携带完整源码和 `.output`，Runtime/Tool provider 记录为 `container`。
- Product Application Root按只读资产处理。Profile/Variable运行时只验证内置artifact，用户编译、同步与动态import cache统一写入State Root的`workspace/.nbook/agent/.staging/`；Docker Compose挂载State Root `.env`，不通过root用户或修改`/app`权限换取可写性。
- `docker-compose.yml` 端口映射为 `${NUXT_PORT}:${NUXT_PORT}`（容器内按 Boot Config 监听 NUXT_PORT）；旧的 `${NUXT_PORT}:3000` 映射在非 3000 端口下不通，已修复。
- Windows Product Portable 错误报告日志落在 `data/logs/`；后端提供 `/api/app/logs/status` 与 `/api/app/logs/download`，日志包只包含日志和 manifest，不包含 config、数据库、workspace 正文、绝对日志目录或进程 `cwd`。
- Passport/Backup 访问唯一官网时统一经过 official-site transport：DNS/连接/TLS/超时返回稳定 502 `passport_site_unreachable`，上游 5xx 返回 `passport_site_unavailable`，业务 4xx 保持原语义；失败摘要进入既有 App JSONL 且不记录 token/body。Node 24 环境代理必须在进程启动前用 `NODE_USE_ENV_PROXY=1` 配置。
- Passport 设备码兑换与 refresh 轮换遵循“本地凭据提交成功后才更新授权状态/cache”：写入失败会终止会话、best-effort 吊销新 grant、清除旧凭据并要求重新关联；前端只在 pending 自动轮询，网络失败会保留设备码并暂停供手动重试。完整 grant、deviceCode 和 token 不进入日志或界面。
- Application State catalog v3 统一编排 `app-sqlite → agent-attachment-v1 → agent-session-v2 → agent-session-v2-review-repair`。Product migration-only registry 支持旧 catalog resume/rollback，v3 journal 保存原始 sentinel，顶层 lease 串行化 Manager 与手工 CLI。Manager 先用候选 Product plan，只有候选通过目标版本与健康检查后才提交 Operation Journal v5；container候选id在健康检查前写入durable effect，native由Windows Job/POSIX supervisor在宿主断连时收口；Nitro只读检查current complete sentinel。
- Windows Portable `start --no-health-check` 只在 migration plan 为 `already_current` 时允许；存在状态升级必须完成目标版本与健康检查。其他 Profile 始终拒绝该参数。
- 数据库已硬切 SQLite-only：App SQLite默认位于 State Root的`workspace/.nbook/neuro-book.sqlite`；Project SQLite 位于每个 Project Workspace 的 `.nbook/project.sqlite`。App SQLite Location统一服务Product Runtime、Prisma CLI、Manager备份与Docker：相对`file:`值必须留在State Root，原生Profile可明确使用外部绝对文件，Docker拒绝未建模的外部数据库；配置继续保存可迁移逻辑URL，不受Product cwd影响。
- Project Workspace 根目录 `project.yaml` 是项目身份与轻量展示 metadata 的真相源，App SQLite 不维护 Project index 或 `Novel` mapping。可选 `cover` 只引用 Project Workspace 内 PNG/JPEG/WebP；列表不探测图片文件，浏览器通过只接收 `projectRoot` 的专用端点懒加载 WebP 封面变体。封面原图保持原始 bytes，上传目标由服务端内容寻址决定。
- 2026-08-01 起，调用 `openProjectForTest()` 的测试必须显式持有隔离 Runtime Workspace Root；SQL、World Engine、CodeAct、Plot API 与相关 Profile 测试不再写源码仓库真实 Workspace Root。六个高风险文件保持文件并行并连续三轮达到 `6/6 files`、`95/95 tests`，每轮前后真实根测试 Project 与锁 metadata 零新增；Preview 已删除测试名前缀过滤。fixture 清理现把 owner marker 留到最后，部分删除失败不再制造 sweep 无法认领的目录。本轮四个已授权 Project 残留、一组 owner 死亡的 stale lock 和四个本轮临时 fixture 已精确清理，另四个未授权测试目录保持不动。详见 [Task 125 Round 04](docs/tasks/125-runtime-artifact-storage-lifecycle/walkthroughs/round-04-workspace-test-isolation.md)。
- Project Workspace File Index 使用弱一致扫描：扫描期间消失的路径仅在 `ENOENT` 时从本轮 snapshot 忽略，权限、越界与其他 I/O 错误继续失败。Project 下载在 OS 临时区对 Project SQLite 和已有 History SQLite 分别生成独立在线 snapshot，不复制 live WAL/SHM；History SQLite 可能包含全文与已删除内容，前端会提示分享隐私风险。
- Boot Config 位于 State Root 的 `config.yaml`，包含启动/部署字段与 `auth.enabled`；开发环境缺省免密、生产环境缺省开启，修改后需重启。Global Config 位于 State Root 的 `workspace/.nbook/config.json`，Project 覆盖位于 `workspace/{project}/.nbook/config.json`；这些运行态文件不进 Git。
- 默认 Project 模板保留 `manuscript/`、`lorebook/`、`agents/`、`manual/`、`reference/`、`world-engine/` 等写作骨架。
- Source Dev 当前通过 Bun `patchedDependencies` 临时修复 `nitropack@2.13.4` 的 dev source-map consumer 生命周期：同一错误按 bundle 复用 consumer、解析失败保留原始栈帧、结束后统一释放；该补丁只治理错误页的重复解析与 `unreachable` 二次故障，不改变原始业务错误或 Product Runtime。真实 13.4 MB map 的 20 错误 × 8 栈帧黑盒已通过，损坏 map 降级也通过；上游跟踪为 [nitrojs/nitro#4454](https://github.com/nitrojs/nitro/pull/4454)。
- Source Dev 公开入口现由 Owned Process launcher 持有完整准备/Nuxt进程树，内部 `dev:runtime` 只供已持有 owner 的 Manager 调用。首次信号走认证 Product shutdown，第二次信号或失败进入强制收口；默认 IPv4 loopback，显式 localhost/IPv6 loopback保持地址族一致。Agent Session Store runtime/migration lease 统一输出稳定 `ELOCKED` 诊断与最小 owner metadata，仍严格使用15秒heartbeat/30秒stale，不按PID自动抢锁或杀进程。Windows真实fixture与隔离State Root两次完整Source Dev启动已通过；可见终端关闭仍待Task 117人工候选验收。
- 不同 State Root 实例复用同一浏览器 origin 时，旧 Session ID 现在稳定返回 `404 SESSION_NOT_FOUND`，不再暴露磁盘 `ENOENT`。主 Agent 面板会在当前 scope 内刷新一次并有界切换到有效 Session；备份 Session 仍不参与在线枚举，history/System Prompt 消失时只显示局部错误，不静默切换上下文。

## Writing Mode v1

- 普通写作入口以 Novel IDE / Markdown Studio 为主，顶栏保留 Bookshelf / World / Plot / User Assets / Agent。
- 顶栏重新暴露 Plot Workbench，且小屏下保持在视口内；RAG Inspector 仍不暴露，欢迎页仍不暴露 Plot / RAG / simulation 快捷入口。
- 左侧侧栏保留 Files / Characters / Plot；Agent 模式侧栏保留 Sessions，并提供 Plot 快捷入口用于切回 IDE Plot 面板；Outline 和 RAG 面板入口隐藏，`NovelPlotPanel` / `NovelRagPanel` 底层组件保留。
- Agent 新建菜单隐藏 `rp.leader` 和 `simulator.leader`；历史 session 的 profile 名称、图标和旧 profile 文件保留。
- Agent 关联生命周期已收口：link/detach 继续作为 append-only 历史账本保存，关系索引维护 archived 集合并为 Runtime、Profile、工具、snapshot、relations API 和关联面板提供同一 effective relation view。单进程关系变更队列串行 create/link、detach、archive、restore；archive/restore 只写自身状态，不跨 Session 改写关系账本。归档隐藏全部入站/出站关系，恢复后仍 linked 的关系自动重新显现；归档期间显式 detach 的关系不会恢复。
- Agent Composer 已接通 Session 图片附件：拖拽、文件选择、粘贴与 `@` 快照统一写入有序图片 Markdown；头部附件面板覆盖全部 Session 分支并支持搜索、分页和重新插入。聊天与附件面板通过共享 Image Variant Module 懒加载 WebP，点击后才请求原图；Chat 中的非图片 Attachment 展示 metadata 并只下载授权原件，Provider 与持久化仍只消费原图。
- 消息气泡的分支切换器基于**对话锚点条件化投影**，不再直接消费原始 entry 树的 `childCount > 1`。服务端 `chatEntryKind()` 是「该 entry 会渲染成哪种 Chat Flow 气泡」的唯一判据，同时作为 `projectAgentChatEntry` 的唯一 null 出口并下发到 `SessionTreeNode.chatEntry`（取代已删除的 `messageId`）；前端只把 user / assistant / 运行报错当作分支锚点，`invocation_lifecycle`、`model_change`、`custom(agent.link.*)` 等运行期记账 entry 一律透明。因此重试、换模型重试和报错重跑产生的分支能正常切换，`agent.link` 造成的假分支（切过去会截断对话）消失。Session Tree 对话框刻意保留 raw 语义作为审计视图，Task 49「气泡与对话框同一套 branch 语义」已被显式推翻，详见 [Task 138](docs/tasks/138-agent-conversation-branch-projection/README.md)。
- 消息工具条的「回退」已更名为「从这里分叉」（`moveTree(position:"at")`，只移动 active leaf，不删除历史）。`/fork` slash command 以同 Profile 另开新会话并用 `fork.from` custom entry 记录出处；它刻意不占用 `parentSessionId`（那是子 Agent 关系，占用会让 fork 从顶层会话列表消失），也**不复制历史**，新会话是空的。
- 默认写作 workflow 中，`08` 是 World Engine 剧情事实确认与状态推进手册，`09` 只在状态已推进后调用 writer 写正文；修订产生新事实时回到 `08` 做 World Engine 回补。
- Markdown Studio 的 Inline AI PromptBar 使用 `inline.editor` 后台 session：发送后不自动打开右侧 Agent 面板、不切换主 Agent active session；PromptBar 内可选择当前 Project 的 Inline AI sessions、独立新建 session、原地调整 inline session 模型，并流式展示当前 session 的思考、正文、edit/write 预览与最终摘要。
- 默认 Project 模板的 `agents/` 只创建 `leader.default/` 与 `writer/` 上下文，不再默认创建 `director`、`rp.leader`、`rp.writer` 或 `simulator.leader`。
- 本版本目标是先把写作模式体验打顺，RP 模式后续再恢复和重新设计入口。

## World Engine

- World Engine 是写作模式的动态世界状态与时间线真相源，用于替代旧 Plot 系统和 `simulation/` 默认运行态。
- Plot System 已通过 Task 78 → Task 87 演进：Task 78 收敛为 Scene-only 桥接层，Task 87 升级为**两棵树**（承载树 Story→StoryAct→StoryChapter→Prose、因果树 Story→StoryPhase→StoryThread→StoryScene，在 `StoryScene.chapterId` 交汇）。`StoryPlot / Plot Beat` 不再是正式模型；`StoryScene` 持久化 `startInstant/endInstant/subjectIdsJson/locationSubjectId` 与 `chapterId` 外键，Scene 可通过服务端 API 查询相关 World Engine slices 和 subject states，并能从 Plot World Context 打开真实 World Engine Workbench。`StoryChapter` 带 ChapterBrief 字段组（章级 writer 指令），Chapter↔Prose 通过 manuscript frontmatter `chapter: <name>` 反指。Scene World Anchor 读取 DTO 会解析 `subjects/locationSubject/unresolvedSubjectIds`，允许占位 subject ID，但 UI/API/Agent 必须显式暴露 unresolved 状态。
- 后端核心是 Project SQLite 三表：`WorldSubject`、`WorldSlice`、`WorldPatch`；公开写入已硬切为 `patches` + 4-op（`replace` / `increment` / `remove` / `append`），collection 支持 `remove + value` 按 stable JSON 值删除元素，patch 不存旧值字段，后端不自动改写后续切面。
- HTTP API 覆盖 schema、subjects、slices、slice delete、state/query；公开时间入参拒绝 raw instant 调试格式、首尾空白和非法 percent encoding。
- Agent 内置 World Engine 工具收敛为单一 `execute_world`：Leader / world.engine 使用 readwrite CodeAct，公共 API 为分组形态 `world.time.*`、`world.subject.*`、`world.search.*`、`world.slice.*`；writer 使用 readonly 模式，不注入 `world.slice.write/editPatches/delete`。旧的 8 个固定工具、Task 69 三工具、`codePath` 查询分支、旧 6-op 写入入口、旧平铺 `world.*` API 和 `world.getMany` 不再保留。
- 默认 Project 模板包含 `world-engine/schema/index.ts`（**Zod schema，硬切，不再支持 `schema.yaml`**）和 `world-engine/calendar.ts`，新 Project 默认 Calendar 已切到 Gregorian 现实日历，format 到分钟、不带秒；新 Project 不再默认生成 `simulation/`。Product Authoring Kit 会自带 World Engine schema helper 与 Zod runtime；Product 编译只允许登记的 `zod` / `nbook/world-engine/schema`，最终 schema artifact 必须内联二者，不能从根 `node_modules` 或 `NODE_PATH` 解析。
- World Engine reference 与 writing workflow 已对齐当前协议：Agent 统一走 `execute_world`，脚本只 return 数据，issues 由运行时 collector 汇总；查询状态时优先在 CodeAct 内把已知 schema 的 attrs 转成文本摘要并 `return string`，工具文本会直接展示该摘要，结构化 `{data, issues}` 仍保留在 details；`world.subject.get()` 返回 attrs 本体；时间在沙箱内用 `world.time.parse/format` 和 instant bigint 互转，读写路径统一 JSON Pointer，同一 instant 只能有一个 slice，同刻多变更必须合并到同一个 `patches` 数组；误写单条 patch 或向已有 slice 补已登记 subject 时优先用 `world.slice.editPatches` 精确修正，冲突错误会返回 `existingSliceId/time/title` 并引导合并 patches；按 subject 查相关切面走 `world.slice.list({subjectIds})`，`world.slice.get` 只接受 sliceId。旧 API 名称仅保留在 migration / legacy / historical research 语境。
- Calendar 已硬切到 `calendar.ts`，不再兼容 `calendar.yaml`；支持 `simple`、`gregorian`、`custom` 三类策略，缺少 `calendar.ts` 时应提示创建；Gregorian calendar 已覆盖公元前年份与默认现实时间 `公元2020年4月12日 18:00` 的 parse/format 往返。`calendar.ts` 与 `schema/index.ts` 是单文件配置入口：入口内容变化可热加载，本地文件、绝对路径和 URL/protocol import/export 会被拒绝，包级 import 与 `node:` 内置模块继续允许；loader 会先把入口转译为内容 hash `.mjs`，再通过 server 原生动态 import seam 与物理 artifact cache 加载，不依赖宿主直接 import TypeScript，也避免 Product bundle resolver 或 Project 目录临时文件影响运行时生成模块导入。
- EmbeddingText 容器初始化死锁已修复：空 `replace /events []` / `replace /memory {}` 可作为 default 初始化基准，非空 embedding 内容仍必须按单条 patch 写入，错误信息会提示 `{text:"..."}` 写法；`vector/model` 由系统维护，显式字段和内部路径绕过都会被拒绝。
- Round 423 已用临时 Project 验证默认模板 API 链路：`calendar.ts` 时间格式下创建 `world/player`、写入 slice、查询 state、删除 slice 和状态回退均通过且 issues 为 0。
- Round 424 已用临时 Project 验证主 IDE Workbench 空项目第一步：默认模板 Project 可打开 Workbench，看到 schema/calendar 入口与创建入口，`创建 world subject` 会真实写入 `world` subject 和 init slice；临时 Project 已清理。
- Round 425 已完成阶段收尾审计：Round 380-424 的真实项目、新 Project、默认模板、连续推演、常用操作和 Calendar 证据足以证明当前“前后端雏形拼接 + 作者视角主路径”阶段已跑通；后续进入体验打磨和新产品决策。
- 主 IDE World Engine Workbench 支持创建 subject、写入 / 编辑 / 删除 slice、查询 state、展示 issues，并能从历史 `simulation/subjects` 发现真实主体系统摘要；该发现路径不代表 `simulation/` 是写作模式默认状态源。Workbench 已移除“示例世界”入口，SliceCard 主画布按 `path / op label / summary` 展示 patch。

## Hidden Legacy Systems

- RAG subject 面板、RAG Inspector、RP profiles、simulation workflow 和 archived simulation 模板都保留在仓库中，方便历史项目和后续恢复；Plot System 已在普通写作入口恢复为工作台（Task 87 后为两棵树：承载树 Act/Chapter/Prose + 因果树 Phase/Thread/Scene）。
- `novel-workflow-05-emulation-bootstrap` 与 `novel-workflow-06-emulation-tick` 已归档到 `docs/archived/skills/`（2026-07-26 写作 skill 体系重组），不再进入 skill catalog；legacy RP / simulation 项目需要时按归档文档手动执行。
- PLAN-E 已移除旧的自动记忆旁路机制；`simulator.actor` 不再自动执行记忆检索或保存。Subject RAG 数据、索引与工具仍保留，但当前没有内置自动记忆消费者；RP 自动记忆需以后续 workflow/job 显式重建。
- `workspace/ming-ding-zhi-shi-2` 已标注为 legacy RP / SillyTavern 导入项目，可作为复杂 schema/calendar 参考，但不是普通写作模式 World Engine 端到端验收样本。
- `assets/workspace/.nbook/templates/project-directory-templates/simulation/` 已归档到 `assets/workspace/.nbook/templates/archived/project-directory-templates/simulation/`。
- 新建 Project 不复制根目录 `simulation/`；真实用户 Project 中已有的 `simulation/` 数据不迁移、不删除。
- 用户 assets 同步会清理未手改的旧 `templates/project-directory-templates/simulation/**` 受管副本；手改副本按现有冲突规则保留。

## Recent Tasks

### 2026-08-01 状态更正：llmlint 3.0.0 与 Skill 依赖生命周期

- Task 130 双 clean build 完成后已解除写入冻结；内置 llmlint runtime 从 sibling source 两级同步到 vendored snapshot 与当前 user runtime，最终同步均为 no-op。三处各 122 个文件，两段逐文件 SHA-256 差异均为 0。当前规则为 360 total / 266 active，scope 分布 `all=240 / narrative=24 / quoted=2`。
- `HARD_CUT_DELETED_MANAGED_SYSTEM_ASSET_PREFIXES` 只清已废弃受管目录，不包含 `node_modules`。版本、提示词、规则或源码更新保留 Skill 本地依赖；只有 `package.json` 安装字段或 `bun.lock` 变化才失效。聚焦链路 2 passed / 70 skipped。
- 隔离首次使用先 frozen install，再运行 `status`，得到 `3.0.0 / initialized=false / fragments + auto / login=none`。真实 user runtime 本轮同步前没有 `node_modules`，因此真实同步只证明文件一致；依赖保留语义由隔离 sentinel 测试证明。

### 2026-07-31 状态更正：Project Catalog 与删除事务

- Task 118 的 Project Catalog 现在由 `useNovelIdeStore` 单点发布完整 snapshot；generation 与同代 single-flight 防止迟到 GET 覆盖 create/delete/cover mutation。revision 只用于同一进程内的连续增量发布，缺失、跳跃或乱序时回读完整 snapshot，服务重启后的较小 revision 也允许整体替换。
- 主 IDE 深链直接以 ProjectSession open 裁决 Project 是否存在，Catalog 刷新是 open 后的 best-effort 辅助。删除路由恢复为 `agent-active guard → close → Lifecycle delete → 按 committed 状态 best-effort 归档 Session`，正常返回 `{revision, projectRoot}`；旧的重复目录删除实现已移除。
- 本轮 Store/删除 17 tests、Picker/恢复/Session snapshot 5 files / 27 tests、Project/封面/Attachment/RFC 13 files / 150 passed / 1 skipped、`file-tools` 49/49 通过。相关在途错误已于 2026-08-01 收口，根 typecheck 最终全绿；本轮浏览器验收未自动执行。

### 2026-07-28 状态更正：Project 激活、Session v2 与 Application State

- Task 118 已完成 ProjectSessionController close-then-open：保存门禁通过后先释放旧 Project surface 与本标签页 presence，再等待目标 `open + presence_ready`；失败回 Picker，不恢复旧 Project。主 IDE、Workflow Preview、World Engine Preview 和 Workspace SSE 都只在 exact ready revision 后挂载数据面。普通切换不调用全局 Project close。用户实测发现并修复 presence 首帧死锁：H3 route 现在先启动 `eventStream.send()` 再推 `presence_ready`，首帧失败会关闭流并释放在场；localhost 已实测立即收到匹配首帧。
- Session runtime 已 hard cut 到 schema v2：仅持久化 `currentProjectRoot?`；`migrationReview` 只表示 `current_project_unresolved` 并与 Current Project 互斥。隔离数据中的三份误判由通用 repair step 修复，最新真实活动时间保持 2026-07-21 19:22:02；真正 unresolved Session 可在 Picker 重绑 Project 或改为 Workspace Root。Picker 重绑只校验 Lifecycle snapshot，不再错误要求目标 Project 已 ready；它不建立 presence/occupancy，invocation admission 仍捕获 exact ready generation。
- Application State catalog v3、Release Manifest v5 和 Manager Operation Journal v5 已落地。`applied` 是永久完成证明，曾 `skipped` 的动态 step 会重新 preflight；complete v3 可用新 runId 执行 repair-only v3 run。Source runner 已在隔离空 State Root 实跑 `plan → apply → already_current`，Application State/Session migration 4 files / 42 tests 通过；真实 `workspace/.nbook` 未修改。
- Manager install/adopt/update/start 已统一候选 plan、服务静止、外层 SQLite 备份、Product apply、launch ownership、目标版本健康检查和提交/反序恢复。此前阻断 Product 的 `profile-compile-worker.mjs` 绝对路径、`.bun` 路径和 package island 已由 Task 130 的 Authoring Kit、最终 bundle 与显式 island 合同消除；同源五代 dirty 可重复构建、仓库外命令/HTTP/认证 shutdown、State Root 释放和 Windows workflow 门禁均已通过。独立 `runtime:typecheck` 现自行生成 Prisma client；generated、scripts、shared 与 server/runtime 均有非 Nuxt tsconfig 边界，并直接持有 Bun/mdast 类型，不再依赖 ignored 生成物、偶然 hoist 或 `.nuxt`。相关 llmlint fixture 漂移已于 2026-08-01 收口，根 typecheck 最终全绿。正式 Release 仍只接受 clean runner，人工浏览器验收不因此视为完成。
- Task 109 的实际文件合同保持为：绑定 Project 时 cwd 是 Current Project Workspace，`workspace/<project>/<relative-path>` 是跨 Project 的正式输入地址；删除的是旧持久化 identity、旧 File Scope 联合及 Adapter，不是该地址语法。

| Task | Status | Notes |
| --- | --- | --- |
| [138 Agent 对话分支投影修复](docs/tasks/138-agent-conversation-branch-projection/README.md) | Merged via [#34](https://github.com/notnotype/neuro-book/pull/34)；浏览器验收待用户 | 消息气泡的分支切换器此前在真实数据上几乎必然不显示：每次 invoke 的首条 entry 必然是 `invocation_lifecycle: start`，抢占了分支根位置，504 个真实会话里由重试 / 编辑 / 报错重跑产生且能显示切换器的分支数为 **0**；同时 `custom(agent.link.*)` 记账 entry 被当成假分支，切过去会截断对话。修复引入**锚点条件化投影**：服务端 `chatEntryKind()` 成为「该 entry 渲染成哪种 Chat Flow 气泡」的唯一判据（同时是 `projectAgentChatEntry` 的唯一 null 出口），经 `SessionTreeNode.chatEntry` 下发（取代已删除的 `messageId`）；前端只认 user / assistant / 运行报错为分支锚点，运行期记账 entry 全部透明——`model_change` 因此天然不构成独立分支，无特例代码。真实数据回归 549 个会话产出 91 个切换器（assistant 49 / user 15 / 运行报错 27），记账 entry 成为 lane 的情况为 0。附带：「回退」更名「从这里分叉」（它本就只移动 active leaf，不删历史），新增 `/fork` slash command 并修掉 fork 误占 `parentSessionId` 导致新会话从顶层列表消失的问题。**显式推翻 Task 49「气泡与对话框同一套 branch 语义」**——Session Tree 对话框作为审计视图保留 raw 语义，一行未动。 |
| [135 Agent 资产安装协议](docs/tasks/135-agent-asset-install-protocol/README.md) | Protocol drafted + self-reviewed (2026-08-02)；协议本身未实施 | 资产更新模型换向：**内置 Skill / Workflow / Profile 不再是投影进 Workspace Root `.nbook` 的模板层，而是随程序附带、已下载好的包**，与创意工坊、git remote 走同一条安装事务。catalog 层级从 `system + user` 变为 **`user + project`**（Skill 因此新获项目级层），Seed Root 退出 catalog 只作安装源。新增 provenance 账本 `agent/installed.json` 记 `origin`（bundled / workshop / git / local）、版本、整包 `contentHash` 与依赖状态；整包 dirty 判定取代 per-file 三方比较，删除的内置包记 `removed` 墓碑不复活、可在设置页手动恢复。安装事务七阶段，账本写入是唯一提交点。Skill 对齐 [Agent Skills 开放标准](https://agentskills.io/specification)：frontmatter 成为身份/展示名/版本真相源，`package.json` 降级为可选，**id 与展示名分离**——目录名 = `name` = ASCII kebab id，中文名走 `metadata.displayName`。git 与工坊来源**当前全部信任放行**（owner-only 私有内测下的显式阶段性决定；第三方 Workflow 隔离威胁模型仍未完成）。**建档后做过一轮对抗式自审，查出并修订 8 处实质缺陷**，其中三处硬伤：①升级门槛只认版本，而实测 17 个内置 Skill 有 15 个、7 个内置 Workflow 全部没有任何版本声明，会导致它们首装后永久停更（改为版本缺失时回退 `contentHash` 比较）；②`restore` 被「版本必须更高」拦死，与「设置页恢复内置资产」的决策直接冲突（拆出 install/upgrade/repair/restore/takeover 五意图，版本门槛只约束 upgrade）；③Profile 的固定入口是共享 root 下的单个文件而非自包含目录，`.compiled/` 与 `manifest.json` 是 root 级共享状态，目录 rename 模型套不上（新增 Commit Unit 小节）。另修：账本无并发控制、账本丢失导致静默永久停更、跨来源同 id 冲突未定义、墓碑与显式安装交互未定义、frontmatter-only Skill 无处声明 `minAppVersion`。**存量冲突面已收口（2026-08-02）**：`skill-creator` 的 `quick_validate.py` 原白名单只放行 `name`/`description` 且拒绝任何缩进行，会直接判定新协议合法 Skill 不合格（已按标准重写，含失败注入验证）；`init_skill.py` 原示例在教「目录名 ≠ name 且 name 用中文」（已重写，`--name` 改 `--display-name`）；`skill-creator/SKILL.md` 四处、`reference/agent/workflow/README.md` 三层改两层、`profile-compiled-artifacts.md` Sync 取代提示、`TERMS.md` 新增 Seed Root / Install Root 术语均已同步。**校验器重写时实测暴露协议漏字段**：`when_to_use` 是 `SkillCatalog` 真实消费、6 个内置 Skill 在用的既有扩展，被我在重写 `skill-package.md` 时整个丢了，已补回。顺带修正文档站两处既有错误：Skill 的「项目层」在 `SkillCatalog` 里根本不存在（构造函数只接 system/user），内置 Skill 实际 17 个而非 16 个（漏了 Task 124 的 `novel-data`）。ADR 0011 已加修订块（Skill 身份读取位置变更，核心单一身份原则不变）。`docs:build` 通过无死链；未运行仓库单元测试（改动的两个 Python 脚本不在 Vitest 覆盖内）。未决项见任务 README：站点跟进 frontmatter 身份、恢复入口位置、依赖安装触发方、`RP模式` 改名与 `skill-creator-zh` 归档（两者均有活代码引用且需补墓碑条目，影响面已列全）、迁移挂载的 catalog 版本。 |
| [134 Agent Profile 设置面板二级化重构](docs/tasks/134-agent-profile-settings-navigation/README.md) | Implemented locally / 8 files 40 tests and root typecheck passed / Manual browser walkthrough pending | 「Agent Profile 模型」从 14 个 Profile 平铺长页重构为面板内 master-detail：左侧提供搜索、默认设置入口、编译状态、覆盖数、未保存标记和当前默认项，右侧只渲染当前 Profile，并按模型参数、运行策略和 Profile 设置分段。保存粒度、scope 继承、Dialog header expose 与服务端 DTO 保持不变；主面板从 1118 行收敛到 701 行。设置目录 8 files / 40 tests 与最终根 typecheck 全绿；浏览器走查尚未执行，后台编译完成覆盖当前未保存草稿仍是既有独立议题。 |
| [132 共享图片变体、Project 封面管理与原图预览](docs/tasks/132-shared-image-variants-project-covers/README.md) | Implemented locally / Repository-external Windows Product and Sharp smoke verified / Linux, macOS and GHCR CI pending / Manual browser acceptance pending | 原图所有权与授权 Adapter 边界不变；Cache 固定在 `Cache Root/image-variants/`。Catalog 由 Store 唯一发布，封面 mutation 成功后始终回读完整 Lifecycle snapshot，direct-open 不依赖列表预判，删除统一走 Lifecycle。raster image Module 统一 64 MP 解码上限，Agent 上传、快照、`read(image)`、Project 封面与变体共用错误识别；工具上下文不再暴露可旁路 Codec 的 Store。live 工具 Attachment 在 locator 到达前也显示不可用卡片，durable 后再启用预览或下载。封面与 create/delete 恢复按 Project 和 attempt 结算，迟到响应不能覆盖新记录。Settings/Catalog/direct-open/Preview/删除/ProjectSession 等最终合并回归 13 files / 77 tests 通过；相关在途错误已于 2026-08-01 收口，根 typecheck 最终全绿。 |
| [131 GitHub 贡献体系第一期](docs/tasks/131-github-contribution-system/README.md) | Implemented & Verified (2026-07-31；五类数字顺序 Issue Form、单一原生 Security 入口与 Actions 远端验收通过，Issue chooser 视觉人工验收待执行) | 建立中英文贡献指南、五类双语 Issue Form、PR 模板、安全政策、23 个标签真相源、维护者同步命令、社区静态校验和两条 PR 工作流；提示词与内置 Agent 资产表单自动带 `area: agent`，无法归类的公开问题有结构化兜底，安全漏洞只保留 GitHub 原生私密报告入口。审查修复补齐 Nuxt prepare、Code Baseline paths、master push 触发、Node 24、Issue 状态机、nullable 标签兼容和轻量文档 PR 入口；Community and Docs Checks [30620354426](https://github.com/notnotype/neuro-book/actions/runs/30620354426) 与 Deploy Docs [30620354444](https://github.com/notnotype/neuro-book/actions/runs/30620354444) 已在 GitHub Ubuntu Runner 通过。标签、#5/#6/#10/#12/#14/#15 分流、Private Vulnerability Reporting、Secret Scanning 和 Push Protection 均已远端核实。代码基线继续为 Advisory，既有 typecheck 26 处 llmlint fixture 漂移与全量 Vitest 超时待 #15 收口后再讨论 required check。 |
| [130 桌面应用前置架构、发行载荷与存储生命周期](docs/tasks/130-desktop-application-foundation/README.md) | Shared Product/lifecycle foundation implemented; Windows Product acceptance passed | 当前优先推荐 **Tauri Desktop Envelope + 独立 Bun Product**，最终技术选择仍等待同矩阵 spike。Runtime Image 已采用真实 ESM specifier、Nitro 单 bundle、shared command chunks、SDK-only Authoring Kit、System Assets projection 与显式 package islands；Profile/Variable 共用显式 Authoring Context，Manager/Release/Product bootstrap 共用 Verified Application Execution 与 Contract v4。当前 clean Windows Product（source revision `4947c1e4ab7643c71d875bae4083a56412f06ee3`）为 3,237 payload files / 133,931,826 bytes，imageId 为 `sha256:cfe3eb79b1087b86944e3240cbf4b52827f3434a862b0d6b7676001af920b196`，sourceDigest 为 `sha256:b52012b3427203e8976bbfcf1a4944731156240ea2421550781b656436409f25`；本地 Windows Product archive 为 41,885,467 bytes，SHA-256 `990F0EF3ECD9F2142FA6B8BE0AE2CB204BE743E4391953A6AFCB7179B8F5DABA`，解压到仓库外后已通过 Product Contract、migration、Profile/Variable、SQLite、Sharp、Workspace CLI、HTTP/shutdown、State Root move/delete 和包含 `world-engine-config` 的完整 Windows verifier。该归档是基于上述 clean revision 的本地验收资产，不是五平台正式 Release；正式五平台 Candidate、真实 Docker/rootless Podman、最终 Portable Verifier、浏览器人工验收、Tauri/Electron spike 和 Linux/macOS 实机 baseline 仍未完成。 |
| [129 项目选择界面与 Agent 入口收敛](docs/tasks/129-project-picker-and-session-entry/README.md) | Implemented locally / Focused browser acceptance complete / Destructive race matrix pending | 裸 `/` 保持未选择 Project；Header、Picker、前后退与深链共用 close-then-open transition。ProjectSession open 是深链存在性真相源；open 成功后文件树立即加载，Catalog 只做后台 best-effort 刷新，失败不会撤销 ready Project。Store mutation 成功后始终回读并冻结服务端完整 Catalog；Picker create 使用单一恢复门禁，delete 与 cover 按 Project 隔离。Agent Composer 初次 active 立即恢复；Project ready/activation owner、Session/recovery generation 与 SSE connection generation 阻止迟到结果，Inline/Prompt Bar 失效操作返回 `superseded`。零主 Session 不隐式创建；只读原因、操作、`aria-readonly` 与禁用光标持续可见，有草稿的只读运行仍保留停止语义。聚焦 7 files / 59 tests、相邻 6 files / 48 tests 与根 typecheck 全绿；已有 Session、零 Session、Profile 不可用、面板重开、Sepia/Dark 和 383px Composer 已浏览器验证。390px 整页仍受既有桌面 drawer 布局限制；只有归档主 Session、非空草稿运行、A→B→A 延迟注入、保存期间切 Project、慢 Catalog 深链、Preview 激活失败、响应丢失与多标签链未做破坏性浏览器验收。 |
| [128 NeuroBook 官方站部署与关联闭环](docs/tasks/128-neuro-book-site-deployment/README.md) | Implementing / 官网部署脚本生产验证完成（2026-07-28） | 官方站已运行于 `https://nbook.notnotype.com`，当前 commit `17dc3ba` / digest `154a6bf4...`；`deploy:dmit` 已通过真实 push、两次 Actions/digest 冷快照升级和同 digest 幂等重跑。首轮 `gh --commit` 兼容失败在 DMIT 写入前停止，修复后容器、双 readiness、只读根/内存、Docker/Nginx/Xray 与 root-only 回执均通过；自动 rollback trap 未故障注入。NeuroBook 本地仍已补齐设备码/refresh 提交补偿、前端暂停重试和 App SQLite 统一迁移门禁，相关 32 + 8 + 17 项及 Manager 完整 164 项通过（另 2 项跳过）；真实 State Root 仍是 catalog 1，补强尚未发布 Product，浏览器关联与加密备份恢复验收待执行。 |
| [113 记忆系统](docs/tasks/113-memory-system/README.md) | Spike 完成 (2026-07-27)；主仓 subject 侧路线待用户拍板 | 三轮讨论收敛出形态并用 spike 验证完毕。**形态**：episode（原始叙事、可重放重建）+ facts（happening，append-only 永不失效，带 subjectIds）+ 关键主体注册表（类型 + **带 tick 的别名** + 一行本体描述，非滚动 summary）+ 轻量状态层（可变状态带失效区间，取代 = invalidate + set）；**边不是存储对象**（≥2 subjectIds 的事实即边，图塌缩为「注册表 + 倒排索引」）；**消解在写入时完成**（每批一次「抽取+归一」联合调用，注册表全量进上下文）。**产物**：sibling 仓 `nb-memory`（TS/Bun 零服务器，四组 API：ingest / search / registry / state；jsonl 事件溯源是唯一事实源，向量与倒排索引 open 即重建；决策考古见其 `docs/adr/0001~0003`）。**B1 同题集对照 baseline（主仓 subject 等价内核）**：时间泄漏率 **0% vs 22.2%**（跨全部 11 跑稳定）、entity 76.3% vs 68.4%（19 题双跑均值，单跑峰值 94.4%）、asof 71–100% vs 57.1%、摄入 22 次 LLM/3 分钟 vs 57 次/21 分钟、分身 0 vs 存在。**revision 37.5% 未达标**——病灶经错题分析定位在状态检索命中率（问「经济来源」而状态名「生计来源」召不回）与评测分辨率（计分题仅 8–11 道、污染率 6.3–18.8% 摆动、单跑散布 25–75%），**不指向形态缺陷**；用户拍板停止刷分收口。**方法论沉淀**（跨任务通用）：跨题集分数不可比（旧门槛须同题集复测才作数）、单跑数字不可信（同配置 entity 两跑差 33pp）、裁决协议 = 扩题量→双跑均值→同题集对照、判卷方差与摄入方差需分开归因。**待拍板**：主仓 subject 侧换用 nb-memory（vendor 进仓）还是原地移植偷设计三项（时间过滤优先，泄漏 22.2%→0 是最大单项收益）。 |
| [126 Agent 上下文组成与缓存诊断面板](docs/tasks/126-agent-context-inspector/README.md) | Implemented (2026-07-27；浏览器走查与真实数据核对待用户) | 参照 Claude Code `/context`，为 Agent 加只读的上下文检查面板：六分区归因（System / Tools / HistorySet / 对话历史 / ModelContext / AppendingSet / 本轮输入）+ 来源名逐条 + per-request 缓存时间轴 + 三级中性诊断。**地基已存在**：Task 86 的 pi trace 默认开启且每次调用完整记录 `{systemPrompt, messages, tools}` 与含 cacheRead/cacheWrite 的 usage，本任务是归因与呈现，不新建采集管线。**已拍板**：不做作者摘要层（composer 状态条已承担）、归因到分区级+来源名、只做可观测不做缓存优化、诊断展示不规训、面板不提供导出/分享（继承 traces 隐私边界）。**查证的既有缺陷（本任务顺手修）**：命中率分母漏 `cacheWrite`（`AgentChatSurface.vue:679` 与 `AgentTextBubble.vue:278` 两份重复实现），且芯片上是 session 累计值——首轮全量 cacheWrite 永久压分母，累计命中率无法用于诊断。**建档同轮自审 9 条**，3 条阻断：面板总数与 composer 芯片必然不等（口径与时刻均不同）、字段名 `origin` 在 session entry 已有两义须改 `promptSource`、`PiTraceIndexEntry` 缺 usage 拆分与 toolsHash 导致缓存时间轴无法只读 index。**明确不做**：`cacheRetention` 暴露到设置、ModelContext 位置调整、把 System/tool schema 纳入 compaction 口径——等面板产出真实数据后再定「5 分钟 TTL」与「ModelContext 中间插入」孰为主因。**首轮实施（07-27）**：批次 A 归因地基（trace `segments` + index 增补 `usage`/`toolsHash` + `custom_message.promptSource{zone,labels}` 落盘 + DSL 具名 `label` + `buildPromptPrefixAttribution`）、批次 B 诊断引擎（13 code 判别联合，10 个恒 info，语气契约用测试锁住）、批次 C 命中率口径（新增 `app/utils/prompt-cache.ts` 唯一口径源，分母补 `cacheWrite`，两处重复实现消除）。**关键实现发现**：`applyCompaction` 按引用保留 `entry.message`，对象标识压缩后成立，故归因走旁表，**未改 `reduce()`/`applyCompaction`**，原担心的 `stored-message-codec` 连带改动未发生。**偏离**：`promptSource` 从纯 labels 改为 `{zone,labels}`（否则 HistorySet 桶会随轮次虚增）；`PiTraceSegment` 去掉 `chars`（避免第二套口径）；`vitest.config.ts` 新增 `server/**/*.test.tsx`（DSL 是 JSX，端到端归因测试必须 .tsx）；批次 B 的组合端点并入批次 D。**批次 D（07-27）**：面板落地——入口 = composer gauge 芯片，`DialogWindow` 非模态双 Tab（组成：堆叠条 + 分区表 + 来源明细可展开 + 可切换历史请求；缓存：provider 能力条 + 逐请求三色柱时间轴 + 每柱诊断）；组合端点 `GET /api/agent/sessions/[id]/context-inspection`（只读、只返聚合量不返正文、事实解析失败退化 null 而非报错）；`CONTEXT_DIAGNOSTIC_CODES` 常量元组 + 编译期对齐断言 + i18n 完备性门（**做过失败注入验证**）；中英文案各约 50 条，诊断严格陈述句无祈使句。**新增 legacy 归因回退**：批次 A 之前建的 session 无 `promptSource`，按位置推断（首条真实 message 之前的连续 custom_message = HistorySet），面板显式披露「首轮提醒与 HistorySet 未分开」。**规划期查实并消解原最高风险项**：校准无需按 provider 分支——pi 层已统一使 `input + cacheRead + cacheWrite` = 真实 prompt 总量（Anthropic 直接映射三字段 / OpenAI 侧 `input = prompt_tokens - cacheRead - cacheWrite`），且 `cacheWrite1h` 是 `cacheWrite` 子集不可重复相加；Q2 因 maxRecords=100 关闭。**批次 D 偏离**：诊断按 Tab 三分而非两分（否则缓存页混进无关观察）、抽出 `resolveModelCacheRetention`/`resolveCompactionTriggerTokens` 两个可单测的决策函数、新建 `observability/context-inspection.ts` 承载装配助手（不再往 7000+ 行的 harness 堆）、**消息正文预览有意裁剪**（来源明细已答主问题，全文走既有 trace 查看器）。验证：typecheck 26/26 全在 llmlint 既有基线零新增；新增 63 个聚焦用例全绿（trace-segments 14 + prepare-run 8 + harness-trace-segments 2 + context-diagnostics 20 + prompt-cache 4 + view-model 11 + i18n 门 7 + 端点集成 4 + helpers 16 中新增部分）；`app/components/novel-ide/agent` + `app/utils` + `server/agent/observability` 57 文件 336 用例全绿；`server/agent/harness` 回归失败集与改动前逐条一致（5 个稳定失败均属他人未提交在途工作，已用 `git show HEAD:` 只读比对定位到 payload 校验路径重构；1 个 `abort clearQueue` 抖动经单独复跑确认）。 |
| [125 文档站体验走查与内容补全](docs/tasks/125-docs-site-overhaul/README.md) | 第一轮 Implemented & Verified (2026-07-26)；第二轮 mermaid + 全站英文化 Implemented (2026-07-27，官网英文版浏览器走查待做) | 5 个只读子代理并行走查（新手作者 / 技术自部署 / Agent 开发者 / RP 用户 / 链接事实审计），结论逐条独立核实后才采纳。**修复**：`profile-tsx` 三页的 `VariableSchema` / `Compaction` 幽灵节点（站点唯一完整示例此前粘贴即抛「未知 DSL 节点」）、3 处 `reference/agent/harness*.md` 404 外链、3 条逃出 srcDir 的越界相对链接、`reference/agent/README.md` 三条同源死链、`srcExclude` 补 5 个会被构建上线的孤儿页（含 2 个 VitePress 脚手架页和 1 篇含项目剧透的内部日志）、鉴权默认值写反（Portable 实际默认关）、`auth:create-admin` 在 GHCR 路径跑不通改 `neuro-book admin create`、变量工具死面小节（14 个内置 profile 零绑定）。**新手漏斗**：首页 hero + 7 卡片按 README「创意写作 IDE」口径重写（World Engine / 承诺账本 / llmlint 首次进 features）、补 Release 下载链接与 `http://localhost:3000`、独立「配置 AI 模型」小节含费用与数据流向、引用此前零使用的 `docs/images/` 截图、启用本地搜索。**架构**：新增「核心能力」分区，Subject RAG 加 danger 标注并降至末位，部署页移出新手分组并与新增运维页合并。**8 个新页**：`core/{world-engine,plot-workbench,markdown-studio,llmlint}`、`agent/{workflow,modes}`、`profile-tsx/authoring`、`operations`（含 traces / history.sqlite / 可分享日志包三条此前零覆盖的隐私边界）。**RP 口径按用户拍板**：七处统一标注「入口已下线、重新设计中」，教程 05 的转折从结尾前置到开头。工具清单补成 14 分组完整表，profile 清单补齐 14 个。**共 12 个新页**（含第二轮补齐的「使用指南」分区：设置中心 / 主题配色 / 变更与文件历史 / 账号与云备份）。**新增防回归门** `server/agent/profiles/docs-tsx-examples.test.ts`：扫描文档 tsx 代码块，断言 JSX 节点与 profile-dsl 具名导入都真实存在，已用失败注入验证能抓住原 bug（DSL 增删节点忘同步文档会直接红）。**创意工坊刻意不写完整页面**（站点未上线 + 客户端未接入，写了会重演本任务修掉的问题）。`bun run docs:build` 三次通过（38 页），Playwright 实跑验收：搜索命中新页中文标题、12 新页全 200、8 个 sidebar 分区就位、截图真实加载、构建产物全量站内链接零断链。**推翻一条子代理结论**：审计称构建可能已失败，实测 CI 最近 8 次全 success，其依据的陈旧 dist 是本地产物。**自审补漏轮**又补 7 处：最关键的是 `reference/agent/profile-guide.md` 里同类的 5 处幽灵节点（`WorkdirReminder` / `ProjectWorkspaceReminder` / `Compaction`）——修了文档站却漏了它链接的真相源，防线扫描范围因此扩到 `reference/agent/`；另修 PowerShell 空格文件名复制即失败、writer 页缺免责声明，新增 `agent/skills.md`（16 个内置 Skill 清单）、Agent 总览图（刻意用纯文本而非 mermaid，因 VitePress 未装渲染插件会显示源码）、introduction 常见疑问段。终验：39 页构建通过、站内链接零断链、37/37 测试通过。**第二轮（07-27）mermaid + 全站英文化**：①mermaid 推翻上一轮「退化成纯文本图」的结论——**没引 `vitepress-plugin-mermaid`**（peerDeps 只声明 vitepress ^1，且拖进 mermaid 9 时代的 `@mermaid-js/mermaid-mindmap`），改为 fence 规则 + `theme/Mermaid.vue`（惰性 import、SSR 出图源占位、语法错误只毁一张图、`watch(isDark)` 重渲染）+ `theme/index.ts`，零新增依赖；新增 4 张图；**浏览器实测抓到真 bug**：`<script setup>` 顶层 `let renderSeq` 是每实例一份，同页两张图撞同一个 DOM id（mermaid 用它生成 style 选择器与箭头 marker），改为独立 `<script>` 块模块级变量。②文档站 i18n：`config.ts` 重构为 `locales`（root=zh-Hans / en），nav+sidebar 双份，补齐 root locale 的中文界面文案与本地搜索中文 UI；`docs/en/` 同名镜像 **37 页全部翻译**，9 个子代理共用一份翻译规范（站内绝对链接加 `/en`、图片相对路径多一层 `../`、mermaid 只译标签、`docs/en/profile-tsx/` 同样在 `docs-tsx-examples.test.ts` 扫描范围内）。③官网 landing 双语：sibling 仓 `agent-design-template` 加 `LocaleContext`，同一套组件出 `official/index.html` 与 `official/en/index.html`（Vite 多入口 + `base:'./'` 按各自 HTML 位置算相对资源路径），文案模式 `defineCopy(zh, en)`；**规范第一版给错**——`{ zh, en } as const` 过不了类型检查，子代理跑基线时发现后改为 `defineCopy`。④**顺带修掉 4 处源文档错误**：`guide/account.md` 说云备份是「项目的完整备份」而产品实际备份整个实例含 `.env`/`config.yaml` 密钥（隐私边界页上的实质错误，中英双改）、`core/plot-workbench.md` 的 MICE 写成「使命/询问/角色/事件」而实际 enum 是 `milieu/idea/character/event`、`agent/tools.md` 的「7 个 `save_story_*`」实为 6+`save_promise_beat`、`operator-bridge.md` 更新流程重复一行 `neuro-book update`。验证：`docs:build` 通过 76 页（39 zh + 37 en，dead link 是构建门）、locale 泄漏双向零、浏览器实测英文首页/语言切换器/双语搜索 UI/英文页 mermaid id 唯一。⑤**官网 llmlint demo 逐条实测**（加载 360 条真实规则用 `RegExp.test` 跑例句）发现三处「展示的规则实际不会命中」：`PAIN_POINTS` 的两个规则 ID 根本不存在、换成真实的 `inflation-not-only-but` 后仍不成立（正则字符类排除逗号而例句带逗号）、demo 第 4 段挂的是默认关闭的规则、第 5 段例句压根不匹配所挂规则——全部换成实测命中且默认启用的配对。**已订正（2026-07-31 拍板）**：「llmlint 340 条规则」实际是 360 条 / 默认启用 266 条（「311 条默认启用」是 `level:medium` 的条数），用户定口径为 360 / 266，已在 README 重写任务中跨三仓订正 17 处（NeuroBook 文档 11 处、llmlint sibling 仓 `skill/README.{md,en.md}` 2 处并已 sync 到两层副本、agent-design-template 官网与 deck 源码 9→仅改源未重建，官网线上仍显示 340）；数字来源改为 `llmlint rules --format json` 的 `registry` 字段而非任务文档。另 `inflation-not-only-but` 不匹配带逗号的「不仅仅是 X，更是 Y」疑为 llmlint 规则缺口。**遗留**：官网英文版未做窄屏走查（桌面宽度已实测双向零语言泄漏、切换往返正常、demo 交互正常）；重建 `docs/public/official/` 会把 sibling 仓未提交改动一并烘进产物（该仓无 remote），提交前需确认。 |
| [124 写作产品线第三批](docs/tasks/124-writing-pipeline-batch3/README.md) | Implemented (2026-07-27 小说数据 Skill 硬切；真实模型冒烟待用户) | `book-deconstruct` 与 `character-qa-fanout` workflow 保持不变。小说数据已硬切为 bundled `novel-data` Skill `1.0.0`：自带 package/lockfile 与 frozen install 依赖门，CLI 只读查询本地 NovelScope 缓存并提供 `rankings`/`book-detail`；`novel-genre-research` 通过 bash 调用并披露快照时间与 stale。仓库外临时副本安装和全场景 CLI 测试证明不借用仓库依赖；官网不承载小说数据 API，不再规划 HTTP 代理或技术设置面板。 |
| [123 仓库目录结构优化](docs/tasks/123-repo-structure-optimization/README.md) | 批次 A 已完成 (2026-07-26) / 长期任务，批次 B–D 未开始 | 全仓结构体检完成，问题清单登记为长期任务。server 层九项：模块分层与 `index.ts` 入口不统一（27 个模块只有 7 个有入口）、`server/utils/novel-chapter.ts` 895 行混装 5 种职责且被 79 文件依赖、`server/utils/` 杂物抽屉横跨 6 域、Prisma client 双导入口径、"models" 一词三义、`server/workspace/` 是长在源码树里的运行时数据目录、43 个孤儿测试无命名约定、`workspace-files/` fan-in 629 却完全扁平、`server/api` 26837 行里 24259 行（90.4%）是内联 OpenAPI 生成物。仓库层四项：13 个僵尸依赖（NestJS 全家桶 + rxjs/tsyringe/croner 等，源码零引用）、`CLAUDE.md` 与 `AGENTS.md` 已分叉成双份 Agent 指令、`.traces/` 调试产物入库、任务文档层膨胀。已拍板：OpenAPI 放弃 `defineRouteMeta` 改独立 spec；本任务不动 `NeuroAgentHarness`、不动预览页、不做 800 行以上组件拆分；**server/ 的 S1–S9 结构改动本轮全部延后**（Task 110/111 workflow 接入正在大面积改 server/agent，此时动结构会互相踩），只做分析登记。已执行：归档 26 个陈旧无外链任务（active 122→96，archived 14→40，`08` 号编号冲突消解）；**批次 A 零逻辑风险清理已完成**——删 14 行僵尸依赖 + 关闭 `experimentalDecorators`/`emitDecoratorMetadata`（全仓装饰器用量 0）、`CLAUDE.md` 收敛为 `@AGENTS.md` 导入、`.traces/` 移出版本控制、删 `architecture.md`。typecheck 26 项错误全在 `llmlint.test.ts`，与既有基线逐项一致零新增。**A6（根级 `.d.ts` 移进 `types/`）已拍板跳过**：`Dockerfile:60` 硬编码了这四个路径。**新发现（需跟进）**：全量测试实测基线为 41–53 个文件失败 / 169–192 个用例失败，远超本文件此前记录的「workflow 目录 12 个失败」；A/B 对照证明与批次 A 无关（还原依赖后对照组 53 文件失败反而更差），缺口主要在 `server/api/` 路由测试（`validateBody` 收到的 mock event 缺 `node`），且两次运行用例总数相差 167、大量 5000ms 超时，套件处于不稳定状态，建议优先于批次 B/C 修复。**R4 的 `RELEASE.md` 一半已完成 (2026-07-27)**：`RELEASE.md` 从 79 KB / 29 个版本块降为只保留当前待发布版本；历史按发布线重写并迁到 `docs/changelog/`（`index` / `v0.8` / `v0.7` / `v0.5`）与英文镜像 `docs/en/changelog/`，已接入 VitePress 双语 nav + sidebar，`docs:build` 85 页通过。`AGENTS.md` 新增「面向用户的语言风格」小节作为长期约束。`PROJECT-STATUS.md`（93 KB）的切分仍未做。 |
| [122 写作链路第二批](docs/tasks/122-writing-pipeline-batch2/README.md) | Implemented (2026-07-26；真实模型冒烟与浏览器验收待用户) | 四线并行落地：①内置 workflow `chapter-write-review-revise`（真实 writer 落盘写章 + 一致性/节奏/文风三维并发评审 + 按 major 修订循环，writer 用真 profile 是 adhoc-only 取向的刻意例外）②内置 workflow `consistency-audit`（章级并发审计对照 lorebook 摘录 + leader 预查 worldFacts，跨章汇总 verdict）③leader.default HistorySet 注入 novel-guide 全文（SkillCatalog 之后）+ leader-default.md Skills 段例外条款 ④tutorials 全量刷新（simulation 口径清零、06 归档 docs/archived/tutorials、路线图 6 篇、03 补阶段四小节）+ 默认模板 13 处 simulation 残留修复。测试：两 workflow 各 3 用例 + catalog expectedPhases 全绿；workflow 目录下 demo-service/job/run-vm 12 个失败是既有 vi API 不兼容与断言漂移（Task 111 未提交面），非本任务引入。 |
| [121 写作 Skill 体系重组](docs/tasks/121-novel-skill-reorg/README.md) | Implemented (2026-07-26；浏览器验收待用户) | 写作 skill 从 15 个收敛为 9 novel + 1 guide：`novel-workflow-*` 前缀退役（与 Task 110 workflow 编排撞名）；三层结构=工具支持（import 系列）/ 随时可用（idea-exploration、genre-research 占位、character-card-workshop）/ 创作流程（`novel-setup` 四合一：项目初始化→世界书框架→角色设计→World Engine 初始化；`novel-writing` 三合一循环：剧情设计→拍板落库→正文/评审/修订，开局黄金三章为其首轮特化 phase）；`novel-guide` 是唯一全局路线图。skill 内接入现有内置 workflow（parallel-brainstorm / write-review-loop / split-book）。05/06 emulation 归档到 `docs/archived/skills/`。skill-catalog + profile-dsl + leader-assets 测试、tutorials、reference、profiles 引用已同步。 |
| [125 可重建运行产物的存储生命周期](docs/tasks/125-runtime-artifact-storage-lifecycle/README.md) | Phase 1/2/3 已实施 (2026-07-27；Phase 4 三环境验收待做，改号 120→125) | **Round 02 已落地**：①一次性受控清理回收 **17.34 GB / 1866 文件**（四步安全校验逐 root 通过，current 引用零缺失）②删除 Profile 的两处冗余 import cache（`ProfileArtifactStore` 与 `importCompiledProfile`；published artifact 落盘名已是内容寻址 sha，staging 路径已含 UUID，物理副本零信息量；`expectedBytes` 校验由 `validateProfileArtifact` 的完整 sha256 重算覆盖，移除后反而更强）③Test Workspace Fixture 换成 **run 级共享只读 snapshot + `systemAssets:"isolated"` opt-in**，snapshot 是对已发布 release 的纯投影（不做编译），加 owner marker/lease、初始化回滚、dispose 错误聚合与保守 sweep ④Profile orphan **硬字节预算 512 MiB/root**，优先级=最小安全年龄地板(10min)>硬预算>7天 grace，并修正 mtime 语义（GC 对 keep 集合 `utimes`，否则长期被引用的 artifact 一脱离 current 就成最优先驱逐对象）+ 退化状态守卫（manifest 无 loaded entry 时跳过预算回收，对应实测到的全量 `compile_failed` 窗口）⑤`runtime-artifact-import` options 改为可选嵌套 `cache` 且 `retention` 必填，类型层堵死 `?? dirname()` 兜底。**实测**：config-service 60/60 通过；fixture 磁盘从「60 次 × 6.3 GB」降到「1 份 snapshot 386 MB + 单 fixture 1 MB」，运行结束残留 0；typecheck 零新增；**新增 19 项聚焦测试全绿**（GC 预算/地板/退化守卫 7 项 + fixture 所有权含 junction sentinel 6 项 + artifact store 2 项 + retention 4 项）。`workspace-files.test.ts` 从 13 项失败收敛到 4 项，其中 2 项已证既有（`init-db` 路径口径、llmlint `show-llm-rules` 版本漂移）、1 项为顺序相关 flake（单跑通过）。共享 snapshot 用**硬链接投影**而非 junction（junction 会被子进程 realpath 穿透）；`linkedApplicationEntries` 必须覆盖 manifest 依赖路径的全部顶层前缀（补了漏掉的 `packages`）；globalSetup 在 published release 过期时**开子进程**自动重编（主进程内编译会 OOM）。**原阻塞已解除但暴露更深问题 (2026-07-27)**：`bun:ffi` 无法解析的直接原因是 `profile-artifact-compiler.ts` 的 `repoAliasBundlePlugin` 只把 Node builtins 和 `node:` 前缀判为 external，`bun:*` 掉进 `resolveBarePackage()` 后 `require.resolve()` 在 Node 下抛错返回 `undefined`。已加 `isPlatformBuiltinModule()` 把 `bun` / `bun:*` 与 Node builtins 同等处理，重跑后 `Could not resolve bun:ffi` 零命中，`system-assets:prepare` 14 个 profile 全部编译成功。**但 `catalog.test.ts` 并未回到 44/44**：解析错误消失后 esbuild 开始真的打包整张依赖图，**单个 profile artifact 达 27 MB、32 个共 875 MB**（实测含 `ProjectRootIdentityModule`、`GetFileAttributesW`），即 Task 118 的 Project identity 图被整体拖进了每个 profile 的 bundle。**Round 03 (2026-07-27) 已解决**：metafile BFS 探针证明三个重依赖族各只有一条进入边，全在 profile-dsl 自己的 import 上——①`defaultSqlSchemaSummaryText` 的两个动态 import（bundle:true 下照样打包，拖进整个 server 树 262 文件→jsdom/prisma/@libsql）改为 `ctx.runtime.sqlSchemaSummary` 宿主注入（helper 独立成 `project-sql-schema-summary.ts`，放 sql module 或 project-session 任一侧都成环）②token 估算器（presentation 唯一 pi-agent-core 值导入→全部 Provider SDK）拆到 `stored-message-tokens.ts` ③`planModeToolDirectory` 拆到纯模块 `plan-mode-directory.ts` ④writer 直接 import 的 `readProjectManifest` 拆到 `project-manifest.ts`（project-workspace re-export）。「点名 external」方案已证不可行：format:esm 下 external 裸包保留为 ESM import，Product 的 `.output/server/node_modules` 不在 artifact 向上查找路径上。**结果：单 artifact 27.3→1.16-1.63 MiB，一代 release 382→17.24 MiB，512 MiB orphan 预算从 1.3 代余量回到约 30 代（预算常量未改）；`catalog.test.ts` 回到 44/44**。并加编译器**依赖门禁**（`assertProfileArtifactDependencyGate`，dependencies 产出后、staging 前）：server/ 目录白名单 + 禁止依赖族（jsdom/@prisma/@libsql/@earendil-works/@mistralai/openai/@anthropic-ai/@google/@smithy/@opentelemetry/@notnotype 等）+ 4 MiB 字节上限，违规=`compile_failed`（message 前缀「依赖门禁违规」；不新增 issue code，`"compile_failed"` 字面量被 14 文件消费），对用户 profile 同样生效；合同见 `reference/agent/profile-compiled-artifacts.md` Dependency Gate 小节。验证：`--force` 全量重编 14/14 loaded、门禁单测 4/4、typecheck 除 llmlint 既有漂移零错误；config-service 26 失败与 rp-profiles/leader-assets 失败均已证为工作区在途未提交漂移（projectPath→projectRoot API 迁移、提示词文案与 workflow 工具清单变更），非本轮引入。原诊断：磁盘爆炸是三段乘法：Profile artifact约381 MiB/次全量发布且orphan只有7天grace、测试fixture逐测试复制已膨胀的完整system `.nbook`、初始化/cleanup/进程强杀后缺少跨运行owner回收。本机快照中295个Temp fixture约119.994 GB，system Profile `.compiled`约4.752 GB且约4.369 GB未被current manifest引用。推荐分离Published Profile Artifact、Runtime Import Cache、Test Workspace Fixture三个owner，共享预算/统计合同但不建大一统Store；ADR 0002等待确认，推荐默认预算与一次性清理仍需用户拍板，尚未修改业务代码。 |
| [120 Agent Skill Package Contract](docs/tasks/120-agent-skill-package-contract/README.md) | Implemented / Verified | runnable Skill 已建立正式 package 合同：`SKILL.md` 保持 portable，`package.json.version` 是 SemVer 真相源，catalog 输出 version/root/location；任意 `agent/skills/<key>/node_modules/` 都不进入受管资产，普通、force no-op、version/prompt/rule 更新均保留安装，只有 Bun install 输入或 `bun.lock` 真正发布时才精确失效对应 Skill。损坏的用户 package 只隔离自身并继续遮蔽同名 system Skill，force 更新可修复坏 manifest。llmlint 2.0.1 已改从 catalog root（或 SKILL.md locator 父目录）运行并保持首次 install → status 门。NeuroBook 43 个聚焦断言、llmlint 282 Vitest + 69 Bun tests、Skill validator、真实 install/status/no-op sync 与三层 hash 对账通过；根 typecheck 仍只有 Task 118 已记录的 26 项既有 llmlint 测试类型漂移。 |
| [118 Project 生命周期、文件快照与 Agent 路径合同联合执行计划](docs/tasks/118-project-catalog-snapshot-path-integration/README.md) | Implementing / Phase 6 migration engine complete, gate 接线随 4B+7 | Phase 0–5已完成。Phase 6的Session离线迁移引擎已收口：新codec产出`currentProjectRoot?` + `schemaVersion: 2`并删除`workspaceRoot`/`workspaceKey`/`projectPath`；真实Workspace Root只读dry-run为499/499（managed 241 / workspace_root 233 / stale 24 / user-assets 1 / external 0），隔离副本apply/rollback/再apply全部499/499且backup逐字节一致，篡改WAL/manifest、文件集合变化、lock竞争与`not_started` rollback均fail closed；CLI入口为`bun run migrate:agent-sessions-v2`。真实盘上另发现38份2026-05旧布局子目录Session（运行时不递归读取、产品内不可达），按用户拍板移入`session-backups/legacy-nested-2026-07-26/`，基线回到499。**Phase 6的fail-closed gate接线经实施确认与session header消费面原子耦合（`SessionMetadata`仍必需`workspaceRoot`/`workspaceKey`；只挂启动gate会让Nitro缺sentinel起不来，先apply又会让当前runtime读不出Session；两把lease归一还受Harness同步构造限制），已改为随Phase 4B + Phase 7同批执行。**为解除后续接线阻塞，`agent-session-store-runtime`已改为按root归一的注册表。**Phase 4B第一批「Project列表去统计化」已完成(2026-07-27)**：`NovelListItemDto`删掉8个统计字段，`novel-chapter.ts`整条统计链(workspace tree扫描/Plot SQLite/session计数)与四套5秒缓存删除、895行降至363行，`project-list-prewarm.ts`删除，`GET /api/projects`去掉`limit`/`includeProjectPath`/`excludeProjectPathPrefix`，书架改展示摘要，预览页过滤移到客户端；新增「连续100次列表读取不触碰File Index、Project SQLite或Agent session」回归用例(以timing分段为判据)。该批**不含任何身份改名**。**Phase 4B第二批「显式close与可等待ProjectSession controller」已完成(2026-07-27)**：新增`POST /api/projects/close`(此前根本不存在close接口)与`user`关闭原因，未打开时幂等、其他标签在场或agent运行中返回409 `PROJECT_IN_USE`；`useProjectSession`新增可等待`activate()`/`disconnect()`，`target`参数改为可选故既有调用点无需改动。原计划同批的「open响应换最终DTO+ensure通知」经核对属身份改名(最终DTO用单段`projectRoot`)，已移交身份硬切批次；调用方显式驱动activate/disconnect也留到那批(选择流程届时本就重写)。**Phase 4B第三批「控制面与数据面身份硬切」已完成(2026-07-27)**：先冻结`SPEC-current-project-root.md`(用户拍板运行时只携handle/重绑走session子资源POST/novels连章节树一起处理)。关键发现=`project-session.ts`的8个字符串入口是唯一身份收窄点(窄腰)，改成ref-only后字符串形态在类型层面不可能存在，typecheck精确列出全部破裂点(生产仅20处)而非预估的288文件。已完成：Facade ref-only(删3个重复`*Ref`变体，新增唯一迁移边界`projectRefFromLegacyPath`)+控制面7个route接入Facade与最终DTO(此前与Phase 4A成果零接触)+数据面入口(plot/world-engine/rag/presence/workflow)收窄+config DTO切单段+删`/api/novels`5个route与OpenAPI条目+前端改名51文件307处(camelCase)与11文件19处(Vue kebab-case)+store换`ProjectMetadataDto`+删死组件族4文件约76KB。验证曾报受影响13 files/72 tests全绿+全仓typecheck 0错误，**但该批「完成」结论已作废**：前端无差别`projectPath`→`projectRoot`改名而服务端未同步，文件面/历史/剧情/世界引擎/工作流全部400或静默降级，`tree.get`退化为全盘扫描整个Workspace Root(实测17–22.5s)；教训=`$fetch<T>`类型参数是手写断言，typecheck+单测对身份改名不构成完成依据(详见任务README更正块与PLAN-batch3-recovery.md)。**第三批回归修复(A+B)已完成(2026-07-27)**：workspace-files 13 route(6 zod body换`ProjectRootDtoSchema.optional()`+5 inline query+2 multipart)、workspace-history 5 route(显式`normalizeProjectPath`换`projectPathFromRef(projectWorkspaceRef(...))`)、plot/world-engine catch-all改复用`requireProjectRefQuery`、workflow runs/catalog改单段(`workspaceKey`显式投影旧形态并注释留slice 2)；4处改名破坏的函数体修正、2个被改错方向的测试改回正确方向；删除零使用的`requireProjectPathQuery`；新增`server/api/project-root-contract.test.ts`源码级审计(HTTP边界零projectPath参数、覆盖全部route文件，即Phase 8审计第一块)；route-map A组9条目+plot/rag query schema声明更正并重新生成37文件defineRouteMeta。验证=审计测试+7个基线route测试+catch-all真实集成22用例+受影响单元全绿，且**真实HTTP冒烟通过**：tree只返回当前Project文件、0.22s走File Index(revision=1)，read/inbox/plot story/world-engine schema/workflow catalog均200，catalog可见project-scoped条目(静默降级消除)。**用户验收另发现A类漏网一处(Agent session面)**：前端`workspaceKey`现发单段而磁盘499个session与服务端过滤仍是`workspace/<root>`精确匹配→session列表全空；数据完好零污染，修复移交[Task 129](docs/tasks/129-project-picker-and-session-entry/README.md) W1（已完成：前端新增唯一投影点 `app/utils/agent-workspace-key.ts`，服务端与 slice 2 边界仍不动）。**Agent session面(slice 2)整体未动**：`SessionMetadata`仍是老三件套，`RunFrame`未收敛为handle，`projectRefFromLegacyPath`当前32处调用点即slice 2工作清单；store里7个请求已删除路由的死函数待单独清理(Phase 8零命中审计前必须清)。Phase 8前不可发布。Settings 只编辑 Current Project 已实际落实为无 Catalog、无跨 Project selector 的只读目标；presence/active Agent 存在时 close 返回 `PROJECT_IN_USE`，无待用户决策门禁。根 typecheck 本轮文件零命中，当前仍有未修改的 Skill `commander` 声明、Session migration 与 llmlint fixture 错误。 |
| [117 Windows 进程树所有权与 Bash 超时](docs/tasks/117-windows-process-tree-lifecycle/README.md) | Implemented locally / Release candidate verification pending | 独立 `@notnotype/owned-process` Module已系统性收口：Windows x64使用Bun FFI Job Object + 独立IPC；POSIX现在也由独立IPC supervisor持有process group，宿主断连统一进入有界清理，不保存裸PID。TerminateJobObject失败返回`terminate-job + Win32 error`并依靠唯一Job handle关闭清树，POSIX signal/probe异常归一化。Windows本机11项通过、2项POSIX-only跳过，Owned Process typecheck与Manager打包通过；真实POSIX runner、candidate workflow与可见CMD关闭验收尚未执行。 |
| [115 Workspace Root Agent 路径合同硬切](docs/tasks/115-workspace-root-agent-path-contract/README.md) | Contract unified / Phase 6 migration engine complete | Agent文件工具与bash cwd最终统一为Workspace Root，输入只保留Workspace Root-relative与absolute filesystem path。Task 118 Phase 1–5已完成Lifecycle、Occupancy、ProjectSession、全部built-in ProjectModule、控制面groundwork与内部数据面迁移；Phase 6的session离线迁移引擎与真实基线演练已完成（499/499，见Task 118）。后续仍需cwd/File Address、CLI、DTO/UI和prompt hard cut，以及随Phase 4B + Phase 7一并执行的session写入fail-closed gate与真实apply。公开Product合同尚未切换。 |
| [112 NeuroBook Passport 与官方站点改造](docs/tasks/112-passport-official-site/README.md) | Implemented locally / Browser acceptance pending | Passport / Workshop / Backup 保持固定官网与本地加密合同。设备码兑换新增 `credential_persist_failed` / `exchange_invalid` 终态；首次关联和 refresh 轮换都只在 App SQLite 提交成功后更新 cache，失败时终止会话、best-effort revoke 并要求重新关联。`usePassportLink` 只在 pending 自动轮询，网络错误暂停并保留设备码，404 先与本地状态对账；底层数据库错误和 token 不进入界面。真实批准/落库/access-token 浏览器闭环仍未执行。 |
| [114 文件快照缓存独立包与 Project File Index 生命周期](docs/tasks/114-file-snapshot-cache-package/README.md) | Phase F integrated | 0-consumer projection/store已删除；NeuroBook通过唯一Adapter接入生产，Project/plain Workspace共用单一SnapshotCache，旧index lifecycle与ResourceOwner已删除。package为3 files / 40 tests；warm-up failure diagnostics与共享重试已通过真实Adapter回归。Phase 4B切换时继续确保Project列表不成为File Index owner。 |
| [111 Workflow 正式接入 NeuroBook Agent](docs/tasks/111-workflow-agent-integration/README.md) | Implemented locally / Manual acceptance pending | WorkflowCatalog、Agent可见模型清单与设置、审批后默认后台的`run_workflow`、AgentJobManager、Job+Run双观察气泡、正式`/workflow.preview`主动触发、当前 8 个使用`adhoc + outputSchema + ephemeral`的内置workflow均已落地。Plan G 把 Header、任务中心和全部`useAgentJob`观察器统一为页面级 Jobs SSE，具备原子快照游标、有界replay/背压、preview合并与gap/epoch恢复，已删除Jobs列表和单Job周期轮询；consumer lease 已收口到 effect scope 内的动态目标，Project/User Assets 工作面 Header 常驻、任务中心按打开挂载、无 Job 目标的观察器和 Project 选择页均为零 consumer。初始快照失败按退避恢复，成功后健康态不再 GET；任务中心手动刷新和协议恢复是唯二快照入口，Workflow 应答/重放/Run 切换不再强刷 Jobs。审查加固与补漏已覆盖创建因果游标、Workflow Run 终态所有权、孤儿Run补偿、目标切换、HTTP 400、错误分区和三路SSE退避；本轮再修复了观察器与首个 consumer 同步建立时的跨 epoch 收敛竞态。既有17文件129项与本轮6文件49项通过；历史 typecheck 基线曾记录 7 个错误，本轮复跑退出码为 0。详情GET与Workflow Run轮询仍按需保留，`/workflow.preview` 正式列表首次 GET 与 demo 零 Jobs consumer 已分开约束。真实Project Workspace拆书、浏览器与真实模型验收仍待执行；Workflow Run SSE、run-as-session、完整脚本沙盒、busy admission、adhoc工具白名单、D15和RP自动记忆重建保留后续。 |
| [116 Agent 与 Workflow 可靠性收口](docs/tasks/116-agent-workflow-reliability/README.md) | Implemented / Manual acceptance pending (2026-07-27 summarizer 回归) | Harness 继续通过统一 SessionWriteExecutor 按 invocation ownership 隔离迟到 transcript/tool/hook/lifecycle 写入。session 773 摘要失败证实 fence 正常：session 774 的隐藏 invocation 不应直接写 source session；根因是生产受管 summarizer Profile 多出跨 session `settleRun` hook。该 hook 已删除，Harness 成为 source 写回唯一入口；真实受管 Profile 已覆盖无 hook、正常写回、source leaf 变化和用户标题锁三条聚焦回归。Workflow 既有合同保持不变；浏览器与真实 provider 验收待用户执行。 |
| [110 Agent Workflow 编排系统](docs/tasks/110-agent-workflow-orchestration/README.md) | Kernel integrated / Continued in Task 111 | 脚本式durable workflow（不引入图DSL）。内核已从sibling仓`nb-workflow`端口化并vendor进`server/vendor/nb-workflow/`（`sync:nb-workflow`），跑在真实`JsonlSessionRepository`上：checkout=moveLeaf、append显式锚定、excursion旁支、acquire按tag跨run复用；session meta落地`kind`/`tags`子集与`origin:"workflow"`。正式工具、waiting/Job、前端气泡与内置库已由Task 111承接完成；内核后续只保留run-as-session、SSE、沙盒、busy admission与D15等深化项。 |
| [108 Agent Attachment 存储内核与图片引用](docs/tasks/108-agent-image-attachment-references/README.md) | Implemented locally / Manual browser acceptance pending | Attachment Store继续位于Workspace Root `.nbook/agent/attachments`，但 Harness 原始 Store 已私有化；locator 授权后只返回绑定引用的 `read()` capability。上传、文件快照和 `read(image)` 共用 `AgentAttachmentCodec` 的 MIME、完整解码和 64 MP 门禁。共享真实 raster fixture 已替换 Harness 假 PNG 文件头；snapshot 超限稳定 `limit_exceeded` 且 Adapter 零写入，归档竞态锁定最终 Session 门禁。live 工具结果有 Attachment metadata 即显示通用卡片，durable entry 到达后再启用图片变体/原图预览或非图片下载。完整 file-tools 49/49；两份 Harness 图片旧失败已归零，剩余两个失败属于未修改的模型恢复与 Variable SDK。未自动执行浏览器验收。 |
| [109 统一 File Scope、File Address 与 Product Runtime 路径合同](docs/tasks/109-agent-workspace-path-runtime/README.md) | Implementing / Integrated locally | Project-bound cwd继续是Current Project Workspace且只作为相对路径基准；read/write/edit/apply_patch现允许任意绝对路径，Project外绝对地址不反推Project身份或进入History/Context Access。`workspace/<project>/...`保留为带open gate与Project记账的显式领域地址，相对路径仍有realpath containment；重复的RuntimeLocationReminder已移除，WorkspaceFocusReminder统一说明cwd与路径合同。Bash仍是受信任完整Shell。公开Product链仍待验证。 |
| [22 Agent Public Event Projection](docs/tasks/22-agent-public-event-projection/README.md) | Implementing / Integrated locally | 原始三形态、有界公开投影和recovery cursor保持成立；Public Tool Identity已覆盖live/durable/history/HTTP/SSE/client patch入口并fail closed。既有浏览器Session证据仍是历史基线，但104–109合并后的公开Product与统一门禁尚未重验，不能维持Verified。 |
| [107 Agent Runtime Event OOM 与 SSE 内存边界](docs/tasks/107-agent-event-memory-boundaries/README.md) | Implementing / Integrated locally | live/replay/SSE有界治理保持成立；Provider终态、事件、durable history与HTTP输入共用512 UTF-8 byte tool-call identity合同。聚焦事件/HTTP/Harness回归通过；公开Product与浏览器SSE证据仍待统一门禁。 |
| [106 Agent Chat Flow Snapshot 分页](docs/tasks/106-agent-chat-flow-pagination/README.md) | Implementing / Integrated locally | 单一session GET与opaque cursor合同保持不变，未发现raw`snapshot.entries`公开旁路。网络窗口与前端常驻历史有界，但Repository仍为O(session file size)；浏览器长session及cold/hot/read bytes/memory证据待完成。 |
| [105 统一安装目录与 NeuroBook Manager](docs/tasks/105-unified-installation-manager/README.md) | Implementing / Canary A verified / 0.9 Candidate pending | `0.8.19` Canary A公开链保持已验证。当前源码为Installation/Release Manifest v5、Operation Journal v5与Application State catalog v3：所有mutation经外置heartbeat lease恢复并重读Manifest；native/container/source执行先验证身份；Product双rename、migration applying、cleanup retry与Source adoption已有持久恢复。Manager `.49` 已公开并通过provenance、tarball、真实bunx与公开输入验证；新clean Portable已通过31项doctor、Product/Manager HTTP和两种真实外置Host卸载终态，Windows全量现为248 passed / 3 skipped。公开A→B/跨Profile矩阵仍由Candidate Actions完成。 |
| [104 Pi Models Runtime Upgrade](docs/tasks/104-pi-models-runtime-upgrade/README.md) | Implementing / Integrated locally | Provider Config ID、Base URL与proxy保持不可隐式迁移；`modelApi`已纠正为可编辑的discovery/候选补全偏好，同端点可复用saved Secret且不改已有`model.api`。一键修复可从一致的已保存模型API确定性补全，混合/空模型不猜测；Session仍只保存Provider Config ID+Model ID。新增 OpenCode Go 请求级重试：`models.providers[].options.requestOptions.maxRetries` 默认 5、0 关闭，设置页提供独立整数输入，高级 JSON 重复声明会在保存和检查/发现请求前拒绝；只重试单个 SDK 请求，不重放 Agent 整轮。聚焦回归与根 typecheck 已通过，浏览器和真实 Provider 验收仍未完成。 |
| [103 AGPL-3.0-only 许可证迁移](docs/tasks/103-agpl-license-migration/README.md) | Implemented | NeuroBook 与 sibling llmlint 已从 PolyForm Noncommercial 迁移到 AGPL-3.0-only；本地第三方文风、写作参考和旧致谢退出 Git / Product source snapshot，llmlint 的 manifest、README、vendored 与 user runtime 许可声明已同步，聚焦资产同步测试通过。 |
| [03 Config System](docs/tasks/03-config-system/README.md) | Updated | Global Config 的模型结构现为完整 Provider Config；Agent session 稳定身份仍为 `providerConfigId + model.id`。2026-07-14确认Config Service测试曾直接rename真实`workspace/.nbook/config.json`，测试中断会把用户配置遗留在系统临时目录；已用7月11日备份恢复，并把39项Config Service测试全部迁入统一临时Workspace assets Context，失败或中断不再触碰真实Global Config。 |
| [56 World Engine](docs/tasks/56-world-engine/README.md) | Stage Complete | 核心模型、API、Workbench、subject system discovery 与作者主路径已完成阶段收尾审计。 |
| [64 World Engine Prompt Engineering](docs/tasks/64-world-engine-prompt-engineering/README.md) | Updated | 写作模式提示词主链与模板已收口：`08` 负责 World Engine 剧情推进，`09` 负责状态已推进后的 writer 写作，legacy RP/simulation 从普通入口降级。 |
| [65 Calendar Enhancement](docs/tasks/65-world-engine-calendar-enhancement/README.md) | Done | `calendar.ts` 硬切，`calendar.yaml` 仅作为历史记录。 |
| [66 Codebase Cleanup](docs/tasks/66-codebase-cleanup/README.md) | Stage Complete | 已完成一轮 World Engine / 写作模式阶段后的代码清理收口：Workbench 纯规则下沉、filter preservation、draft surface auto-open、issue level/status mapping 和专用 util 测试拆分已落地；命名 / 文件结构与复杂主体语境候选已记录待审批，后续等待真实作者使用反馈或用户重新开启。 |
| [69 World Engine Tool Cleanup](docs/tasks/69-world-engine-tool-cleanup/README.md) | Done | 历史任务：旧协议收口到 `patches/path/4-op`，运行时/DB 术语改为 `WorldPatch`，collection 支持按值删，内部全量查询统一走 `queryState`；其 Agent 三工具形态已在 Task 71 继续收敛为 `execute_world`。 |
| [71 World Engine CodeAct Readwrite](docs/tasks/71-world-engine-codeact-readwrite/README.md) | Implemented | Agent World Engine 工具收敛为单一 `execute_world`，读写合一进入 CodeAct deferred 事务；新增 issue collector、`editMutations` 精确编辑、`parseTime` / `formatTime`、`slices({withPatches:true})` patchId 与 JSON Pointer 读写统一。 |
| [72 Error Report Logs](docs/tasks/72-error-report-logs/README.md) | Implemented | 新增后端 JSONL 日志、日志包下载 API、请求/异常/Agent 摘要日志和 Windows portable `data/logs/` 落盘。 |
| [73 Agent Session List Performance Pagination](docs/tasks/73-agent-session-list-performance-pagination/README.md) | Implemented | `/api/agent/sessions` 改为分页 Page 返回，新增服务端 search；列表路径使用轻量运行态摘要和 profileKey 批量解析，避免按 session 重复解析 profile catalog。 |
| [74 Agent Command Performance](docs/tasks/74-agent-command-performance/README.md) | Implemented | `/api/agent/sessions/:id/commands` 轻控制命令返回 live state，不再生成完整 snapshot；relation index 已收敛到 write executor after-write observer，并修复 rebuild pending link/detach 漏账；profile catalog dirty cache 已加 generation race 防护和 HTTP runtime watcher。 |
| [75 World Engine API / Calendar / EmbeddingText 收口](docs/tasks/75-world-engine-api-calendar-embedding-cleanup/README.md) | Implemented | World API、Calendar、loader与runtime artifact合同已收口；`ming-ding-zhi-shi-2`的events和character memory已切为`EmbeddingText`。受控SQLite migration默认dry-run，apply前备份并在单事务内重写，验证patch计数与slice文本reduce等价；`world.search.text`现在区分无检索能力的400与合法空结果。 |
| [78 Plot Scene / World Engine Bridge](docs/tasks/78-plot-scene-world-engine-bridge/README.md) | Complete | Plot System 已收敛为 Scene-only：Scene 通过时间范围、地点 subject 和出场 subject 连接 World Engine；Plot 入口已恢复到顶栏、左侧侧栏、Agent 模式侧栏快捷入口和 Plot 面板内按钮；旧 `StoryPlot / Plot Beat` 已从正式模型、工具和 UI 中删除，旧项目迁移会备份并合并数据到 Scene。Agent 易用性主链已改为 leader.default 直接负责 Plot / Scene：剧情初步设计 -> 推进 World Engine -> 剧情设计 -> 更新 Plot -> 调用 writer；leader.default 持有 Thread / Scene 读写、Scene World Context 和 `get_chapter_writer_brief`，director 保留为高级/手动 profile，不再是普通写作必经节点；writer 仍无 Plot tools，world.engine 仍只负责复杂世界引擎维护。四个实现切片已完成：Profile Contract Cleanup、OpenAPI Explicit Path、Chapter Writer Brief Module、Agent Tool Binding；`get_chapter_writer_brief` text 返回 `suggestedBriefMarkdown`，details 返回完整 DTO。Round 140 completion audit 跑过 13 files / 62 tests passed；Round 141 调整 leader-owned Plot 主链；Round 142 补齐稳定 reference 与 server 侧测试门禁，移除不被 Vitest 收集的 assets writer 假门禁。真实 Agent 模型行为 smoke 与浏览器验证未执行，不作为 Task 78 完成门禁；ChapterOverride 已单列后续 Task 80。 |
| [79 Profile Build System](docs/tasks/79-profile-build-system/README.md) | Implemented | profile 编译系统 Phase 0–3 已收口：`editor-snapshot` 不再返回/触发 profile settings 大包；settings/build-status 专用接口与前端状态块已接入；`.compiled` 切为内容寻址 `artifacts/<sha>.mjs` + profileKey 映射 manifest；`compile_failed` 写入账本且 runtime 严格拒绝 stale/failed；HTTP runtime worker 只生成 staging release，server 主线程 `ProfileReleasePublisher` 经 `proper-lockfile` 做 in-process 原子发布并翻 Registry；`ProfileArtifactStore` / `ProfileFreshnessChecker` / `ProfileRegistry` / `ProfileSourceWatcher` 已从 catalog facade 拆出；保存/创建/删除/外部编辑 500ms 去抖后台编译，启动 boot sweep 非阻塞自愈 user profile；profile assets sync 改 staging→Publisher，copy-if-absent 并可修复损坏的同 sha user artifact；内容寻址 artifact GC 带 grace，worker service 已扩为受互斥约束的 worker 池，全量编译已改为 worker 池 entry fan-out、主线程 fan-in 一次 manifest 发布；single compile 改为 worker 只产 entry staging、Publisher 在 publish lock 内合并当前 manifest；Publisher 增加 per-root in-process 发布队列，确保磁盘 release 与 Registry 翻转同 root 串行；HTTP runtime user-assets sync 已改为 in-process 强一致返回，profile assets sync 一次同步只发布一个 batch patch release，真实 `.compiled` artifact 修复收敛到 Store publish lock 内；watcher freshness gate 已验证 artifact/type artifact 后再跳过编译，unlink 与 stale source-missing 升格 full build，避免删除/并发保存漏账；final guard 已锁定所有 full replacement 发布前校验 source file set + entry source hash/bytes；`profile status` 对 stale 返回非零，Product staging 会剔除 manifest 未引用 artifact，Windows portable 与 staging 均按 manifest 校验嵌套产物存在性和路径可迁移性；共享 Profile helper 语义变化已通过 compilerVersion 7 强制旧 bundle 失效重编；profile/workspace 测试 root 已隔离，三文件并行回归不再污染真实 user-assets。 |
| [80 Chapter Override / Writer Brief](docs/tasks/archived/chapter-override-writer-brief/README.md) | Archived | 从未落地，被 Task 87 吸收：`ChapterOverride` 更名 `ChapterBrief`，作为一等实体 `StoryChapter` 字段组落地，不再按 `chapterPath` 字符串关联。目录已归档到 `docs/tasks/archived/`。 |
| [81 Profile MCP Config](docs/tasks/81-profile-mcp-config/README.md) | Design | profile 级 MCP 支持先走配置化路线：Workspace Root 注册 MCP server，profile 通过显式 tool allowlist 启用 MCP tools；第一版不提供 profile helper 加载 MCP、不升级 inline execute 工具类型、不自动启用整个 server，并要求模型可见工具集合与执行硬权限共用同一份冻结 run tool snapshot。已补充 [ARCHITECTURE.md](docs/tasks/81-profile-mcp-config/ARCHITECTURE.md)，从性能、用户体验、加载逻辑、Harness 复杂度、开发人员心智、安全与失败恢复比较方案，推荐 `RunToolSnapshotResolver` 作为实现中心。 |
| [83 Project List Performance](docs/tasks/83-project-list-performance/README.md) | Superseded by Task 118 | 历史诊断与缓存方案保留作为证据；当前 `/api/projects` 已改为 Task 118 的轻量 Lifecycle snapshot，只读取 Project manifest，不再统计 Session、Workspace tree 或 Project SQLite，也没有 5 秒统计缓存和后台预热。连续 100 次列表读取的数据面零触碰门禁仍在。 |
| [84 llmlint Standalone Development Repo](docs/tasks/84-llmlint-standalone-repo/README.md) | Implemented | llmlint 真相源迁到 sibling `../llmlint` 开发仓，仓库根承载 `skill/`、`evals/`、`tests/` 和开发脚本；可安装 runtime package 固定为 `skill/`。新增 `scripts/cli/sync-llmlint-skill.ts` 将 `../llmlint/skill` 镜像到 NeuroBook bundled snapshot，user-assets 同步硬切清理旧 `.git`、`node_modules`、`evals` 和 `.gitignore`，真实 `workspace/.nbook/agent/skills/llmlint/` 已保持 runtime-only。验证通过 llmlint 根安装、根 typecheck、`bun run verify`、runtime、同步、NeuroBook typecheck 与关键测试；早先根依赖 resolver 阻塞已复查关闭。 |
| [85 Fullstack Template / UI Library](docs/tasks/85-fullstack-template-ui-library/README.md) | Implemented | 已创建 sibling `../nb-ui` 与 `../nb-fullstack-template`，没有移动 NeuroBook 或 llmlint 目录，也未迁移两个现有项目的源码 import。`nb-ui` 已补齐 `@notnotype/nb-ui/components` 手动导入导出面，保留 Nuxt module 自动注册，并把 `@nuxt/kit` 与 dev `vite.server.fs.allow` 契约修稳；模板默认依赖已切到 Bun 正确 link 形态 `link:@notnotype/nb-ui`，已明确 `link:../nb-ui` 是错误反例。v0.6 补齐 Button / Panel、表单常用属性、默认 dark/light theme、`applyNbTheme` 和组件 smoke tests；后续补强 `FormField` 语义 Interface、`--color-scheme` 主题 token、主题化 Notification、IconButton 类型导出、Dropdown 稳定 id、SegmentedControl group label 与 SwitchField switch 语义。模板改用共享 Button/Panel/Form，主题抽到 `app/theme/default-theme.ts`，补充 admin env、受保护页面和 `/api/protected` 示例，并新增 `app/middleware/auth.ts` 作为受保护页面标准 route middleware。数据库主路径 `db:setup = prisma generate -> db:create -> prisma migrate deploy -> init-db` 已修复 Prisma 7 SQLite 首次运行文件创建问题。验证通过 nb-ui typecheck/test/build、模板 typecheck/test/build、linked symlink frozen install，以及临时 SQLite 的 `db:setup` 非交互迁移和默认 admin 初始化。 |
| [86 Pi Request Observability](docs/tasks/86-pi-request-observability/README.md) | Implemented (待前端手动验收) | Pi 请求可观测层 P0+P1+可搬运化收尾已落地：`tracedStreamSimple/tracedCompleteSimple` 统一入口透明代理默认开启记录每次 provider 请求（pi 规范化 context + 原生 payload、httpStatus/headers/usage/stopReason、TTFT/耗时；error/abort 由 finalize 挂 `result()` 兜底；请求侧 apiKey/headers 从不落盘，response.headers 经敏感头 denylist 过滤且豁免 ratelimit 头），落 `<repo.tracesRoot>`（`.nbook/agent/traces/<sessionId>/`），每 session 保留最近 N 条（默认 100）；health-check 划出范围不记录；统一入口由 `unified-entry-guard.test.ts` 扫描强制（allowlist 仅 traced-provider 与 model-settings）。前端：IDE 顶栏「Trace」入口（Plot 按钮旁，聊天顶栏挂点已移除）打开查看器——scope 可切「最近请求（跨 bucket 聚合 `GET /api/agent/traces/recent`，缺省）/ 某 session / `_system` 无 session 请求」，invocation 分组、六 tab 详情、trace↔session 双向跳转（先选 session 再开面板）、清空当前 bucket；+ 设置页「可观测」开关；`observability.piTrace` 是一等 config section。core（recorder/reader/traced-provider/view-model）零 NeuroBook 依赖（tracesRoot/onWriteError 注入），抽库 runbook 见 `reference/agent/pi-trace-observability.md`，建仓等第二个消费者。**traces 含完整 prompt 与小说正文，严禁进入 task 72 可分享日志包或任何导出诊断流程**。查看器 UI 未浏览器验证。 |
| [87 Plot 两棵树 + Writer 双模式 + ChapterBrief](docs/tasks/87-plot-two-trees-and-writer-modes/README.md) | Implemented (autonomous only) | Plot 从 Scene-only 升级为**两棵树**：承载树 Story→StoryAct→StoryChapter→Prose、因果树 Story→StoryPhase→StoryThread→StoryScene，在 `StoryScene.chapterId` 外键交汇；`StoryChapter` 新增 `brief*` 字段组（ChapterBrief：章节目标/POV/信息控制/开头收尾/禁写），吸收原 Task 80 ChapterOverride。Chapter↔Prose 通过 manuscript frontmatter `chapter: <name>` 反指，复用 `ProjectWorkspaceIndex`。brief 编译器 `getChapterWriterBrief(projectPath, chapterId, mode)` 支持防全知双模式，status 链新增 `needs_chapter_brief`（信息控制四字段全空时阻断 handoff）。writer profile 加 Plot 只读、PayloadSchema 用 `chapterId`。**本轮只做 autonomous 模式**：curated + 反问逃脱口因 profile 静态 toolKeys / harness 无 settings 门控 seam 延后。前端 Plot 面板已完整迁移：`chapters` 改从 StoryChapter 实体派生（不再扫 manuscript 文件树），面板子树 `chapterPath → chapterId` 全链路改名，新增 `PlotChapterEditorDialog`（章节 + 完整 ChapterBrief 表单）、章节管理条、新建卷对话框，以及只读接口 `GET /chapters/:id/prose` 的 Prose 关联视图；`bun run typecheck` 0 error，backend plot/profiles/API 测试全绿。**bootstrap 已在《命定之诗2》真实项目实测**：2 Act + 2 Chapter + frontmatter 反指 + 0 迁移孤儿、幂等重跑无副作用，并彻底修复实测炸出的 2 个单测测不到的 bug——(1) 事务超时：把 manuscript 扫描 / frontmatter 回写移出 Prisma interactive transaction，事务内 `applyCarrierTree` 只做纯 DB 写入；(2) libsql/bun/Windows 句柄不释放导致进程不退出 + 下次 SQLITE_BUSY：一次性 CLI 末尾显式 `process.exit`。新 UI 尚未浏览器验证。 |
| [88 Workshop Platform](docs/tasks/88-workshop-platform/README.md) | Phase 1 Implemented (后端 + Web) | NeuroBook Workshop（创意工坊）设计定稿并完成 **Phase 1 后端 + Web 前端**（sibling 仓 `nb-workshop`，从 `nb-fullstack-template` 派生）：Prisma 扩展 InviteCode / WorkshopItem（安装名 name、status 三态）/ ItemVersion / Like / Favorite / Comment / Report，API v1 全套 20 路由（公开浏览下载 / 登录社交与上传 / admin 签码举报下架），邀请码注册，zip + `nbook-package.json` manifest 校验（缺 manifest / 字段非法 / entry 缺失 / version 不递增提示 N+1 / 非首版 type-name 不一致全拒绝），文件落盘 `WORKSHOP_FILES_DIR`。实现后完成一轮审查 + 修复：API 清单对照零遗漏、39 条边角链路走查全通；修复邀请码并发双花（事务内条件更新）、同版本并发上传字节与库记录不一致（改为先写库后落盘 + 唯一约束 409 + 落盘失败补偿删除）、slug 并发抢注 500→409、下架条目收藏/点赞撤不掉（新增 `requireItemBySlug` 供撤销类操作）、meta 补 platformVersion。**Web 前端全量页面已建成**（2026-07-06）：`/` 浏览（筛选态映射 URL query）、`/items/:slug` 详情（双栏 + sticky 下载栏 + 点赞/收藏乐观更新 + 举报 + 评论区 + 404 态）、`/users/:username`、`/publish` 四步向导（前端 fflate 解析 manifest + 重试防 slug 409）、`/me`、`/admin`、`/login`+`/register`（补邀请码）；类型化 `useWorkshopApi` 单一 $fetch 出口，description 纯文本渲染规避存储型 XSS；补后端缺口 `GET /api/v1/me/items`（本人全部状态条目）。验证：install / typecheck / test / build 全绿，**41 测试**含 21 个真实 HTTP 集成用例（spawn build 产物、含并发用例），下载 sha256 与上传字节一致。**完全信任用户，安全防线全部记入安全债清单**按两个门回补。实施发现：Prisma 7 生成 client 的 `__dirname` polyfill 被 nitro bundle 后 Windows 启动即崩，`nb-workshop` 内已加 build-time rollup patch 兜底（模板仓未动）。上线前动作（ToS / 备份 / 种子内容 / 安全债回补）与 NeuroBook 客户端安装闭环（Phase 2）未开始；未做浏览器验证。 |
| [89 Theme System v2.1 & Custom Themes](docs/tasks/89-theme-system-v2/README.md) | Implemented (待浏览器验收) | Novel IDE 主题系统已收口到 36 个 v2.1 CSS 变量：8 套内置预设保留完整字面值，`theme-vars.css` fallback 与 `sepia` 同步，`--we-*` World Engine 别名层保留但改指向，app/ 中主要 Tailwind 调色板类与 `dark:` 变体已按语义变量迁移，分类色板按约束不迁移。主题 runtime 已支持 `ui.theme: string` 与 `ui.customThemes` 写入 Global Config，bootstrap 应用主题快照防 FOUC，Agent client variables、Monaco/diff、登录/Admin/Profile Template 入口均接入 resolved theme。设置页新增自定义主题编辑器：核心 13 色、36 变量高级区、实时预览、按核心色重新生成、JSON 导入导出、删除/复制/编辑；取色器使用 `vue3-colorpicker` + `colord`。规范已同步 `AGENTS.md`、`CLAUDE.md`、`app/utils/theme/README.md` 与 `reference/theme/system.md`。主题聚焦测试 4 files / 9 tests passed，OpenAPI 生成 40 routes updated / 0 failed；全量 typecheck/test 仍被既有 Agent plan-mode/Profile DSL/low-code-form 等非主题基线阻塞。浏览器自定义主题全流程验证尚未执行，需用户同意后进行。 |
| [90 Agent 模式系统（normal/discuss/plan）](docs/tasks/90-agent-mode-system/README.md) | Implemented (待浏览器验收) | Agent 二元 Plan Mode 升级为三模式：normal（可读可写）/ discuss（只读讨论）/ plan（只读计划），`enter_plan_mode`/`exit_plan_mode` 合并为 `switch_mode`（切换=审批，批准后生效）。数据模型硬切不兼容旧数据：`planModeActive` → `agentMode`，customState `ui.planMode.active`/`agent.planMode` → `ui.agentMode`/`agent.mode`（含 phase enter/reentry/steady/exit + fromMode + hasExitedPlan）。只读模式下 write/edit/apply_patch 按 `mutatesWorkspace` 能力标记由 harness 注入审批挂起（复用 tool_user_input_required 链），批准仅豁免单次调用且**在 resolution 消费点真实执行工具**；plan 模式写 `.agent/plan/*.md` 豁免；bash 放行靠提示词约束只读；同模式切换被前置拦截为 no-op。提示词层 `ModeReminder`（didChange 出 phase 全文 / 周期重放出 steady 轻文案，7 种 ModeSlot 可覆盖）+ `ModeAvailabilityReminder`（仅 normal）。前端三态循环按钮 + Shift+Tab + 按模式 placeholder/badge，`/mode`（`/plan` 别名）命令，`AgentSwitchModeBubble` 泛化审批气泡。typecheck 0 error（除并发任务 low-code-form 一处），后端 harness 157 tests + profile/dsl/tools/前端全绿。浏览器验证未执行。2026-07-07 代码审查修复 P1–P6：apply_patch `Move to` 目标不再绕过只读写豁免（复用 `extractPatchTargetPaths`）；注入写审批的识别改为无条件并入 mutatesWorkspace（修好重启后列表 idle 误判 / 发消息误报 session repair / 切 normal 后 pending 丢失，顺带消除冗余 reduce）；`hasExitedPlan` 只在退回 normal 置位（plan↔discuss 互切不再误判 reentry）；execute_sql/execute_world/plot/variable_patch 在 discuss/plan 提示词中按 bash 同级收口只读；前端 AgentMode 枚举去重 + 气泡改 `parseToolArgsObject` + 穷尽 slot 映射等技术债清理。修复后 harness 160 + apply-patch 单测 6 + 各套件全绿，typecheck 仍仅剩并发任务一处。 |
| [91 操作日志 / 文件历史模块](docs/tasks/91-operation-log-file-history/README.md) | Spike Implemented (验收全绿,待集成) | 「先写后补」讨论升级为完整操作日志系统并设计定稿：append-only 事件溯源（`OperationLog` 信封 + `file.*` 判别联合 + 内容寻址快照），一份日志投影四个视图（单文件时间线 / 删除找回 / 用户收件箱 accept+revert / 会话游标 unseen 提醒）。关键决策：不用 git；独立 `.nbook/history.sqlite`（**隐私红线同 traces：含全文快照，严禁进入 task 72 可分享日志包**）；OperationActor 四类含 system(source) 与 external；每 session 标量游标 + 每 (user,path) 接受位点（n 用户 × n 会话就绪）；revert / restore 一等事件且走提醒通道；保留策略分层稀疏（默认 90 天全量 → 每日末版）且**参数配置文件可配**。**spike 已按 [GOAL.md](docs/tasks/91-operation-log-file-history/GOAL.md) 建成并验收**（sibling 仓 `../nb-history`，bun + TS strict + libsql 手写 SQL，零 NeuroBook 依赖）：场景验收 T1–T12 全绿（31 tests 多轮连跑 0 fail，含审查轮修复的时间线出生边界 bug 与读写分离双连接）、typecheck 全绿、性能达标（30KB 写入 ~10ms；unseen 5000 条积压 18–35ms；inbox 代表性 8–11ms；100 版 × 30KB 库 3.00MB 去重生效）、Windows 句柄双测通过（libsql close 需模块内建 GC 协助释放；`bun:sqlite` 对照零等待——驱动选择留给集成决策）。已知偏差如实记录：inbox 零接受全扫 1 万条 best 32–67ms 超 50ms 线（缓解 = 宿主 auto-accept 策略，兼解「未接受段永不 prune」）。NeuroBook 零改动；集成任务（写入面收口、watcher 喂 reconcile、harness 提醒机制 + profile fileChangeAwareness 策略、UI、task 72 排除清单）单列待启。 |
| [92 Project 资源生命周期统一管理](docs/tasks/92-project-resource-lifecycle/README.md) | Implemented (已被 94 演进) | Project 级常驻资源（Plot Project PrismaClient 缓存 / agent execute_sql 单槽 libsql client / workspace tree index chokidar watcher / World Engine GC 兜底）收敛到统一注册表 `server/workspace-files/project-resources.ts`：属主自注册 `{close, closeAll?, busy?}`，访问时报活。三条释放路径全部接通——删除 Project（`deleteProjectWorkspace` 的四行手工枚举改为 `closeProjectResources()`，新资源种类自注册即可，不再依赖"记得补一行"）、切换 Project（空闲清扫：TTL 10 分钟 + 60s 轮询 + timer unref，SSE 订阅在线视为 busy 跳过并刷新报活，解决旧项目 PrismaClient 无限累积与 chokidar 目录句柄常驻导致的 Windows EBUSY）、服务关停（新增 nitro `close` hook 插件）。注册表 4 单测 + 既有删除流程 3 集成测试不改而过 + 回归 sweep 23 files/188 tests 全绿；typecheck 全绿（顺手修复昨日提交带入的既有 `low-code-form/index.ts:798` 索引收窄错误）。**Task 94 GOAL-A 已将该文件演进更名为 `project-session.ts`（显式生命周期）**，隐式 touch/TTL 模型退役；本行保留为历史档案。 |
| [93 Plot 规划层](docs/tasks/93-plot-planning-layer/README.md) | Implemented (2026-07-09 审查修复轮收口；真实项目端到端实测待用户验收) | **已实施（数据层 + 维护/读取工具 + brief 消费段）**：在两棵树之上新增面向未来的规划层（World Engine=事实 / 因果树=编排 / 承载树=呈现 / 规划层=读者债务与作者意图）。StoryPromise + PromiseBeat（读者债务账本：**不设 kind 分类，行为由字段有无驱动**——deadline 才有逾期、cadence 只是提示性节奏参考；beat 挂 Scene，计划/事实/archived-不参与三态随 Scene.status 派生；fulfilled 回退仅在无剩余 payoff beat 时发生，Scene archive/delete 与 Thread delete 均接入同一回退入口；自由文本三层分工 summary/payoffExpectation/beat.note；伏笔按兑现机制四分类 tags 词表 setup_payoff/prophecy/motif/mirror，mirror 双线由 beats 跨 Thread 分布天然表达）；StoryDecision（ADR 式：open 态防 writer 擅自写死，decided 态 decision/motivation/risk 必填，dropped 强制 note，anchor 读时归一化 + serves/dependsOn 写入校验与死引用容错，值域 open/decided/superseded/dropped）；Scene.outcomeType（值域补全 yes/no/no_conflict/passive，null 只表示未填写——已提炼为全系统设计原则 D29）/pacingRole 与 Thread.miceType。brief 消费两段已接入 `get_chapter_writer_brief`：「本章 Promise 任务」（详细形态）+「未决决策警告」（不阻断）；D9 refs 词表清理已执行（system.md 删 foreshadows/pays_off）。工具面随 [Task 97](docs/tasks/97-plot-tool-surface-redesign/README.md) 形态落地。lint / ledger / report / workbench UI 仍暂缓待专门讨论；草稿系统化移出（思维链+ADR 够用）。验证：`bun test server/plot` 70 pass、`server/agent/tools` 147 pass、profiles 170 tests、typecheck plot 相关 0 error；真实项目端到端实测未执行（单测分段覆盖，验收降级已在任务 README 声明）。 |
| [94 Project 生命周期模型重构](docs/tasks/94-project-lifecycle-model/README.md) | Implemented & Accepted (浏览器验收待执行，未 commit) | Task 92 隐式模型重构为 VS Code 式**显式生命周期**。**GOAL-A 核心机制已落地（2026-07-08，ultracode workflow 实现 + 对抗式审查 27 agent）**：一等 ProjectSession（`server/workspace-files/project-session.ts`，由 project-resources.ts 演进更名）——显式 open（幂等 + 目录 404 校验 + initProjectDatabase 收敛到 open 跑一次）/ presence（**专用 SSE `GET /api/projects/presence`** 连接计数 + harness agent 运行中 invocation 探针）/ 归零→5min 宽限→级联 close / 泄漏表 + 30s 周期维护；`POST /api/projects/open`；前端 `useProjectSession` composable（重连先重跑 open、读超时看门狗）。审查轮修复 close-vs-reopen 竞态类（per-key 串行锁 + release 世代绑定 + sweep 重入闸）并把 D12 收敛为**仅 agent 在场阻删**。**GOAL-B 基线与数据面补漏硬化已闭环**：Plot / World Engine / execute_sql / workspace-files / config project 等数据面入口未 open 时稳定拒绝（HTTP 409 `PROJECT_NOT_OPEN`），plot/world/sql 三处 per-call `initProjectDatabase` 已移除并收敛到 open；2026-07-09 补齐 RAG 数据面与 Project Profile Home 写入/reset/读时初始化守卫，二次补齐 Agent session snapshot/system prompt 与 profile compile/preview worker 的 Project Profile Home 生命周期守卫，保留 Project Manifest、项目列表/创建/删除/zip 导入、纯 Global Profile Home 等控制面/全局面豁免；worker compile 普通失败仍返回 DTO issue，Project lifecycle violation 重新抛 typed error 并由 HTTP 层映射 409。补漏细节、边界解释和验证记录见任务 README / NOTES-B。**2026-07-09 GOAL-B 验收通过（主会话）**：代码走查（守卫 helper / 四资源咽喉 / 12 条 workspace-files 路由 / RAG·config·profile 路由 wrapper / worker lifecycleError 通道 / initProjectDatabase 残留审计）全部属实；typecheck + 守卫聚焦 6 文件 30 例 + 大组合 57 文件 510 例全绿；NOTES-B 遗留的 harness black-box「既有失败」经复核为两处与 Task 94 无关的既有测试时序缺陷（首用例 5s 暖机预算不足 + followup 等待锚点恒真竞态），验收轮已修复，harness 套件 239 例全绿。 |
| [95 nb-history 集成](docs/tasks/95-nb-history-integration/README.md) | Implemented & Accepted (2026-07-09；2026-07-18 runtime artifact 路径收口) | nb-history 以 **vendor 源码**进仓（`server/vendor/nb-history/` + `sync:nb-history` 脚本 + VENDOR.json 锁 commit），WorkspaceHistory 作为 ProjectSession 资源接入：每项目 `.nbook/history.sqlite`，open 预热 + 懒开兜底 + close 级联；写面全收口——编辑器/平台路由走 `tracked-workspace-files`，Agent 工具走 `agent-file-recorder`，watcher + D15 扫描负责对账。2026-07-18 补齐 Project Runtime Artifact 分类：World Engine 生成物迁入 Project Workspace `.nbook`，watcher/history/文件索引/下载共用排除策略；每个 WorkspaceHistory 实例首次打开时在 D15、inbox 和 unseen 前原子清理旧错误路径，保留正常历史与 cursor，未使用 History 的 ProjectSession 不强制开库。Project 下载包含 History SQLite 的独立一致 snapshot，完整备份与 Task 72 可分享日志包的隐私边界保持分离。Task 102 继续持有 notice、安全 diff、revision 与预算语义；D15b 外部变更列表、时间线/删除找回仍留 TODO。 |
| [96 Session 标题所有权与 summarizer 策略](docs/tasks/96-session-title-summarizer/README.md) | Implemented (2026-07-27 生产 Profile 回归已修；待浏览器验收) | Session 手动改名与 `/summarize` 所有权语义保持不变。受管 summarizer Profile 已删除跨 session `settleRun` 写回 hook，只返回结构化结果；Harness 的 `settleSessionSummarizerResult()` 唯一负责 source session 标题、摘要、用户标题锁与 leaf-stale 判定。真实受管 Profile 已覆盖正常写回、source leaf 变化和用户标题锁三条回归，ownership fence 不放宽。 |
| [97 Plot 工具面重排](docs/tasks/97-plot-tool-surface-redesign/README.md) | Implemented (2026-07-09 审查修复轮收口；F1 真实会话冒烟待用户验收) | plot 工具面从 14 个（三套前缀 + 四个死代码工具）收敛为 **15 个（读 8 + 写 7）**：读统一 `get_story_*` 前缀（`get_plot_tree`→`get_story_tree`、`get_chapter_plot`→`get_story_chapter`，新增 `get_story_promise` / `get_story_decision`，无 id=列表模式）；写合并为 `save_*` + **必填 action 枚举**（create/update + 生命周期动作 archive/abandon/fulfill/decide/drop 进 action=Agent 软删出口，物理删除不开放给 Agent）；Act/Chapter 死代码以 `save_story_act` / `save_story_chapter` 新形态接活（修 F1：leader 无工具补 ChapterBrief 导致 `needs_chapter_brief` 死锁）；`plotReadBindings` / `plotWriteBindings` 分层 bundle，writer 只持读组。**D8 已落地**：7 个 save_* 标 `mutatesWorkspace`（`tool()` helper 必填 `mutates` 强制每个工具显式声明读写意图），discuss/plan 只读模式下 plot 写面由提示词软约束升级为 harness 硬门控（复用 Task 90 机制；bash/execute_sql/execute_world 仍按 Task 90 TODO 走软约束）。验证：agent/tools 147 pass、profiles 170 tests、profile:metadata 重编译通过；F1 真实会话冒烟未执行（单测模拟覆盖，降级已在任务 README 声明）。 |
| [98 leader.assets 用户资产助手重塑](docs/tasks/98-leader-assets-profile-reshape/README.md) | Implemented | 用户资产助手 profile 重塑为「介绍 + 创建/修改/管理用户资产 + UI 指路」：新 system prompt 补齐用户资产地图（templates 两类模板 / `agents/{profileKey}/` 全局 home / config.json settings 存储）、「哪里做什么」三入口分工（设置表单填值 / agent 改文件 / TSX Profile 工作台编译）与「设置表单与 Home 资源」路由级知识；修复四类结构问题——AppendingSet 裸 Message 每轮无去重堆积（纯删除+负向断言）、SkillCatalog fork 漂移（DSL `mode="userAssets"` 参数根治）、agent-v2 死文本、缺 `ModeAvailabilityReminder`；新增契约级 **skill 白名单** `defineAgentProfile.skills.include`（prepare 包装层统一过滤 ctx.skills，catalog 可见性≠权限隔离），leader.assets 白名单收窄到 4 个资产编辑 skill；自身加 settingsForm（customTopSystemPrompt）。知识链同步：profile-system-guide SKILL.md + reference 补 Settings Form / Profile Home 两节（含 writer 文风预设 canonical 步骤与 frontmatter 契约）、修死链与旧术语（InputSchema→InitialSchema、mainRunToolKeys→toolKeys、幻影 DSL 节点名）；tsx-profile-editing 补 settingsForm/home/skills 代码骨架与 tool bundle 用法；leader.default Notes 补设置面板指引句。profile-dsl 28 + leader-assets 15 测试全绿、typecheck 全绿、涉及 profile 已重编译。浏览器验收未执行。 |
| [99 Plot 规划层前端](docs/tasks/99-plot-planning-ui/README.md) | Implemented (2026-07-09/10 审查修复收口；浏览器验收待用户，建议与 93 真实项目实测合并) | Task 93 规划层的人机 UI 直读先行批次（路线 B，纯前端零后端改动）：剧本工作台从 6 个装饰 tab（activeTab 只改高亮、主体不切换）重组为 **3 个真 tab**（线程规划=原三件套 / 承诺账本 / 决策记录），砍掉 overview/章节设计/草稿池/timeline/tree 装饰位；顶栏 mock 残留（「新小说」面包屑 / 写死「主线：」/ 虚构「已保存 刚刚」）一并清除。承诺账本：列表（状态点/派生阶段/importance/四类节拍计数/期限章 + open 默认筛选）+ ADR 式详情（节拍按章序时间线、期限章已删 danger、场景跳转回线程规划）+ 编辑/节拍/兑现·放弃·重开/物理删全操作面（节拍对话框回填既有 kind/note 防静默清空，payoff 默认 autoFulfill 并通知自动兑现）。决策记录：open 置顶分组列表 + ADR 分段详情（risk warning 刹车点色块、否决记录待补理由标注、serves/dependsOn 死引用删除线 + 编辑器行级 danger 标注）、拍板对话框（候选单选+全新方案、decision/motivation/risk 三必填、未选项转否决骨架提示）、作废强制失效原因。Scene/Thread 编辑器（完整编辑器+工作台 Inspector）补 outcomeType/pacingRole/miceType 三下拉（「未填写」=null，D29 人侧对齐；AI 批注 draft 同步）；底部详情面板与 Inspector 渲染「服务承诺」promiseBeats 只读芯片（点击跳账本聚焦，数据来自此前被丢弃的 scene 详情字段）；侧栏「承诺 N / 未决 N」计数入口（消费此前被丢弃的 PlotTreeDto 计数，0 弱化不隐藏）；账本 UI 写操作经 mutated→planningMutated 事件链刷新树计数并强刷受影响 Scene 详情缓存（与 Agent 侧 plotRefreshVersion 对称）。新建 `app/components/novel-ide/plot/planning/` 11 文件（含 typed API client `plot-planning-api.ts`——Task 87「typed query 防再犯」新端点部分收口；标签/色调 META 单一来源 `plot-planning.types.ts`）。顺手收口：workbench refs 目标候选三条硬编码 demo 数据改接 workspace tree 真实内容节点 + 引用弹层按 query 即时搜索（突破 40 条截断）；Inspector refs 词表删 foreshadows/pays_off（93 D9 前端残留）；Thread/Scene 编辑器保存错误改独立 editorError（原写 treeError 被模态遮挡不可见）。实施链 4 分片（2/3 fable 并行）+ 三维审查 10 findings（1 major：Promise 编辑草稿被 SSE 后台刷新静默重置——快照修复）全部处置；typecheck 全绿、planning 组件全部 <800 行、调色板/demo 残留零命中。明确不做（93 拍板对齐）：overdue/停滞计算（等 frontier+lint 批次）、报告页、张力折线、supersede 专门 UI。浏览器验收未执行。 |
| [101 Markdown Studio 方言重构与输入性能修复](docs/tasks/101-markdown-studio-dialect-and-performance/README.md) | Implemented (含反馈轮 2/3 + 审查修复轮；浏览器验收由用户执行) | Markdown Studio 输入性能修复 + 方言扩展一轮：**性能**——TipTap 与 Monaco 统一走 `useEditorChangeDebounce` 共享防抖协议（每键只 schedule，300ms 或 blur/Ctrl+S/submit 结算；外部覆盖 cancel；unmount cancel 防串位），消除每键「1 次全文序列化 + 4 次全文扫描 + 2~3 次隐藏 Monaco 全文推送」与源码模式每键 O(全文) `getValue` + store 全文对比链；隐藏编辑器惰性同步；评论列表与高亮 decoration 缓存进 ProseMirror 插件状态。**同步协议**——store `registerActiveEditorFlush` 钩子：`persistActiveWorkspaceBuffer` / `saveCurrentFile` / `syncWorkspaceFromDisk` 开头先结算编辑器防抖输入再取内容快照，根治「外部工具改文件 + 防抖窗口 → forceDisk 覆盖本地输入」的文本回退；`syncWorkspaceFromDisk` 按 `mtimeMs === lastSyncedMtimeMs` 抑制自己保存的 watcher 回声（不假警告、不白重载）；dirty 预警通知文案含 Ctrl+S 打开冲突对比界面指引（diff 界面仍只在保存乐观锁 409 时弹出，有意设计）；**StructuredTextEditor（v-if 懒挂载的 15+ 处表单编辑器）模式切换前 sync watch 结算防抖，堵住切 rich/source 丢最后 300ms 输入**（审查修复轮 F1）。**方言**——评论合并为 `<comment>` 单标签双形态（段内闭合=行内 mark；开闭独立成行=跨段落 commentBlock）；`<ruby text="hàn zì">汉字</ruby>` 注音与 `<bilingual text="译文">原文</bilingual>` 对照块；未知 HTML 兜底只保源码不渲染（块级 HtmlBlock / 行内 RawInlineHtml chip，失配残片退化单标签 chip 含 `Vec<String>` 类伪标签），**渲染能力只属显式 `<html>` 块**（HtmlEmbed：默认源码卡片 → 点击渲染 `sandbox="allow-scripts"` iframe，postMessage bridge 预留 `window.nbook.request` 数据接口注入点 `resolveDataApi`）；**宽容形态读时规范化**：`正文<comment>␊…␊</comment>` 开标签黏正文形态由 `normalizeMarkdownDialectBlocks`（shared，三重守卫：逻辑行首/缩进代码/闭标签存在性 + fence 感知 + EOS 不算行尾 + 开标签正则与各方言 pattern 逐标签同构）在编辑器读入时拆段为标准块级，流式 chunk 与 Monaco 不规范化；HtmlBlockBridge 对配对闭合恰好覆盖到结尾（`findMatchingCloseEnd` 真配对判据）的 DOM 可解析方言标签交回 DOM 自愈。四个陷阱已固化为注释与契约：marked tokenizer 后注册先执行（兜底 1390/1400，新方言必须更低）、段落中断 `src.slice(1)` 伪装（块级 start 只认 `\n` 前缀）、**ProseMirror 空文档默认填充节点取 priority 最高块节点**（paragraph 提到 1500 压过兜底，否则空文件出 HTML 卡片；新块级扩展 priority 不得超过 1500）、读时规范化判据边界（normalize 函数注释）。方言核心扩展组单一来源 `markdown-dialect-extensions.ts`（真实编辑器与三个测试文件共用，防测试 schema 静默分叉）；inline AI 引用高亮 ~300 行拆出独立模块 `InlineAiReferenceHighlight.ts`。验证：shared+app 全量 301/301、本轮文件 typecheck 干净；浏览器验收（打字流畅度、空文件、卡片交互、外部改文件冲突链、混合形态、StructuredTextEditor 模式切换不丢字）由用户执行。 |
| [102 Agent 变更收件箱与提示词顺序](docs/tasks/102-agent-change-inbox-and-prompt-order/README.md) | Implemented & Accepted (2026-07-10；2026-07-18 notice 指导去重) | Agent Composer 输入卡上方是默认收起的独立变更卡片，完整 History Dialog 保留深度审查；敏感正文在服务端读取前阻断。Profile DSL 只产出 `<FileChangeNotice mode={...} />`，Harness 物化 notice 时从最终 runtime 读取单文件 diff 上限（默认 512）；Profile 自定义 settings 与运行策略完全分层。notice 保持英文 Git 风格、安全路径阻断、预算与游标语义；逐文件条目只陈述事实，read/sensitive/deleted clause 由 footer 每类汇总一次，敏感与删除路径不再承诺存在于 Inbox。Reminder、Workspace 和 Variable 的既定边界不变。 |
| [18 Agent Runtime Pipeline Hooks](docs/tasks/18-agent-runtime-pipeline-hooks/README.md) | Updated | Agent 基础工具协议已补齐并重新修复当前工作区回写回归：`request_user_input` 已从 Low-Code Form 拆出，模型可见结果保持格式化文本和结构化 `details.answers`，协议只保留问题、单选 options、开放 note，不再支持默认值/推荐/多选；`read` 支持按需/自动行号和定位 details，offset/limit 收紧为正整数；`edit` 批量 exact replacement 先预检再写入并返回可读失败诊断；task custom state 维持即时 + savePoint 状态写入，覆盖下一轮读取和旧分支不污染语义。 |
| [51 llmlint Text Lint Skill](docs/tasks/51-anti-ai-slop-skill/README.md) | Updated to 3.0.0 | 默认 `builtin/default` 为 360 rules / 266 active；规则 scope 是 `all / narrative / quoted` 一等合同，compact/full/rules JSON、stylish 与 Web registry 都输出 resolved scope。`guide` 带严格 profile/规则集/文本指纹守门；`contribute` 只写本地 outbox，`detect` 是独立外部正文链路。真相源仍是 sibling `../llmlint/skill`，NeuroBook 内目录只是无 `.git` / `node_modules` / `evals` 的 vendored runtime snapshot。 |
| [58 Agent Profile Settings Low-Code](docs/tasks/58-agent-profile-settings-low-code/README.md) | Updated | `leader.default` 接入低代码 settings 与 profile home persona 资源，支持协作模式、熟练度、提问策略、Leader 人设和最高优先级置顶提示词；`writer` 同步接入最高优先级置顶提示词并清理 `enableKittenAdultStyle` 隐藏字段；低代码合并层忽略已下线字段的存档残留。 |
| Writer Profile 重构 | Done | 去除小猫之神角色定义，理清 profile / reference / skill 职责边界，从 650 行压缩到 535 行。 |

### Task 111 最新收口（2026-08-04）

反馈闭环已补齐显式 `messageIdentity` 与 `custom_message` 系统投影、直接/队列/恢复身份回归、公开 Run `journal/events` 与 Job/tool/frontend usage 去除 invocation `cost`、异步 Workflow `get_job` 详情和独立 `deliveryStatus`、`wait:true` 遇 ask 取消边界、全量 resume 校验，以及 Composer/preview 的 Markdown description 和逐 Run 防重复提交。服务端自动化验证 17 个文件 260 项、前端 5 个文件 39 项通过；该阶段记录的 `bun run typecheck` 曾有既有 7 个仓库错误，本轮已重新复跑通过。浏览器、真实 Project Workspace/provider/模型验收未执行。

本轮另修复 adhoc 动态 `outputSchema` 的 `report_result` binding 漏判：provider 可见 schema 现在要求 `data`，缺失字段会进入 harness 错误重试，不再静默完成并向 workflow 投影 `null`。新增真实 harness 回归覆盖非空、空 schema 和未声明 schema 的兼容边界；profile、harness、issue 所列 7 个 bundled workflow 的 5 个测试文件共 16 项聚焦测试通过，`bun run typecheck` 退出码为 0。catalog 后续加入的 `llmlint-full-review` 也使用该动态合同，但本轮未执行依赖外部 llmlint 命令的完整 workflow 专项冒烟。

## Known Follow-ups

- 一次性对话模型接入（Task 136，设计已收敛、待用户确认开工）：见 [#19](https://github.com/notnotype/neuro-book/issues/19) 与 [Task 136](docs/tasks/136-one-shot-model-providers/README.md)。
- Nitro dev source-map 临时补丁复核（等上游稳定版实际包含修复后再移除补丁与登记）：见 [#20](https://github.com/notnotype/neuro-book/issues/20)。
- 写作产品线后续批次（dogfooding 写书轮 + Task 116 剩余真 provider 挂账）：见 [#21](https://github.com/notnotype/neuro-book/issues/21)。
- 整书导入 / 作品导入（承载树反建 + Plot 结构提议 + World Engine 渐进初始化）：见 [#22](https://github.com/notnotype/neuro-book/issues/22)。
- **[Task 87 已订正·非回归]** `NovelChapterPanel` 的「本章剧情」widget 曾被判为路由契约回归（仍向 `GET /api/projects/plot/chapter` 传旧 `chapterPath` → 400），**核实后推翻**：该组件是全仓无引用的 legacy novel-model 死代码（左栏实际用 `WorkspaceCharacterPanel` + `NovelPlotPanel`），400 运行时不触发，不为死代码建反向桥（用户拍板）。收尾单列：清理 legacy novel-chapter view 死簇 + 给 plot 前端 `$fetch` 配 typed query 防契约漂移漏网，详见 [Task 87 walkthrough](docs/tasks/87-plot-two-trees-and-writer-modes/README.md) TODO。
- World Engine 写作模式主路径已阶段收尾；后续重点是体验打磨、`memory.jsonl` / `state.md` 是否显式 commit、以及真实作者长期使用反馈。
- Plot / World Engine 桥接的 Agent 易用性第一阶段已完成；当前普通写作主链已在 Round 141 改为 leader.default 直接负责 Plot / Scene，leader 持有全套 Plot 读写和 brief 工具。**Task 87 后 writer 在 autonomous 模式下已获得 Plot 只读**（`get_chapter_writer_brief` / `get_story_chapter` / `get_story_scene_context` 等，只读不写；工具名已按 Task 97 Slice 1 重排：读统一 `get_story_*` 前缀，写合并为 `save_story_*` + 显式 action），world.engine 工具面未扩大。Round 140 completion audit 覆盖了原 Director + Brief Compiler 完成态；Round 141 是之后的 profile 主链调整。真实 Agent 模型行为 smoke 尚未执行，因此不声明模型在真实会话中的规划质量已验证。后续章节级 POV、语气、节奏、信息控制、开头/收尾和禁写事项已由 Task 87 的 `ChapterBrief`（`StoryChapter` 字段组，吸收原 Task 80 ChapterOverride）落地，brief 编译器双模式只做了 autonomous，curated + 反问逃脱口延后。Task 78 Round 63-135 是实现前设计、风险和 runbook 记录；当前状态以 Round 136 之后的完成/调整记录为准。
- World Engine 后续单独讨论更大的 API 形态：是否增加 subject 视角便利查询、批量读取命名、列表过滤 DSL。
- RP 模式恢复时需要重新设计入口、profile routing、simulation 资料使用边界。
- Agent 用户输入 pending 已按 Task 63 收口到 durable user resolution 语义和唯一底部多 pending 面板：问答、审批、模式切换与 Low-Code Form 必须逐项明确处理后统一提交 `resolutions[]`，不再自动批准后续项；普通 Composer 被替换但保留正文/模型/图片草稿，回答与终止分别由 `canResolveUserInput` / `canAbort` 控制。面板总高度固定为响应式 320–420px，切题只滚动内部内容；开放回答、其他答案和补充说明复用 Composer 富文本编辑器，支持引用/selection/skill chip，但仍按 pending item 隔离草稿，图片 resolution 合同尚未扩展。提交复用 Task 129 Project owner 并绑定主 Session 与 pending batch，迟到结果和旧 `finally` 不回填新批次；12 files / 118 tests 与根 typecheck 通过，真实浏览器高度、chip 交互与多窗口同步仍待用户授权验收。
- Agent session 列表已新增分页与服务端搜索；后续如果 Workspace Root 中 session 数增长到数千级，再设计持久化 session 摘要索引。
- Session 摘要空闲触发（idle debounce 后台摘要，Task 96 拍板暂缓）：见 [#23](https://github.com/notnotype/neuro-book/issues/23)。
- Agent command 轻控制路径已移出完整 snapshot，relation/profile 热路径一致性已补齐；后续若完整 snapshot 首屏仍慢，优先单独治理 `snapshotSystemPrompt` 冷路径成本。
- Profile 编译系统底层重构（Task 79）已完成 Phase 0–3 收口：读路径硬切、内容寻址 artifact、原子 manifest、`compile_failed` 记账、严格无 stale、主线程 in-process Publisher、Registry 翻转、自动编译 Coordinator、boot sweep、profile assets sync Publisher 化、artifact grace GC、独立 ArtifactStore/FreshnessChecker/Registry/SourceWatcher 组件、全量 worker 池 fan-out/fan-in、发布前 source file set + entry source hash/bytes 重校验、磁盘发布成功后 source 不回滚、Registry 半成功 committed error、profile/workspace 测试 root 隔离与嵌套恢复均已落地；`.compiled` 新格式稳定契约见 `reference/agent/profile-compiled-artifacts.md`。
