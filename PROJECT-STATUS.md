# Project Status

## Summary

neuro-book 当前处于快速开发阶段。本轮产品主路径收敛到 **novel 写作模式 v1**：作者围绕 Markdown Studio、Project Workspace、Agent、World Engine 和 Scene-only Plot 工作台写作；旧 RAG subject 面板 / legacy simulation 默认模板从普通入口隐藏，底层代码和历史资料保留。写作提示词主链已收口为 `灵感探索 → 项目/Lorebook → World Engine 初始化 → 08 剧情规划/状态推进 → 09 章节写作 → 写后回补/修订`。**RP 模式 v2 运行逻辑优化 P1-P9 已完成数据面实现**（Task 100）：除开团门禁、唯一 turn/幂等提交、主动事件、规则结算、层级地图/NPC、十阶段 pipeline、关注度/强度、四分页、更新窗口和可恢复世界切片树外，现已具备 light/standard/deep 一致性审计、安全索引修复、切片损坏问题报告、玩家确认式祖先恢复候选和仅测试固定随机。100 Tick 规模场景覆盖 20 角色、30 地图节点、10 周期资源、三档强度、四次长跳与三条时间线并以 deep healthy 收口。首次服务器实测已修复九个 RP 多操作工具的 Provider schema 根类型缺失、RP 页面重挂载时 session 恢复缺口、开团确认入口缺失、Agent 猜写 Schema/Calendar 导致的 config 循环、Bootstrap map/characters 阶段职责权限缺口，以及 world_snapshot 越权审批先于权限校验和 get_agent 状态硬编码造成的首回合假失败。Bootstrap 现由服务端六阶段状态机逐步验收，配置阶段由受信工具生成真实 Zod Schema 与 Gregorian/Simple Calendar；地图阶段由 screenwriter 逐地点提交 bootstrap 提案、world review；角色阶段允许 world 登记已确认具名 NPC，但未具名群演不入 roster，没有具名初始 NPC 时空 roster 合法。失败停留原阶段直接修正；开场正文先写 staging，只有 World/地图/角色/事件/叙事全部通过并激活后才发布到 Tick 000000。工具领域预授权现统一发生在任何审批或用户输入 pending 之前，get_agent 投影真实 waiting/running/idle。RP 侧栏 overview 现先检查独立世界线初始化状态：企划引导期缺少 `rp/world-engine/` 时正常返回 intake 与侧栏数据，不会提前触发 Calendar loader；初始化完成后才读取 RP 角色主体。剩余为用户决定是否执行浏览器交互验收；RP 与写作模式继续在项目内完全分离。

2026-07-31 发布准备：本分支按 RP 专用 prerelease 版本 `0.8.19-rp` 发布（用户侧语义为 `0.8.19.rp`）。默认 Global Config 已恢复为初始未配置状态：不预置模型 Provider、默认模型、API Key、Agent Profile 覆盖或额外提示词条目；新用户需要在设置中心自行配置 API 与 Agent 偏好。

## Product / Workspace Facts

- 部署代码已收口到独立包 `@notnotype/neuro-book-manager`，公开命令是 `neuro-book`；实例发现、离线检查、导入和Source接管使用独立Module，无参数入口按当前目录切换实例管理、损坏实例处理、Git checkout接管和新部署菜单，blessed TUI同时展示已注册与待导入/接管候选。用户级配置只保存偏好、默认实例、目录索引和有限搜索根，部署真相源仍是各Installation Root的`.deploy/installation.json`。
- 推荐顺序为 Windows Portable、GHCR、Product Bun、Source Dev/Source Product、Source Docker。Installation Manifest v4固定记录Source/Product/Manager/Manager Runtime/Application Runtime/Tools与容器Profile选定的Docker/Podman engine；Release Manifest v3完整覆盖Windows x64、Linux x64/AArch64 glibc、macOS x64/ARM64 Product。Windows Portable zip包含根源码、根`.output`、managed Bun/rg/PortableGit/bash、版本化Manager和`data/` State Root。
- 本地一键启动版：`/0.82/`（commit `ae09982b`，第一版）与 `/0.83/`（commit `5bba77d7`，2026-07-27：RP 实测修复/开团引导/正文面板/ComfyUI 插图）解压于仓库根、已 gitignore；本地打包流程记录在 `docs/tasks/26-windows-portable-packaging/README.md`「本地一键启动打包记录」。
- `NEURO_BOOK_STATE_ROOT` 是状态路径真相源：默认 State Root 等于 Installation Root，Windows Portable 为 `data/`。Workspace Root、Boot Config、Product `.env`、SQLite 相对路径和日志均从 State Root 解析，公开 Project Path 仍为 `workspace/{project}`。Agent session只持久化Agent Workspace Root Reference，运行时文件工具按当前State Root解析Agent Workspace Filesystem Root；Manager会诊断Portable根`workspace/`与`data/workspace/`的数据分叉。
- 鉴权属于 State Root `config.yaml` 的 Boot Config：服务器 Profile 默认开启，Windows Portable 默认关闭；`Create Admin.cmd` 通过 Manager 调用 Product 管理员脚本。管理员 API 统一走同一访问守卫；关闭时管理员接口也无鉴权。配置中心只读展示当前进程状态，不提供热写。
- GHCR 安装不在宿主机 clone 源码；Manager 生成 `.deploy/docker-compose.generated.yml` 并拉取 Release Manifest 固定 digest 的镜像。镜像 `/app` 携带完整源码和 `.output`，Runtime/Tool provider 记录为 `container`。
- Product Application Root按只读资产处理。Profile/Variable运行时只验证内置artifact，用户编译、同步与动态import cache统一写入State Root的`workspace/.nbook/agent/.staging/`；Docker Compose挂载State Root `.env`，不通过root用户或修改`/app`权限换取可写性。
- `docker-compose.yml` 端口映射为 `${NUXT_PORT}:${NUXT_PORT}`（容器内按 Boot Config 监听 NUXT_PORT）；旧的 `${NUXT_PORT}:3000` 映射在非 3000 端口下不通，已修复。
- Windows Product Portable 错误报告日志落在 `data/logs/`；后端提供 `/api/app/logs/status` 与 `/api/app/logs/download`，日志包只包含日志和 manifest，不包含 config、数据库或 workspace 正文。
- Windows Portable可通过Manager `start --no-health-check`临时跳过HTTP启动探测、120秒终止和自动打开浏览器；恢复、迁移、State Root与前台Product生命周期合同仍保留，其他Profile拒绝该参数。
- 数据库已硬切 SQLite-only：App SQLite默认位于 State Root的`workspace/.nbook/neuro-book.sqlite`；Project SQLite 位于每个 Project Workspace 的 `.nbook/project.sqlite`。App SQLite Location统一服务Product Runtime、Prisma CLI、Manager备份与Docker：相对`file:`值必须留在State Root，原生Profile可明确使用外部绝对文件，Docker拒绝未建模的外部数据库；配置继续保存可迁移逻辑URL，不受Product cwd影响。
- Project Workspace 根目录 `project.yaml` 是项目身份真相源，App SQLite 不维护 Project index 或 `Novel` mapping。
- Project Workspace File Index 使用弱一致扫描：扫描期间消失的路径仅在 `ENOENT` 时从本轮 snapshot 忽略，权限、越界与其他 I/O 错误继续失败。Project 下载在 OS 临时区对 Project SQLite 和已有 History SQLite 分别生成独立在线 snapshot，不复制 live WAL/SHM；History SQLite 可能包含全文与已删除内容，前端会提示分享隐私风险。
- Boot Config 位于 State Root 的 `config.yaml`，包含启动/部署字段与 `auth.enabled`；开发环境缺省免密、生产环境缺省开启，修改后需重启。Global Config 位于 State Root 的 `workspace/.nbook/config.json`，Project 覆盖位于 `workspace/{project}/.nbook/config.json`；这些运行态文件不进 Git。
- 默认 Project 模板保留 `manuscript/`、`lorebook/`、`agents/`、`manual/`、`reference/`、`world-engine/` 等写作骨架。

## Writing Mode v1

- 普通写作入口以 Novel IDE / Markdown Studio 为主，顶栏保留 Bookshelf / World / Plot / User Assets / Agent。
- 顶栏重新暴露 Plot Workbench，且小屏下保持在视口内；RAG Inspector 仍不暴露，欢迎页仍不暴露 Plot / RAG / simulation 快捷入口。
- 左侧侧栏保留 Files / Characters / Plot；Agent 模式侧栏保留 Sessions，并提供 Plot 快捷入口用于切回 IDE Plot 面板；Outline 和 RAG 面板入口隐藏，`NovelPlotPanel` / `NovelRagPanel` 底层组件保留。
- Agent 新建菜单隐藏 `rp.leader` 和 `simulator.leader`；历史 session 的 profile 名称、图标和旧 profile 文件保留。
- Agent 关联生命周期已收口：link/detach 继续作为 append-only 历史账本保存，关系索引维护 archived 集合并为 Runtime、Profile、工具、snapshot、relations API 和关联面板提供同一当前关系语义。单进程关系变更队列串行 create/link、detach、archive，禁止归档后并发写入新 link；归档统一解除全部入站/出站关系，手动与 Project Workspace 批量归档语义一致。跨 JSONL 文件写入按 detach→archive 顺序可重试，不宣称原子事务。
- 默认写作 workflow 中，`08` 是 World Engine 剧情事实确认与状态推进手册，`09` 只在状态已推进后调用 writer 写正文；修订产生新事实时回到 `08` 做 World Engine 回补。
- Markdown Studio 的 Inline AI PromptBar 使用 `inline.editor` 后台 session：发送后不自动打开右侧 Agent 面板、不切换主 Agent active session；PromptBar 内可选择当前 Project 的 Inline AI sessions、独立新建 session、原地调整 inline session 模型，并流式展示当前 session 的思考、正文、edit/write 预览与最终摘要。
- 默认 Project 模板的 `agents/` 只创建 `leader.default/` 与 `writer/` 上下文，不再默认创建 `director`、`rp.leader`、`rp.writer` 或 `simulator.leader`。
- 本版本目标是先把写作模式体验打顺，RP 模式后续再恢复和重新设计入口。

## World Engine

- World Engine 是写作模式的动态世界状态与时间线真相源，用于替代旧 Plot 系统和 `simulation/` 默认运行态。
- Plot System 已通过 Task 78 → Task 87 演进：Task 78 收敛为 Scene-only 桥接层，Task 87 升级为**两棵树**（承载树 Story→StoryAct→StoryChapter→Prose、因果树 Story→StoryPhase→StoryThread→StoryScene，在 `StoryScene.chapterId` 交汇）。`StoryPlot / Plot Beat` 不再是正式模型；`StoryScene` 持久化 `startInstant/endInstant/subjectIdsJson/locationSubjectId` 与 `chapterId` 外键，Scene 可通过服务端 API 查询相关 World Engine slices 和 subject states，并能从 Plot World Context 打开真实 World Engine Workbench。`StoryChapter` 带 ChapterBrief 字段组（章级 writer 指令），Chapter↔Prose 通过 manuscript frontmatter `chapter: <name>` 反指。Scene World Anchor 读取 DTO 会解析 `subjects/locationSubject/unresolvedSubjectIds`，允许占位 subject ID，但 UI/API/Agent 必须显式暴露 unresolved 状态。
- 后端核心是 Project SQLite 三表：`WorldSubject`、`WorldSlice`、`WorldPatch`；公开写入已硬切为 `patches` + 4-op（`replace` / `increment` / `remove` / `append`），collection 支持 `remove + value` 按 stable JSON 值删除元素，patch 不存旧值字段，后端不自动改写后续切面。
- HTTP API 覆盖 schema、subjects、slices、slice delete、state/query；公开时间入参拒绝 raw instant 调试格式、首尾空白和非法 percent encoding。
- Agent 内置 World Engine 工具收敛为单一 `execute_world`：Leader / world.engine 使用 readwrite CodeAct，公共 API 为分组形态 `world.time.*`、`world.subject.*`、`world.search.*`、`world.slice.*`；writer 使用 readonly 模式，不注入 `world.slice.write/editPatches/delete`。旧的 8 个固定工具、Task 69 三工具、`codePath` 查询分支、旧 6-op 写入入口、旧平铺 `world.*` API 和 `world.getMany` 不再保留。
- 默认 Project 模板包含 `world-engine/schema/index.ts`（**Zod schema，硬切，不再支持 `schema.yaml`**）和 `world-engine/calendar.ts`，新 Project 默认 Calendar 已切到 Gregorian 现实日历，format 到分钟、不带秒；新 Project 不再默认生成 `simulation/`。
- World Engine reference 与 writing workflow 已对齐当前协议：Agent 统一走 `execute_world`，脚本只 return 数据，issues 由运行时 collector 汇总；查询状态时优先在 CodeAct 内把已知 schema 的 attrs 转成文本摘要并 `return string`，工具文本会直接展示该摘要，结构化 `{data, issues}` 仍保留在 details；`world.subject.get()` 返回 attrs 本体；时间在沙箱内用 `world.time.parse/format` 和 instant bigint 互转，读写路径统一 JSON Pointer，同一 instant 只能有一个 slice，同刻多变更必须合并到同一个 `patches` 数组；误写单条 patch 或向已有 slice 补已登记 subject 时优先用 `world.slice.editPatches` 精确修正，冲突错误会返回 `existingSliceId/time/title` 并引导合并 patches；按 subject 查相关切面走 `world.slice.list({subjectIds})`，`world.slice.get` 只接受 sliceId。旧 API 名称仅保留在 migration / legacy / historical research 语境。
- Calendar 已硬切到 `calendar.ts`，不再兼容 `calendar.yaml`；支持 `simple`、`gregorian`、`custom` 三类策略，缺少 `calendar.ts` 时应提示创建；Gregorian calendar 已覆盖公元前年份与默认现实时间 `公元2020年4月12日 18:00` 的 parse/format 往返。`calendar.ts` 与 `schema/index.ts` 是单文件配置入口：入口内容变化可热加载，本地文件、绝对路径和 URL/protocol import/export 会被拒绝，包级 import 与 `node:` 内置模块继续允许；loader 会先把入口转译为内容 hash `.mjs`，再通过 server 原生动态 import seam 与物理 artifact cache 加载，不依赖宿主直接 import TypeScript，也避免 Product bundle resolver 或 Project 目录临时文件影响运行时生成模块导入。
- EmbeddingText 容器初始化死锁已修复：空 `replace /events []` / `replace /memory {}` 可作为 default 初始化基准，非空 embedding 内容仍必须按单条 patch 写入，错误信息会提示 `{text:"..."}` 写法；`vector/model` 由系统维护，显式字段和内部路径绕过都会被拒绝。
- Round 423 已用临时 Project 验证默认模板 API 链路：`calendar.ts` 时间格式下创建 `world/player`、写入 slice、查询 state、删除 slice 和状态回退均通过且 issues 为 0。
- Round 424 已用临时 Project 验证主 IDE Workbench 空项目第一步：默认模板 Project 可打开 Workbench，看到 schema/calendar 入口与创建入口，`创建 world subject` 会真实写入 `world` subject 和 init slice；临时 Project 已清理。
- Round 425 已完成阶段收尾审计：Round 380-424 的真实项目、新 Project、默认模板、连续推演、常用操作和 Calendar 证据足以证明当前“前后端雏形拼接 + 作者视角主路径”阶段已跑通；后续进入体验打磨和新产品决策。
- 主 IDE World Engine Workbench 支持创建 subject、写入 / 编辑 / 删除 slice、查询 state、展示 issues，并能从历史 `simulation/subjects` 发现真实主体系统摘要；该发现路径不代表 `simulation/` 是写作模式默认状态源。Workbench 已移除“示例世界”入口，SliceCard 主画布按 `path / op label / summary` 展示 patch。

## Hidden Legacy Systems

- RAG subject 面板、RAG Inspector、RP profiles、simulation workflow 和 archived simulation 模板都保留在仓库中，方便历史项目和后续恢复；Plot System 已在普通写作入口恢复为工作台（Task 87 后为两棵树：承载树 Act/Chapter/Prose + 因果树 Phase/Thread/Scene）。
- `novel-workflow-05-emulation-bootstrap` 与 `novel-workflow-06-emulation-tick` 保留为 legacy RP / simulation skill，不作为普通写作模式推荐路径。
- `workspace/ming-ding-zhi-shi-2` 已标注为 legacy RP / SillyTavern 导入项目，可作为复杂 schema/calendar 参考，但不是普通写作模式 World Engine 端到端验收样本。
- `assets/workspace/.nbook/templates/project-directory-templates/simulation/` 已归档到 `assets/workspace/.nbook/templates/archived/project-directory-templates/simulation/`。
- 新建 Project 不复制根目录 `simulation/`；真实用户 Project 中已有的 `simulation/` 数据不迁移、不删除。
- 用户 assets 同步会清理未手改的旧 `templates/project-directory-templates/simulation/**` 受管副本；手改副本按现有冲突规则保留。

## Recent Tasks

| Task | Status | Notes |
| --- | --- | --- |
| [113 Agent Profile Prompt Settings](docs/tasks/113-agent-profile-prompt-settings/README.md) | Implemented（浏览器验收由用户决定） | 配置中心 Agent Profile 模型页已收敛：运行默认值/单 Profile 运行策略默认折叠；模型选择、Temperature、推理强度和实时输出均有真实运行时消费者，删除无 Pi 通用合同的 TopK。提示词改为“前置条目 → 固定 Profile 结构 → 末尾条目”双槽，可增删、启停、槽内排序、跨槽切换，并支持包含提示词与特色设置的整套预设；19 个内置 Profile 均已接线。新增服务端真实 `profile.prepare()` 完整预览，可切当前草稿/已保存配置，未保存 Profile Home 资源仅进内存覆盖层；必填 initial 的 RP Actor 等特殊 Profile 自动使用同 Profile 真实 Session 上下文，不再空对象解析或伪造角色数据。Compaction Prompt/摘要前缀退出普通配置并固定为 Harness 内部常量。每次 invocation 重新读取 effective config；热更新专项 2/2 证明同一 Session 下设置下一次调用立即生效。后续验收修复已抽出同职责共享契约：`rp.writer` 获得 8 项 Writer 设置，`simulator.actor` 获得 5 项 Actor 设置；Leader 原有 5 项设置改为明确“特色设置”分区。最新保存故障根因是 `leader.default` 旧 Home 缺少默认提示词资源，现已通过 manifest v2 + `home.upgrade()` 系统迁移修复；提示词启停改为明确“已启用 / 已禁用”，预设标明页面草稿语义，保存失败提供全局通知。专项 5 files / 50 tests 通过，真实 runtime Home 已升级且 settings issues 为空。组合验证 281/339 通过；其余为 Windows symlink 环境阻断的 57 项初始化和 1 条无关既有 Harness 断言。typecheck 仅剩既有 World Engine TS2589，浏览器验收未自动执行。 |
| [112 novel-audio-script-distill 音声台本拆解 skill](docs/tasks/112-audio-script-distill-skill/README.md) | Implemented (待真实会话实测) | 新增 bundled workspace skill `novel-audio-script-distill`：从音声作品台本（WEBVTT/SRT/LRC/剧本体）提取角色人设、故事发展、世界观、说话风格与叙事手法，完全对齐 `novel-book-distill` 的两阶段卡片契约（暂存 `reference/distill/{作品名}/`，六类卡片，采纳后转写 lorebook / 归档 reference/），证据字段用 `tracks` 替代 `chapters`。音声特有方法论在 references/：单侧台词的听者言行还原（复述/回应反推/空隙补完三级手法）、出声角色≠听者角色≠视点（双听者 NTR 结构）、推断分级强制标注（【实证】/【强推断】/【弱推断】，仅前两级可进 lorebook）、A/B/C 噪音分级过滤露骨拟声行。原始台本归档 `reference/audio-script/{作品名}/`。纯提示词 skill，真实实测待执行。 |
| [112 ComfyUI 生图插画](docs/tasks/112-comfyui-illustration/README.md) | Implemented (待真实 ComfyUI 出图验收) | 小说/RP 正文引用生图：Nitro 直连本地 ComfyUI 原生 API（/prompt + WS 进度 + /history + /view），新模块 `server/comfyui/`（client/ws-listener/workflow-template/user-workflows/job-manager/illustration-store/prompt-distill）；配置中心新增 `comfyui` Global-only section（连接/测试连接/蒸馏模型/提示词前后缀/参数默认/工作流导入与注入点 mapping）；悬浮面板（DialogWindow）调参+SSE 进度+插入正文；Markdown Studio 走 BubbleMenu 选区 + `insertMarkdownAt`，RP 走自建选区浮层 + prose.md 锚点四级降级写回（409 确认追加）；图片落 `<project>/assets/illustrations/`，新增 `GET /api/workspace-files/raw`（魔数+ETag），TipTap/marked 双端渲染层重写相对 src、序列化保持相对路径。LLM 蒸馏复用 Pi runtime（trace 记 health-check bucket）。mock ComfyUI 端到端测试 + 浏览器 UI 自测通过（含真实 deepseek 蒸馏）；真实 ComfyUI 出图待用户环境验收。 |
| [111 novel-book-distill 拆书 skill](docs/tasks/111-novel-book-distill-skill/README.md) | Implemented (待真实会话实测) | 新增 bundled workspace skill `novel-book-distill`：从外部小说/设定文本提取设定、人设、情节、故事线、文风、写作手法，卡片化暂存 `reference/distill/{书名}/`（`_meta.json` 进度真相源 + `cards/` + `INDEX.md`），两阶段流程——阶段 A 只写暂存区，阶段 B 按用户点名把 setting/character 按节点契约转写进 lorebook（正式节点 + 来源 tags + 变形确认）、plot/storyline/style/technique 归档 `reference/plot-patterns|style|technique/`。独立能力，不与其他 skill 做流程衔接；防搬运边界（仅文风卡留短引文）。纯提示词 skill，真实拆书实测待执行。 |
| [110 Agent Workflow 编排系统](docs/tasks/110-agent-workflow-orchestration/README.md) | Initial integration verified | 脚本式durable workflow（不引入图DSL）。内核已从sibling仓`nb-workflow`端口化并vendor进`server/vendor/nb-workflow/`（`sync:nb-workflow`），跑在真实`JsonlSessionRepository`上：checkout=moveLeaf、append显式锚定、excursion旁支、acquire按tag跨run复用；session meta落地`kind`/`tags`子集与`origin:"workflow"`。demo页`/workflow.preview`含四个mock×真实session场景+一个真模型并发场景；适配器测试4/4、typecheck全绿。仍属初步接入：run态内存、轮询非SSE、面B工具/waiting穿透/沙盒化/sidecar迁移未做（D2-D5、D13待确认）。 |
| [108 Agent Attachment 存储内核与图片引用](docs/tasks/108-agent-image-attachment-references/README.md) | Implementing | Attachment locator由Session Entry Context携带Project身份并执行open gate，route保留typed 409。Migration manifest严格绑定Session真相源与本次run路径，拒绝重叠、跨控制区及symlink/junction；POSIX create/rename补父目录fsync。公开Product/canary与浏览器图片展示仍待验收。 |
| [109 统一 File Scope、File Address 与 Product Runtime 路径合同](docs/tasks/109-agent-workspace-path-runtime/README.md) | Implementing / Integrated locally | read/write/edit/apply_patch与Subject共用Authorized File Operation，统一目标Project open gate、File Address与realpath containment；Bash明确是受信任完整Shell，只通过`authorizeProcessCwd()`验证当前Project与cwd，不属于文件级隔离承诺。公开Product链仍待验证。 |
| [22 Agent Public Event Projection](docs/tasks/22-agent-public-event-projection/README.md) | Implementing / Integrated locally | 原始三形态、有界公开投影和recovery cursor保持成立；Public Tool Identity已覆盖live/durable/history/HTTP/SSE/client patch入口并fail closed。既有浏览器Session证据仍是历史基线，但104–109合并后的公开Product与统一门禁尚未重验，不能维持Verified。 |
| [107 Agent Runtime Event OOM 与 SSE 内存边界](docs/tasks/107-agent-event-memory-boundaries/README.md) | Implementing / Integrated locally | live/replay/SSE有界治理保持成立；Provider终态、事件、durable history与HTTP输入共用512 UTF-8 byte tool-call identity合同。聚焦事件/HTTP/Harness回归通过；公开Product与浏览器SSE证据仍待统一门禁。 |
| [106 Agent Chat Flow Snapshot 分页](docs/tasks/106-agent-chat-flow-pagination/README.md) | Implementing / Integrated locally | 单一session GET与opaque cursor合同保持不变，未发现raw`snapshot.entries`公开旁路。网络窗口与前端常驻历史有界，但Repository仍为O(session file size)；浏览器长session及cold/hot/read bytes/memory证据待完成。 |
| [105 统一安装目录与 NeuroBook Manager](docs/tasks/105-unified-installation-manager/README.md) | Implementing / Canary A verified | `0.8.19` workflow全绿并发布最终Manifest/SHA256SUMS：五平台Product、原生双架构OCI、Windows完整`0.8.6 data/`复用、Docker双架构与rootless Podman完整用户链均通过。原生OCI约7–8分钟，完整workflow 27.4分钟；A→B事务update与Apple Silicon双engine设备证据仍待完成。 |
| [104 Pi Models Runtime Upgrade](docs/tasks/104-pi-models-runtime-upgrade/README.md) | Implementing / Integrated locally | Provider Config ID、Base URL与proxy保持不可隐式迁移；`modelApi`已纠正为可编辑的discovery/候选补全偏好，同端点可复用saved Secret且不改已有`model.api`。一键修复可从一致的已保存模型API确定性补全，混合/空模型不猜测；Session仍只保存Provider Config ID+Model ID。真实配置写回与浏览器验收仍未完成。 |
| [103 AGPL-3.0-only 许可证迁移](docs/tasks/103-agpl-license-migration/README.md) | Implemented | NeuroBook 与 sibling llmlint 已从 PolyForm Noncommercial 迁移到 AGPL-3.0-only；本地第三方文风、写作参考和旧致谢退出 Git / Product source snapshot，llmlint 的 manifest、README、vendored 与 user runtime 许可声明已同步，聚焦资产同步测试通过。 |
| [03 Config System](docs/tasks/03-config-system/README.md) | Updated | Global Config 的模型结构现为完整 Provider Config；Agent session 稳定身份仍为 `providerConfigId + model.id`。2026-07-14确认Config Service测试曾直接rename真实`workspace/.nbook/config.json`，测试中断会把用户配置遗留在系统临时目录；已用7月11日备份恢复，并把39项Config Service测试全部迁入统一临时Workspace assets Context，失败或中断不再触碰真实Global Config。 |
| [56 World Engine](docs/tasks/56-world-engine/README.md) | Stage Complete | 核心模型、API、Workbench、subject system discovery 与作者主路径已完成阶段收尾审计。 |
| [64 World Engine Prompt Engineering](docs/tasks/64-world-engine-prompt-engineering/README.md) | Updated | 写作模式提示词主链与模板已收口：`08` 负责 World Engine 剧情推进，`09` 负责状态已推进后的 writer 写作，legacy RP/simulation 从普通入口降级。 |
| [65 Calendar Enhancement](docs/tasks/65-world-engine-calendar-enhancement/README.md) | Done | `calendar.ts` 硬切，`calendar.yaml` 仅作为历史记录。 |
| [66 Codebase Cleanup](docs/tasks/66-codebase-cleanup/README.md) | Stage Complete | 已完成一轮 World Engine / 写作模式阶段后的代码清理收口：Workbench 纯规则下沉、filter preservation、draft surface auto-open、issue level/status mapping 和专用 util 测试拆分已落地；命名 / 文件结构与复杂主体语境候选已记录待审批，后续等待真实作者使用反馈或用户重新开启。 |
| [69 World Engine Tool Cleanup](docs/tasks/69-world-engine-tool-cleanup/README.md) | Done | 历史任务：旧协议收口到 `patches/path/4-op`，运行时/DB 术语改为 `WorldPatch`，collection 支持按值删，内部全量查询统一走 `queryState`；其 Agent 三工具形态已在 Task 71 继续收敛为 `execute_world`。 |
| [71 World Engine CodeAct Readwrite](docs/tasks/71-world-engine-codeact-readwrite/README.md) | Implemented | Agent World Engine 工具收敛为单一 `execute_world`，读写合一进入 CodeAct deferred 事务；新增 issue collector、`editMutations` 精确编辑、`parseTime` / `formatTime`、`slices({withPatches:true})` patchId 与 JSON Pointer 读写统一。 |
| [72 Error Report Logs](docs/tasks/72-error-report-logs/README.md) | Implemented | 新增后端 JSONL 日志、日志包下载 API、请求/异常/Agent 摘要日志和 Windows portable `data/logs/` 落盘。 |
| [73 Agent Session List Performance Pagination](docs/tasks/73-agent-session-list-performance-pagination/README.md) | Implemented | `/api/agent/sessions` 改为分页 Page 返回，新增服务端 search；列表路径使用轻量运行态摘要和 profileKey 批量解析，避免按 session 重复解析 profile catalog。 |
| [74 Agent Command Performance](docs/tasks/74-agent-command-performance/README.md) | Implemented | `/api/agent/sessions/:id/commands` 轻控制命令返回 live state，不再生成完整 snapshot；relation index 已收敛到 write executor after-write observer，并修复 rebuild pending link/detach 漏账；profile catalog dirty cache 已加 generation race 防护和 HTTP runtime watcher。 |
| [75 World Engine API / Calendar / EmbeddingText 收口](docs/tasks/75-world-engine-api-calendar-embedding-cleanup/README.md) | Implemented | World API、Calendar、loader与runtime artifact合同已收口；`ming-ding-zhi-shi-2`的events和character memory已切为`EmbeddingText`。受控SQLite migration默认dry-run，apply前备份并在单事务内重写，验证patch计数与slice文本reduce等价；`world.search.text`现在区分无检索能力的400与合法空结果。 |
| [78 Plot Scene / World Engine Bridge](docs/tasks/78-plot-scene-world-engine-bridge/README.md) | Complete | Plot System 已收敛为 Scene-only：Scene 通过时间范围、地点 subject 和出场 subject 连接 World Engine；Plot 入口已恢复到顶栏、左侧侧栏、Agent 模式侧栏快捷入口和 Plot 面板内按钮；旧 `StoryPlot / Plot Beat` 已从正式模型、工具和 UI 中删除，旧项目迁移会备份并合并数据到 Scene。Agent 易用性主链已改为 leader.default 直接负责 Plot / Scene：剧情初步设计 -> 推进 World Engine -> 剧情设计 -> 更新 Plot -> 调用 writer；leader.default 持有 Thread / Scene 读写、Scene World Context 和 `get_chapter_writer_brief`，director 保留为高级/手动 profile，不再是普通写作必经节点；writer 仍无 Plot tools，world.engine 仍只负责复杂世界引擎维护。四个实现切片已完成：Profile Contract Cleanup、OpenAPI Explicit Path、Chapter Writer Brief Module、Agent Tool Binding；`get_chapter_writer_brief` text 返回 `suggestedBriefMarkdown`，details 返回完整 DTO。Round 140 completion audit 跑过 13 files / 62 tests passed；Round 141 调整 leader-owned Plot 主链；Round 142 补齐稳定 reference 与 server 侧测试门禁，移除不被 Vitest 收集的 assets writer 假门禁。真实 Agent 模型行为 smoke 与浏览器验证未执行，不作为 Task 78 完成门禁；ChapterOverride 已单列后续 Task 80。 |
| [79 Profile Build System](docs/tasks/79-profile-build-system/README.md) | Implemented | profile 编译系统 Phase 0–3 已收口：`editor-snapshot` 不再返回/触发 profile settings 大包；settings/build-status 专用接口与前端状态块已接入；`.compiled` 切为内容寻址 `artifacts/<sha>.mjs` + profileKey 映射 manifest；`compile_failed` 写入账本且 runtime 严格拒绝 stale/failed；HTTP runtime worker 只生成 staging release，server 主线程 `ProfileReleasePublisher` 经 `proper-lockfile` 做 in-process 原子发布并翻 Registry；`ProfileArtifactStore` / `ProfileFreshnessChecker` / `ProfileRegistry` / `ProfileSourceWatcher` 已从 catalog facade 拆出；保存/创建/删除/外部编辑 500ms 去抖后台编译，启动 boot sweep 非阻塞自愈 user profile；profile assets sync 改 staging→Publisher，copy-if-absent 并可修复损坏的同 sha user artifact；内容寻址 artifact GC 带 grace，worker service 已扩为受互斥约束的 worker 池，全量编译已改为 worker 池 entry fan-out、主线程 fan-in 一次 manifest 发布；single compile 改为 worker 只产 entry staging、Publisher 在 publish lock 内合并当前 manifest；Publisher 增加 per-root in-process 发布队列，确保磁盘 release 与 Registry 翻转同 root 串行；HTTP runtime user-assets sync 已改为 in-process 强一致返回，profile assets sync 一次同步只发布一个 batch patch release，真实 `.compiled` artifact 修复收敛到 Store publish lock 内；watcher freshness gate 已验证 artifact/type artifact 后再跳过编译，unlink 与 stale source-missing 升格 full build，避免删除/并发保存漏账；final guard 已锁定所有 full replacement 发布前校验 source file set + entry source hash/bytes；`profile status` 对 stale 返回非零，Product staging 会剔除 manifest 未引用 artifact，Windows portable 与 staging 均按 manifest 校验嵌套产物存在性和路径可迁移性；共享 Profile helper 语义变化已通过 compilerVersion 7 强制旧 bundle 失效重编；profile/workspace 测试 root 已隔离，三文件并行回归不再污染真实 user-assets。 |
| [80 Chapter Override / Writer Brief](docs/tasks/archived/chapter-override-writer-brief/README.md) | Archived | 从未落地，被 Task 87 吸收：`ChapterOverride` 更名 `ChapterBrief`，作为一等实体 `StoryChapter` 字段组落地，不再按 `chapterPath` 字符串关联。目录已归档到 `docs/tasks/archived/`。 |
| [81 Profile MCP Config](docs/tasks/81-profile-mcp-config/README.md) | Design | profile 级 MCP 支持先走配置化路线：Workspace Root 注册 MCP server，profile 通过显式 tool allowlist 启用 MCP tools；第一版不提供 profile helper 加载 MCP、不升级 inline execute 工具类型、不自动启用整个 server，并要求模型可见工具集合与执行硬权限共用同一份冻结 run tool snapshot。已补充 [ARCHITECTURE.md](docs/tasks/81-profile-mcp-config/ARCHITECTURE.md)，从性能、用户体验、加载逻辑、Harness 复杂度、开发人员心智、安全与失败恢复比较方案，推荐 `RunToolSnapshotResolver` 作为实现中心。 |
| [83 Project List Performance](docs/tasks/83-project-list-performance/README.md) | Implemented | `/api/projects` 已收口 5s 短缓存契约：include-only 查询与默认完整列表共享缓存，manifest/session count/per-project workspace+plot stats 分层缓存并带 in-flight 去重；Novel IDE 主入口不再发送 include-only query，World Engine preview 保留有限列表补回当前选择；route 已接 `Server-Timing` 与 500ms 慢日志，启动后后台渐进预热子缓存但不占用默认完整列表 pending。 |
| [84 llmlint Standalone Development Repo](docs/tasks/84-llmlint-standalone-repo/README.md) | Implemented | llmlint 真相源迁到 sibling `../llmlint` 开发仓，仓库根承载 `skill/`、`evals/`、`tests/` 和开发脚本；可安装 runtime package 固定为 `skill/`。新增 `scripts/cli/sync-llmlint-skill.ts` 将 `../llmlint/skill` 镜像到 NeuroBook bundled snapshot，user-assets 同步硬切清理旧 `.git`、`node_modules`、`evals` 和 `.gitignore`，真实 `workspace/.nbook/agent/skills/llmlint/` 已保持 runtime-only。验证通过 llmlint 根安装、根 typecheck、`bun run verify`、runtime、同步、NeuroBook typecheck 与关键测试；早先根依赖 resolver 阻塞已复查关闭。 |
| [85 Fullstack Template / UI Library](docs/tasks/85-fullstack-template-ui-library/README.md) | Implemented | 已创建 sibling `../nb-ui` 与 `../nb-fullstack-template`，没有移动 NeuroBook 或 llmlint 目录，也未迁移两个现有项目的源码 import。`nb-ui` 已补齐 `@notnotype/nb-ui/components` 手动导入导出面，保留 Nuxt module 自动注册，并把 `@nuxt/kit` 与 dev `vite.server.fs.allow` 契约修稳；模板默认依赖已切到 Bun 正确 link 形态 `link:@notnotype/nb-ui`，已明确 `link:../nb-ui` 是错误反例。v0.6 补齐 Button / Panel、表单常用属性、默认 dark/light theme、`applyNbTheme` 和组件 smoke tests；后续补强 `FormField` 语义 Interface、`--color-scheme` 主题 token、主题化 Notification、IconButton 类型导出、Dropdown 稳定 id、SegmentedControl group label 与 SwitchField switch 语义。模板改用共享 Button/Panel/Form，主题抽到 `app/theme/default-theme.ts`，补充 admin env、受保护页面和 `/api/protected` 示例，并新增 `app/middleware/auth.ts` 作为受保护页面标准 route middleware。数据库主路径 `db:setup = prisma generate -> db:create -> prisma migrate deploy -> init-db` 已修复 Prisma 7 SQLite 首次运行文件创建问题。验证通过 nb-ui typecheck/test/build、模板 typecheck/test/build、linked symlink frozen install，以及临时 SQLite 的 `db:setup` 非交互迁移和默认 admin 初始化。 |
| [86 Pi Request Observability](docs/tasks/86-pi-request-observability/README.md) | Implemented (待前端手动验收) | Pi 请求可观测层 P0+P1+可搬运化收尾已落地：`tracedStreamSimple/tracedCompleteSimple` 统一入口透明代理默认开启记录主 turn / sidecar turn / compaction 的每次 provider 请求（pi 规范化 context + 原生 payload、httpStatus/headers/usage/stopReason、TTFT/耗时；error/abort 由 finalize 挂 `result()` 兜底；请求侧 apiKey/headers 从不落盘，response.headers 经敏感头 denylist 过滤且豁免 ratelimit 头），落 `<repo.tracesRoot>`（`.nbook/agent/traces/<sessionId>/`），每 session 保留最近 N 条（默认 100）；health-check 划出范围不记录；统一入口由 `unified-entry-guard.test.ts` 扫描强制（allowlist 仅 traced-provider 与 model-settings）。前端：IDE 顶栏「Trace」入口（Plot 按钮旁，聊天顶栏挂点已移除）打开查看器——scope 可切「最近请求（跨 bucket 聚合 `GET /api/agent/traces/recent`，缺省）/ 某 session / `_system` 无 session 请求」，invocation 分组、六 tab 详情、trace↔session 双向跳转（先选 session 再开面板）、清空当前 bucket；+ 设置页「可观测」开关；`observability.piTrace` 是一等 config section。core（recorder/reader/traced-provider/view-model）零 NeuroBook 依赖（tracesRoot/onWriteError 注入），抽库 runbook 见 `reference/agent/pi-trace-observability.md`，建仓等第二个消费者。**traces 含完整 prompt 与小说正文，严禁进入 task 72 可分享日志包或任何导出诊断流程**。查看器 UI 未浏览器验证。 |
| [87 Plot 两棵树 + Writer 双模式 + ChapterBrief](docs/tasks/87-plot-two-trees-and-writer-modes/README.md) | Implemented (autonomous only) | Plot 从 Scene-only 升级为**两棵树**：承载树 Story→StoryAct→StoryChapter→Prose、因果树 Story→StoryPhase→StoryThread→StoryScene，在 `StoryScene.chapterId` 外键交汇；`StoryChapter` 新增 `brief*` 字段组（ChapterBrief：章节目标/POV/信息控制/开头收尾/禁写），吸收原 Task 80 ChapterOverride。Chapter↔Prose 通过 manuscript frontmatter `chapter: <name>` 反指，复用 `ProjectWorkspaceIndex`。brief 编译器 `getChapterWriterBrief(projectPath, chapterId, mode)` 支持防全知双模式，status 链新增 `needs_chapter_brief`（信息控制四字段全空时阻断 handoff）。writer profile 加 Plot 只读、PayloadSchema 用 `chapterId`。**本轮只做 autonomous 模式**：curated + 反问逃脱口因 profile 静态 toolKeys / harness 无 settings 门控 seam 延后。前端 Plot 面板已完整迁移：`chapters` 改从 StoryChapter 实体派生（不再扫 manuscript 文件树），面板子树 `chapterPath → chapterId` 全链路改名，新增 `PlotChapterEditorDialog`（章节 + 完整 ChapterBrief 表单）、章节管理条、新建卷对话框，以及只读接口 `GET /chapters/:id/prose` 的 Prose 关联视图；`bun run typecheck` 0 error，backend plot/profiles/API 测试全绿。**bootstrap 已在《命定之诗2》真实项目实测**：2 Act + 2 Chapter + frontmatter 反指 + 0 迁移孤儿、幂等重跑无副作用，并彻底修复实测炸出的 2 个单测测不到的 bug——(1) 事务超时：把 manuscript 扫描 / frontmatter 回写移出 Prisma interactive transaction，事务内 `applyCarrierTree` 只做纯 DB 写入；(2) libsql/bun/Windows 句柄不释放导致进程不退出 + 下次 SQLITE_BUSY：一次性 CLI 末尾显式 `process.exit`。新 UI 尚未浏览器验证。 |
| [88 Workshop Platform](docs/tasks/88-workshop-platform/README.md) | Phase 1 Implemented (后端 + Web) | NeuroBook Workshop（创意工坊）设计定稿并完成 **Phase 1 后端 + Web 前端**（sibling 仓 `nb-workshop`，从 `nb-fullstack-template` 派生）：Prisma 扩展 InviteCode / WorkshopItem（安装名 name、status 三态）/ ItemVersion / Like / Favorite / Comment / Report，API v1 全套 20 路由（公开浏览下载 / 登录社交与上传 / admin 签码举报下架），邀请码注册，zip + `nbook-package.json` manifest 校验（缺 manifest / 字段非法 / entry 缺失 / version 不递增提示 N+1 / 非首版 type-name 不一致全拒绝），文件落盘 `WORKSHOP_FILES_DIR`。实现后完成一轮审查 + 修复：API 清单对照零遗漏、39 条边角链路走查全通；修复邀请码并发双花（事务内条件更新）、同版本并发上传字节与库记录不一致（改为先写库后落盘 + 唯一约束 409 + 落盘失败补偿删除）、slug 并发抢注 500→409、下架条目收藏/点赞撤不掉（新增 `requireItemBySlug` 供撤销类操作）、meta 补 platformVersion。**Web 前端全量页面已建成**（2026-07-06）：`/` 浏览（筛选态映射 URL query）、`/items/:slug` 详情（双栏 + sticky 下载栏 + 点赞/收藏乐观更新 + 举报 + 评论区 + 404 态）、`/users/:username`、`/publish` 四步向导（前端 fflate 解析 manifest + 重试防 slug 409）、`/me`、`/admin`、`/login`+`/register`（补邀请码）；类型化 `useWorkshopApi` 单一 $fetch 出口，description 纯文本渲染规避存储型 XSS；补后端缺口 `GET /api/v1/me/items`（本人全部状态条目）。验证：install / typecheck / test / build 全绿，**41 测试**含 21 个真实 HTTP 集成用例（spawn build 产物、含并发用例），下载 sha256 与上传字节一致。**完全信任用户，安全防线全部记入安全债清单**按两个门回补。实施发现：Prisma 7 生成 client 的 `__dirname` polyfill 被 nitro bundle 后 Windows 启动即崩，`nb-workshop` 内已加 build-time rollup patch 兜底（模板仓未动）。上线前动作（ToS / 备份 / 种子内容 / 安全债回补）与 NeuroBook 客户端安装闭环（Phase 2）未开始；未做浏览器验证。 |
| [89 Theme System v2.1 & Custom Themes](docs/tasks/89-theme-system-v2/README.md) | Implemented (待浏览器验收) | Novel IDE 主题系统已收口到 36 个 v2.1 CSS 变量：8 套内置预设保留完整字面值，`theme-vars.css` fallback 与 `sepia` 同步，`--we-*` World Engine 别名层保留但改指向，app/ 中主要 Tailwind 调色板类与 `dark:` 变体已按语义变量迁移，分类色板按约束不迁移。主题 runtime 已支持 `ui.theme: string` 与 `ui.customThemes` 写入 Global Config，bootstrap 应用主题快照防 FOUC，Agent client variables、Monaco/diff、登录/Admin/Profile Template 入口均接入 resolved theme。设置页新增自定义主题编辑器：核心 13 色、36 变量高级区、实时预览、按核心色重新生成、JSON 导入导出、删除/复制/编辑；取色器使用 `vue3-colorpicker` + `colord`。规范已同步 `AGENTS.md`、`CLAUDE.md`、`app/utils/theme/README.md` 与 `reference/theme/system.md`。主题聚焦测试 4 files / 9 tests passed，OpenAPI 生成 40 routes updated / 0 failed；全量 typecheck/test 仍被既有 Agent plan-mode/Profile DSL/low-code-form 等非主题基线阻塞。浏览器自定义主题全流程验证尚未执行，需用户同意后进行。 |
| [90 Agent 模式系统（normal/discuss/plan）](docs/tasks/90-agent-mode-system/README.md) | Implemented (待浏览器验收) | Agent 二元 Plan Mode 升级为三模式：normal（可读可写）/ discuss（只读讨论）/ plan（只读计划），`enter_plan_mode`/`exit_plan_mode` 合并为 `switch_mode`（切换=审批，批准后生效）。数据模型硬切不兼容旧数据：`planModeActive` → `agentMode`，customState `ui.planMode.active`/`agent.planMode` → `ui.agentMode`/`agent.mode`（含 phase enter/reentry/steady/exit + fromMode + hasExitedPlan）。只读模式下 write/edit/apply_patch 按 `mutatesWorkspace` 能力标记由 harness 注入审批挂起（复用 tool_user_input_required 链），批准仅豁免单次调用且**在 resolution 消费点真实执行工具**；plan 模式写 `.agent/plan/*.md` 豁免；bash 放行靠提示词约束只读；同模式切换被前置拦截为 no-op。提示词层 `ModeReminder`（didChange 出 phase 全文 / 周期重放出 steady 轻文案，7 种 ModeSlot 可覆盖）+ `ModeAvailabilityReminder`（仅 normal）。前端三态循环按钮 + Shift+Tab + 按模式 placeholder/badge，`/mode`（`/plan` 别名）命令，`AgentSwitchModeBubble` 泛化审批气泡。typecheck 0 error（除并发任务 low-code-form 一处），后端 harness 157 tests + profile/dsl/tools/前端全绿。浏览器验证未执行。2026-07-07 代码审查修复 P1–P6：apply_patch `Move to` 目标不再绕过只读写豁免（复用 `extractPatchTargetPaths`）；注入写审批的识别改为无条件并入 mutatesWorkspace（修好重启后列表 idle 误判 / 发消息误报 session repair / 切 normal 后 pending 丢失，顺带消除冗余 reduce）；`hasExitedPlan` 只在退回 normal 置位（plan↔discuss 互切不再误判 reentry）；execute_sql/execute_world/plot/variable_patch 在 discuss/plan 提示词中按 bash 同级收口只读；前端 AgentMode 枚举去重 + 气泡改 `parseToolArgsObject` + 穷尽 slot 映射等技术债清理。修复后 harness 160 + apply-patch 单测 6 + 各套件全绿，typecheck 仍仅剩并发任务一处。 |
| [91 操作日志 / 文件历史模块](docs/tasks/91-operation-log-file-history/README.md) | Spike Implemented (验收全绿,待集成) | 「先写后补」讨论升级为完整操作日志系统并设计定稿：append-only 事件溯源（`OperationLog` 信封 + `file.*` 判别联合 + 内容寻址快照），一份日志投影四个视图（单文件时间线 / 删除找回 / 用户收件箱 accept+revert / 会话游标 unseen 提醒）。关键决策：不用 git；独立 `.nbook/history.sqlite`（**隐私红线同 traces：含全文快照，严禁进入 task 72 可分享日志包**）；OperationActor 四类含 system(source) 与 external；每 session 标量游标 + 每 (user,path) 接受位点（n 用户 × n 会话就绪）；revert / restore 一等事件且走提醒通道；保留策略分层稀疏（默认 90 天全量 → 每日末版）且**参数配置文件可配**。**spike 已按 [GOAL.md](docs/tasks/91-operation-log-file-history/GOAL.md) 建成并验收**（sibling 仓 `../nb-history`，bun + TS strict + libsql 手写 SQL，零 NeuroBook 依赖）：场景验收 T1–T12 全绿（31 tests 多轮连跑 0 fail，含审查轮修复的时间线出生边界 bug 与读写分离双连接）、typecheck 全绿、性能达标（30KB 写入 ~10ms；unseen 5000 条积压 18–35ms；inbox 代表性 8–11ms；100 版 × 30KB 库 3.00MB 去重生效）、Windows 句柄双测通过（libsql close 需模块内建 GC 协助释放；`bun:sqlite` 对照零等待——驱动选择留给集成决策）。已知偏差如实记录：inbox 零接受全扫 1 万条 best 32–67ms 超 50ms 线（缓解 = 宿主 auto-accept 策略，兼解「未接受段永不 prune」）。NeuroBook 零改动；集成任务（写入面收口、watcher 喂 reconcile、harness 提醒机制 + profile fileChangeAwareness 策略、UI、task 72 排除清单）单列待启。 |
| [92 Project 资源生命周期统一管理](docs/tasks/92-project-resource-lifecycle/README.md) | Implemented (已被 94 演进) | Project 级常驻资源（Plot Project PrismaClient 缓存 / agent execute_sql 单槽 libsql client / workspace tree index chokidar watcher / World Engine GC 兜底）收敛到统一注册表 `server/workspace-files/project-resources.ts`：属主自注册 `{close, closeAll?, busy?}`，访问时报活。三条释放路径全部接通——删除 Project（`deleteProjectWorkspace` 的四行手工枚举改为 `closeProjectResources()`，新资源种类自注册即可，不再依赖"记得补一行"）、切换 Project（空闲清扫：TTL 10 分钟 + 60s 轮询 + timer unref，SSE 订阅在线视为 busy 跳过并刷新报活，解决旧项目 PrismaClient 无限累积与 chokidar 目录句柄常驻导致的 Windows EBUSY）、服务关停（新增 nitro `close` hook 插件）。注册表 4 单测 + 既有删除流程 3 集成测试不改而过 + 回归 sweep 23 files/188 tests 全绿；typecheck 全绿（顺手修复昨日提交带入的既有 `low-code-form/index.ts:798` 索引收窄错误）。**Task 94 GOAL-A 已将该文件演进更名为 `project-session.ts`（显式生命周期）**，隐式 touch/TTL 模型退役；本行保留为历史档案。 |
| [93 Plot 规划层](docs/tasks/93-plot-planning-layer/README.md) | Implemented (2026-07-09 审查修复轮收口；真实项目端到端实测待用户验收) | **已实施（数据层 + 维护/读取工具 + brief 消费段）**：在两棵树之上新增面向未来的规划层（World Engine=事实 / 因果树=编排 / 承载树=呈现 / 规划层=读者债务与作者意图）。StoryPromise + PromiseBeat（读者债务账本：**不设 kind 分类，行为由字段有无驱动**——deadline 才有逾期、cadence 只是提示性节奏参考；beat 挂 Scene，计划/事实/archived-不参与三态随 Scene.status 派生；fulfilled 回退仅在无剩余 payoff beat 时发生，Scene archive/delete 与 Thread delete 均接入同一回退入口；自由文本三层分工 summary/payoffExpectation/beat.note；伏笔按兑现机制四分类 tags 词表 setup_payoff/prophecy/motif/mirror，mirror 双线由 beats 跨 Thread 分布天然表达）；StoryDecision（ADR 式：open 态防 writer 擅自写死，decided 态 decision/motivation/risk 必填，dropped 强制 note，anchor 读时归一化 + serves/dependsOn 写入校验与死引用容错，值域 open/decided/superseded/dropped）；Scene.outcomeType（值域补全 yes/no/no_conflict/passive，null 只表示未填写——已提炼为全系统设计原则 D29）/pacingRole 与 Thread.miceType。brief 消费两段已接入 `get_chapter_writer_brief`：「本章 Promise 任务」（详细形态）+「未决决策警告」（不阻断）；D9 refs 词表清理已执行（system.md 删 foreshadows/pays_off）。工具面随 [Task 97](docs/tasks/97-plot-tool-surface-redesign/README.md) 形态落地。lint / ledger / report / workbench UI 仍暂缓待专门讨论；草稿系统化移出（思维链+ADR 够用）。验证：`bun test server/plot` 70 pass、`server/agent/tools` 147 pass、profiles 170 tests、typecheck plot 相关 0 error；真实项目端到端实测未执行（单测分段覆盖，验收降级已在任务 README 声明）。 |
| [94 Project 生命周期模型重构](docs/tasks/94-project-lifecycle-model/README.md) | Implemented & Accepted (浏览器验收待执行，未 commit) | Task 92 隐式模型重构为 VS Code 式**显式生命周期**。**GOAL-A 核心机制已落地（2026-07-08，ultracode workflow 实现 + 对抗式审查 27 agent）**：一等 ProjectSession（`server/workspace-files/project-session.ts`，由 project-resources.ts 演进更名）——显式 open（幂等 + 目录 404 校验 + initProjectDatabase 收敛到 open 跑一次）/ presence（**专用 SSE `GET /api/projects/presence`** 连接计数 + harness agent 运行中 invocation 探针）/ 归零→5min 宽限→级联 close / 泄漏表 + 30s 周期维护；`POST /api/projects/open`；前端 `useProjectSession` composable（重连先重跑 open、读超时看门狗）。审查轮修复 close-vs-reopen 竞态类（per-key 串行锁 + release 世代绑定 + sweep 重入闸）并把 D12 收敛为**仅 agent 在场阻删**。**GOAL-B 基线与数据面补漏硬化已闭环**：Plot / World Engine / execute_sql / workspace-files / config project 等数据面入口未 open 时稳定拒绝（HTTP 409 `PROJECT_NOT_OPEN`），plot/world/sql 三处 per-call `initProjectDatabase` 已移除并收敛到 open；2026-07-09 补齐 RAG 数据面与 Project Profile Home 写入/reset/读时初始化守卫，二次补齐 Agent session snapshot/system prompt 与 profile compile/preview worker 的 Project Profile Home 生命周期守卫，保留 Project Manifest、项目列表/创建/删除/zip 导入、纯 Global Profile Home 等控制面/全局面豁免；worker compile 普通失败仍返回 DTO issue，Project lifecycle violation 重新抛 typed error 并由 HTTP 层映射 409。补漏细节、边界解释和验证记录见任务 README / NOTES-B。**2026-07-09 GOAL-B 验收通过（主会话）**：代码走查（守卫 helper / 四资源咽喉 / 12 条 workspace-files 路由 / RAG·config·profile 路由 wrapper / worker lifecycleError 通道 / initProjectDatabase 残留审计）全部属实；typecheck + 守卫聚焦 6 文件 30 例 + 大组合 57 文件 510 例全绿；NOTES-B 遗留的 harness black-box「既有失败」经复核为两处与 Task 94 无关的既有测试时序缺陷（首用例 5s 暖机预算不足 + followup 等待锚点恒真竞态），验收轮已修复，harness 套件 239 例全绿。 |
| [95 nb-history 集成](docs/tasks/95-nb-history-integration/README.md) | Implemented & Accepted (2026-07-09；2026-07-18 runtime artifact 路径收口) | nb-history 以 **vendor 源码**进仓（`server/vendor/nb-history/` + `sync:nb-history` 脚本 + VENDOR.json 锁 commit），WorkspaceHistory 作为 ProjectSession 资源接入：每项目 `.nbook/history.sqlite`，open 预热 + 懒开兜底 + close 级联；写面全收口——编辑器/平台路由走 `tracked-workspace-files`，Agent 工具走 `agent-file-recorder`，watcher + D15 扫描负责对账。2026-07-18 补齐 Project Runtime Artifact 分类：World Engine 生成物迁入 Project Workspace `.nbook`，watcher/history/文件索引/下载共用排除策略；每个 WorkspaceHistory 实例首次打开时在 D15、inbox 和 unseen 前原子清理旧错误路径，保留正常历史与 cursor，未使用 History 的 ProjectSession 不强制开库。Project 下载包含 History SQLite 的独立一致 snapshot，完整备份与 Task 72 可分享日志包的隐私边界保持分离。Task 102 继续持有 notice、安全 diff、revision 与预算语义；D15b 外部变更列表、时间线/删除找回仍留 TODO。 |
| [96 Session 标题所有权与 summarizer 策略](docs/tasks/96-session-title-summarizer/README.md) | Implemented (通用运行策略收口，待浏览器验收) | Session 支持手动改名（侧边栏/列表按钮 + `/rename`），改名后标题所有权归用户；`/summarize` 交还所有权并 force 重跑。2026-07-12 Summarizer、Compaction 与单文件 diff 上限统一为 Harness 六层运行策略。审查轮补齐通用 defaults 脏检查、字段级严格校验和真实继承来源展示；Profile DSL 与 Config 共用 shared 严格 schema。手动 summarize/compact 强制执行最终策略，system summarizer 禁止递归摘要且默认关闭 Compaction。 |
| [97 Plot 工具面重排](docs/tasks/97-plot-tool-surface-redesign/README.md) | Implemented (2026-07-09 审查修复轮收口；F1 真实会话冒烟待用户验收) | plot 工具面从 14 个（三套前缀 + 四个死代码工具）收敛为 **15 个（读 8 + 写 7）**：读统一 `get_story_*` 前缀（`get_plot_tree`→`get_story_tree`、`get_chapter_plot`→`get_story_chapter`，新增 `get_story_promise` / `get_story_decision`，无 id=列表模式）；写合并为 `save_*` + **必填 action 枚举**（create/update + 生命周期动作 archive/abandon/fulfill/decide/drop 进 action=Agent 软删出口，物理删除不开放给 Agent）；Act/Chapter 死代码以 `save_story_act` / `save_story_chapter` 新形态接活（修 F1：leader 无工具补 ChapterBrief 导致 `needs_chapter_brief` 死锁）；`plotReadBindings` / `plotWriteBindings` 分层 bundle，writer 只持读组。**D8 已落地**：7 个 save_* 标 `mutatesWorkspace`（`tool()` helper 必填 `mutates` 强制每个工具显式声明读写意图），discuss/plan 只读模式下 plot 写面由提示词软约束升级为 harness 硬门控（复用 Task 90 机制；bash/execute_sql/execute_world 仍按 Task 90 TODO 走软约束）。验证：agent/tools 147 pass、profiles 170 tests、profile:metadata 重编译通过；F1 真实会话冒烟未执行（单测模拟覆盖，降级已在任务 README 声明）。 |
| [98 leader.assets 用户资产助手重塑](docs/tasks/98-leader-assets-profile-reshape/README.md) | Implemented | 用户资产助手 profile 重塑为「介绍 + 创建/修改/管理用户资产 + UI 指路」：新 system prompt 补齐用户资产地图（templates 两类模板 / `agents/{profileKey}/` 全局 home / config.json settings 存储）、「哪里做什么」三入口分工（设置表单填值 / agent 改文件 / TSX Profile 工作台编译）与「设置表单与 Home 资源」路由级知识；修复四类结构问题——AppendingSet 裸 Message 每轮无去重堆积（纯删除+负向断言）、SkillCatalog fork 漂移（DSL `mode="userAssets"` 参数根治）、agent-v2 死文本、缺 `ModeAvailabilityReminder`；新增契约级 **skill 白名单** `defineAgentProfile.skills.include`（prepare 包装层统一过滤 ctx.skills，catalog 可见性≠权限隔离），leader.assets 白名单收窄到 4 个资产编辑 skill；自身加 settingsForm（customTopSystemPrompt）。知识链同步：profile-system-guide SKILL.md + reference 补 Settings Form / Profile Home 两节（含 writer 文风预设 canonical 步骤与 frontmatter 契约）、修死链与旧术语（InputSchema→InitialSchema、mainRunToolKeys→toolKeys、幻影 DSL 节点名）；tsx-profile-editing 补 settingsForm/home/skills 代码骨架与 tool bundle 用法；leader.default Notes 补设置面板指引句。profile-dsl 28 + leader-assets 15 测试全绿、typecheck 全绿、涉及 profile 已重编译。浏览器验收未执行。 |
| [100 RP 模式 v2 重构](docs/tasks/100-rp-mode-v2/README.md) | P1-P9 + Bootstrap hardening Implemented（浏览器验收由用户决定） | 18 组运行逻辑合同已全部落入 P1-P9。P8 的完整/差量世界切片树之上，P9 新增三档一致性审计、提交/长跳/恢复门禁、安全索引修复、切片损坏祖先诊断与问题报告、状态页手工检查，以及 test-only 并发隔离固定随机。首次服务器实测后的管线加固已覆盖 Provider object schema、RP session 重挂载恢复、左侧玩家确认入口、六阶段 Bootstrap 真实验收，以及审批前工具领域预授权与 get_agent 真实状态投影；config 改由服务端 `initialize_config` 生成受信配置。最新 RP 相关联合回归 8 文件、43/43 通过；首个常规 turn 已恢复 snapshot，未擅自推进后续剧情。 |
| [99 Plot 规划层前端](docs/tasks/99-plot-planning-ui/README.md) | Implemented (2026-07-09/10 审查修复收口；浏览器验收待用户，建议与 93 真实项目实测合并) | Task 93 规划层的人机 UI 直读先行批次（路线 B，纯前端零后端改动）：剧本工作台从 6 个装饰 tab（activeTab 只改高亮、主体不切换）重组为 **3 个真 tab**（线程规划=原三件套 / 承诺账本 / 决策记录），砍掉 overview/章节设计/草稿池/timeline/tree 装饰位；顶栏 mock 残留（「新小说」面包屑 / 写死「主线：」/ 虚构「已保存 刚刚」）一并清除。承诺账本：列表（状态点/派生阶段/importance/四类节拍计数/期限章 + open 默认筛选）+ ADR 式详情（节拍按章序时间线、期限章已删 danger、场景跳转回线程规划）+ 编辑/节拍/兑现·放弃·重开/物理删全操作面（节拍对话框回填既有 kind/note 防静默清空，payoff 默认 autoFulfill 并通知自动兑现）。决策记录：open 置顶分组列表 + ADR 分段详情（risk warning 刹车点色块、否决记录待补理由标注、serves/dependsOn 死引用删除线 + 编辑器行级 danger 标注）、拍板对话框（候选单选+全新方案、decision/motivation/risk 三必填、未选项转否决骨架提示）、作废强制失效原因。Scene/Thread 编辑器（完整编辑器+工作台 Inspector）补 outcomeType/pacingRole/miceType 三下拉（「未填写」=null，D29 人侧对齐；AI 批注 draft 同步）；底部详情面板与 Inspector 渲染「服务承诺」promiseBeats 只读芯片（点击跳账本聚焦，数据来自此前被丢弃的 scene 详情字段）；侧栏「承诺 N / 未决 N」计数入口（消费此前被丢弃的 PlotTreeDto 计数，0 弱化不隐藏）；账本 UI 写操作经 mutated→planningMutated 事件链刷新树计数并强刷受影响 Scene 详情缓存（与 Agent 侧 plotRefreshVersion 对称）。新建 `app/components/novel-ide/plot/planning/` 11 文件（含 typed API client `plot-planning-api.ts`——Task 87「typed query 防再犯」新端点部分收口；标签/色调 META 单一来源 `plot-planning.types.ts`）。顺手收口：workbench refs 目标候选三条硬编码 demo 数据改接 workspace tree 真实内容节点 + 引用弹层按 query 即时搜索（突破 40 条截断）；Inspector refs 词表删 foreshadows/pays_off（93 D9 前端残留）；Thread/Scene 编辑器保存错误改独立 editorError（原写 treeError 被模态遮挡不可见）。实施链 4 分片（2/3 fable 并行）+ 三维审查 10 findings（1 major：Promise 编辑草稿被 SSE 后台刷新静默重置——快照修复）全部处置；typecheck 全绿、planning 组件全部 <800 行、调色板/demo 残留零命中。明确不做（93 拍板对齐）：overdue/停滞计算（等 frontier+lint 批次）、报告页、张力折线、supersede 专门 UI。浏览器验收未执行。 |
| [101 Markdown Studio 方言重构与输入性能修复](docs/tasks/101-markdown-studio-dialect-and-performance/README.md) | Implemented (含反馈轮 2/3 + 审查修复轮；浏览器验收由用户执行) | Markdown Studio 输入性能修复 + 方言扩展一轮：**性能**——TipTap 与 Monaco 统一走 `useEditorChangeDebounce` 共享防抖协议（每键只 schedule，300ms 或 blur/Ctrl+S/submit 结算；外部覆盖 cancel；unmount cancel 防串位），消除每键「1 次全文序列化 + 4 次全文扫描 + 2~3 次隐藏 Monaco 全文推送」与源码模式每键 O(全文) `getValue` + store 全文对比链；隐藏编辑器惰性同步；评论列表与高亮 decoration 缓存进 ProseMirror 插件状态。**同步协议**——store `registerActiveEditorFlush` 钩子：`persistActiveWorkspaceBuffer` / `saveCurrentFile` / `syncWorkspaceFromDisk` 开头先结算编辑器防抖输入再取内容快照，根治「外部工具改文件 + 防抖窗口 → forceDisk 覆盖本地输入」的文本回退；`syncWorkspaceFromDisk` 按 `mtimeMs === lastSyncedMtimeMs` 抑制自己保存的 watcher 回声（不假警告、不白重载）；dirty 预警通知文案含 Ctrl+S 打开冲突对比界面指引（diff 界面仍只在保存乐观锁 409 时弹出，有意设计）；**StructuredTextEditor（v-if 懒挂载的 15+ 处表单编辑器）模式切换前 sync watch 结算防抖，堵住切 rich/source 丢最后 300ms 输入**（审查修复轮 F1）。**方言**——评论合并为 `<comment>` 单标签双形态（段内闭合=行内 mark；开闭独立成行=跨段落 commentBlock）；`<ruby text="hàn zì">汉字</ruby>` 注音与 `<bilingual text="译文">原文</bilingual>` 对照块；未知 HTML 兜底只保源码不渲染（块级 HtmlBlock / 行内 RawInlineHtml chip，失配残片退化单标签 chip 含 `Vec<String>` 类伪标签），**渲染能力只属显式 `<html>` 块**（HtmlEmbed：默认源码卡片 → 点击渲染 `sandbox="allow-scripts"` iframe，postMessage bridge 预留 `window.nbook.request` 数据接口注入点 `resolveDataApi`）；**宽容形态读时规范化**：`正文<comment>␊…␊</comment>` 开标签黏正文形态由 `normalizeMarkdownDialectBlocks`（shared，三重守卫：逻辑行首/缩进代码/闭标签存在性 + fence 感知 + EOS 不算行尾 + 开标签正则与各方言 pattern 逐标签同构）在编辑器读入时拆段为标准块级，流式 chunk 与 Monaco 不规范化；HtmlBlockBridge 对配对闭合恰好覆盖到结尾（`findMatchingCloseEnd` 真配对判据）的 DOM 可解析方言标签交回 DOM 自愈。四个陷阱已固化为注释与契约：marked tokenizer 后注册先执行（兜底 1390/1400，新方言必须更低）、段落中断 `src.slice(1)` 伪装（块级 start 只认 `\n` 前缀）、**ProseMirror 空文档默认填充节点取 priority 最高块节点**（paragraph 提到 1500 压过兜底，否则空文件出 HTML 卡片；新块级扩展 priority 不得超过 1500）、读时规范化判据边界（normalize 函数注释）。方言核心扩展组单一来源 `markdown-dialect-extensions.ts`（真实编辑器与三个测试文件共用，防测试 schema 静默分叉）；inline AI 引用高亮 ~300 行拆出独立模块 `InlineAiReferenceHighlight.ts`。验证：shared+app 全量 301/301、本轮文件 typecheck 干净；浏览器验收（打字流畅度、空文件、卡片交互、外部改文件冲突链、混合形态、StructuredTextEditor 模式切换不丢字）由用户执行。 |
| [102 Agent 变更收件箱与提示词顺序](docs/tasks/102-agent-change-inbox-and-prompt-order/README.md) | Implemented & Accepted (2026-07-10；2026-07-18 notice 指导去重) | Agent Composer 输入卡上方是默认收起的独立变更卡片，完整 History Dialog 保留深度审查；敏感正文在服务端读取前阻断。Profile DSL 只产出 `<FileChangeNotice mode={...} />`，Harness 物化 notice 时从最终 runtime 读取单文件 diff 上限（默认 512）；Profile 自定义 settings 与运行策略完全分层。notice 保持英文 Git 风格、安全路径阻断、预算与游标语义；逐文件条目只陈述事实，read/sensitive/deleted clause 由 footer 每类汇总一次，敏感与删除路径不再承诺存在于 Inbox。Reminder、Workspace 和 Variable 的既定边界不变。 |
| [18 Agent Runtime Pipeline Hooks](docs/tasks/18-agent-runtime-pipeline-hooks/README.md) | Updated | Agent 基础工具协议已补齐并重新修复当前工作区回写回归：`request_user_input` 已从 Low-Code Form 拆出，模型可见结果保持格式化文本和结构化 `details.answers`，协议只保留问题、单选 options、开放 note，不再支持默认值/推荐/多选；`read` 支持按需/自动行号和定位 details，offset/limit 收紧为正整数；`edit` 批量 exact replacement 先预检再写入并返回可读失败诊断；task custom state 维持即时 + savePoint 状态写入，覆盖下一轮读取和旧分支不污染语义。 |
| [51 llmlint Text Lint Skill](docs/tasks/51-anti-ai-slop-skill/README.md) | Updated | `llmlint` 已升级为 flat Rule Registry 和稿件级 CLI：默认 `builtin/default`，340 rules / 311 active，支持 namespace/rule override、`review`（agent/human/none）、`fixability`（auto/candidate/manual）、`check` 多文件/glob/Markdown 遮罩/JSON/stylish 紧凑输出，以及 `fix` 的机械 auto 修复。当前真相源已随 Task 84 切到 sibling `../llmlint/skill`；NeuroBook `assets/.../llmlint` 只是 vendored runtime snapshot，不再包含嵌套仓、node_modules 或 evals。 |
| [58 Agent Profile Settings Low-Code](docs/tasks/58-agent-profile-settings-low-code/README.md) | Updated | `leader.default` 接入低代码 settings 与 profile home persona 资源，支持协作模式、熟练度、提问策略、Leader 人设和最高优先级置顶提示词；`writer` 同步接入最高优先级置顶提示词并清理 `enableKittenAdultStyle` 隐藏字段；低代码合并层忽略已下线字段的存档残留。 |
| Writer Profile 重构 | Done | 去除小猫之神角色定义，理清 profile / reference / skill 职责边界，从 650 行压缩到 535 行。 |

## Known Follow-ups

- **[TODO·后续任务] 整书导入（作品导入）**：把一部完整外部小说逆向导入 NeuroBook = 承载树反建（Task 87 bootstrap 已具备）+ LLM 通读正文建立 Plot 结构（章 summary → Scene/Thread 提议，正文无可靠 Scene 标记，只能语义切分）+ World Engine 渐进初始化（人类决定在哪里展开初始切片）。依赖 Task 91 操作日志/文件历史模块（spike 已验收）与「先写后补」模式先落地（同步与提议-确认机制）；token 与精细度权衡（章 summary 可用免费/廉价模型生成）。
- **[Task 87 已订正·非回归]** `NovelChapterPanel` 的「本章剧情」widget 曾被判为路由契约回归（仍向 `GET /api/projects/plot/chapter` 传旧 `chapterPath` → 400），**核实后推翻**：该组件是全仓无引用的 legacy novel-model 死代码（左栏实际用 `WorkspaceCharacterPanel` + `NovelPlotPanel`），400 运行时不触发，不为死代码建反向桥（用户拍板）。收尾单列：清理 legacy novel-chapter view 死簇 + 给 plot 前端 `$fetch` 配 typed query 防契约漂移漏网，详见 [Task 87 walkthrough](docs/tasks/87-plot-two-trees-and-writer-modes/README.md) TODO。
- World Engine 写作模式主路径已阶段收尾；后续重点是体验打磨、`memory.jsonl` / `state.md` 是否显式 commit、以及真实作者长期使用反馈。
- Plot / World Engine 桥接的 Agent 易用性第一阶段已完成；当前普通写作主链已在 Round 141 改为 leader.default 直接负责 Plot / Scene，leader 持有全套 Plot 读写和 brief 工具。**Task 87 后 writer 在 autonomous 模式下已获得 Plot 只读**（`get_chapter_writer_brief` / `get_story_chapter` / `get_story_scene_context` 等，只读不写；工具名已按 Task 97 Slice 1 重排：读统一 `get_story_*` 前缀，写合并为 `save_story_*` + 显式 action），world.engine 工具面未扩大。Round 140 completion audit 覆盖了原 Director + Brief Compiler 完成态；Round 141 是之后的 profile 主链调整。真实 Agent 模型行为 smoke 尚未执行，因此不声明模型在真实会话中的规划质量已验证。后续章节级 POV、语气、节奏、信息控制、开头/收尾和禁写事项已由 Task 87 的 `ChapterBrief`（`StoryChapter` 字段组，吸收原 Task 80 ChapterOverride）落地，brief 编译器双模式只做了 autonomous，curated + 反问逃脱口延后。Task 78 Round 63-135 是实现前设计、风险和 runbook 记录；当前状态以 Round 136 之后的完成/调整记录为准。
- World Engine 后续单独讨论更大的 API 形态：是否增加 subject 视角便利查询、批量读取命名、列表过滤 DSL。
- Agent 用户输入 pending 已按 Task 63 收口到 durable user resolution 语义；Task 90 后审批面扩展到三模式 `switch_mode` 与只读模式写工具注入审批；后续前端验收仍建议覆盖多窗口同步、approval resume、模式切换（normal/discuss/plan）、compact、edit/retry/rollback/fallback 和流式工具卡片。
- Agent session 列表已新增分页与服务端搜索；后续如果 Workspace Root 中 session 数增长到数千级，再设计持久化 session 摘要索引。
- **[TODO] Session 摘要空闲触发**：把 summarizer 的后续触发从"每 16 个用户轮"改为"用户离开一段时间后（idle debounce）后台跑一次"；项目目前没有 idle/定时基础设施，需要先补最小的进程内 debounce 机制（Task 96 拍板暂缓）。
- **[TODO] dev server 间歇性静默退出（exit code 5）**：2026-07-27 一天内 `bun run dev`（nuxt dev）三次在正常运行/HMR 重建后无任何错误输出直接退出，退出码 5 疑似 Windows 0xC0000005（原生段错误）低字节截断——嫌疑面：bun 或某原生模块（libsql/watcher）在重建期崩溃。暂用「自动重启守护循环」缓解；待复现规律后再定位（可尝试固定 bun 版本、`nuxt dev --no-fork`、排查 libsql 句柄）。
- **[TODO·Task 100] RP v2 待验收项**：① 浏览器交互验证（三态切换/侧栏四分页/开团确认按钮及自动续跑/Bootstrap 六阶段进度与失败提示/激活后正文发布/候选操作/更新窗口/切片树初始化与恢复/图谱/会话列表）；`workspace/wo-de-xiao-yuan-sheng-huo` 已激活并进入首个常规 turn，当前同一 turn 停在 `world_snapshot`，snapshot 与 runtime plan 已恢复、旧 failure 已 resolved，可由主持从 `condition_check` 继续；② rp_character_update 的 god-view 操作目前靠提示词约束 actor 不越权，后续可加工具级 scope 硬门控；③ RP 模式状态未持久化（刷新回 Agent/IDE）。
- **数据面回归测试已建**（2026-07-27，「AI 代演」两层方案第一层）：`server/rp/rp-multi-tick-scenario.test.ts`（RP 三 Tick 剧本：注册表/权威编号/掷骰/pending 兑现/摘要滚动/视图隔离）+ `server/plot/writing-main-chain.test.ts`（写作主链：lorebook→WE→Plot→ChapterBrief→manuscript 反指），剧本用内置「勇者召唤」。测试抓出并修复 bootstrap schema 模板 passthrough 不被 WE 校验器支持的 bug。第二层「桥接式 AI 当模型」真端到端待需要时搭建。
- **[TODO·Task 100] 首次 LLM 实测暴露的管线问题**：①-④ 已于 2026-07-27 系统性修复（角色 id 注册表 registry.json + 工具层重名拒绝/名称解析/未登记即错；`rp_tick_info` 权威 Tick 总账工具 + leader 宣告纪律；execute_world 描述补沙盒限制 + rp.world 契约「配置缺失只报错」；四份 rp-v2 文档替换 legacy import + bootstrap skill 可抄 schema/calendar 模板与目录骨架），详见任务 README「七」。剩余：⑤ 掷骰暂停流程未经实测（本局无判定点，需下次实测做一个高风险动作验证）；xin-xiao-shuo 项目里的历史重复档案（brauer/bulaoer 等）未合并，可让彩绘合并或开新项目实测。
- **[Task 100] 开团引导技术门禁已落地**（2026-07-28）：旧版仅 Prompt 的“确认后开团”已升级为 Project Workspace `.nbook/rp/intake/` 持久草案 + 版本快照 + `rp_intake` 用户审批 + Bootstrap/正式文件/子 Agent/World/角色写硬门禁；通用文件工具不能伪造 intake 状态，rp.leader 已移除 bash 绕过面。测试 79 项通过；完整记录见任务 README「十」。后续继续 P2 回合事务与文件化更新账本。
- **[Task 100] P2 回合事务已落地**（2026-07-28）：`rp_turn` 为每个 IC 回合分配唯一 ID，维护 waiting/committing/committed/failed/cancelled 与恢复扫描；完整结算/阶段/错误写 `.nbook/rp/runtime/`。World Engine 新增 operationId，同世界写入在一个 SQLite 事务记录结果，响应丢失重试不会重复结算；失败事务可同 id 重试。active RP 子 Agent 必须绑定当前回合，未 committed prose 不进入正文面板。
- **[Task 100] P3 正式事件与主动主持已落地**（2026-07-28）：`rp_event` 在 `.nbook/rp/runtime/events/` 维护四卡、保留/失效/重新校验、服务端随机、普通 active 三名额、硬性第四焦点和完整生命周期；screenwriter/leader/world 权限由工具硬校验。`rp_turn commit` 只在 committed 后按 turnId 幂等更新平淡回合计数，连续五次触发新候选提示。下一阶段 P4 时间、资源、关系与认知。
- **[Task 100] P4 时间、资源、关系与认知已落地**（2026-07-28）：RP World Engine Instant 是唯一时间源，长跳对 active/suspended/到期硬性事件执行阻断并要求绑定具体区间的玩家审批凭证；`mechanics-store` 支持账本、时间派生、动态资源、周期阶段和服务端 ppm 风险抽取；`relation-store` 维护八维有向关系并拒绝骰子直接建关系或系统替化身决定核心情感；`cognition-store` 分离世界事实、角色 belief 与玩家 OOC knowledge。四领域由 `rp_turn commit.rules` 统一预检、按 turnId 幂等写入，失败不留下半套状态。P1-P4 合并回归 17 文件、78/78 通过；下一阶段 P5 层级地图与 NPC 生命周期。
- **[Task 100] P5 层级地图与 NPC 生命周期已落地**（2026-07-28）：`rp_map` 维护与 World Engine location subject 同 id 的六级地点目录、传闻降级、秘密路线、首次抵达固化、关闭/毁坏保留、地点冲突和整批小说导入确认；`rp_npc` 维护 named/resident/major/major_inactive、敌对角色擢升、主要角色软上限 8、历史记忆补建与精确资源待办。`confirm_import`、`approve_conflict`、`promote` 均要求真实玩家审批；P1-P5 合并回归 20 文件、93/93 通过。下一阶段 P6 回合编排与关注度。
- **[Task 100] P6 回合编排与关注度已落地**（2026-07-28）：`rp_pipeline` 把常规 IC 回合升级为十阶段代码状态机；world 快照 id 贯穿 screenwriter/cast/extras/冲突解决/终裁，主要 actor 缺失或失败不能代演，角色意图不能被 screenwriter 计划覆盖，未登记 prose 不能提交。`rp_focus` 维护四级关注度与 light/standard/deep，档位只改变远端预算，硬事件和确定性模块始终运行；长跳只保存一次批量摘要。P1-P6 合并回归 23 文件、105/105 通过。下一阶段 P7 侧边栏与更新窗口。
- **[Task 100] P7 RP 侧边栏与世界更新窗口已落地**（2026-07-28）：新增不泄露事件幕后安排、NPC persona 或 actor 内心的玩家安全 runtime overview；侧栏拆为状态、事件、地图、角色四分页，展示十阶段进度/失败、关注度、资源、候选事件操作、稳定层级地图、NPC 名册与擢升建议。强度和候选操作直接写服务端状态，不调用 Agent；非模态更新窗口分页读取摘要并按点击加载 committed settlement。P1-P7 合并回归 24 文件、108/108 通过。下一阶段 P8 世界切片树。
- **[Task 100] P8 世界切片树已落地**（2026-07-28）：`.nbook/rp/branches/` 使用根/安全/分支点/每十层完整材料和中间文件差量，统一覆盖 `world-rp.sqlite`、RP runtime、ticks、dice 与角色记忆；每节点最多四条直接分支，第五条在 world commit 前要求玩家选择未锁定归档对象。UI 支持完整性与 active→目标影响预览、安全切片和显式恢复；角色 belief 随分支切换、玩家 OOC 事实保留，恢复后创建新主持会话隔离旧 cast/actor 上下文。P1-P8 合并回归 25 文件、114/114 通过。下一阶段 P9 一致性与长期验收。
- **[Task 100] P9 一致性与长期验收已落地**（2026-07-29）：新增 light/standard/deep 跨 store 审计与 `.nbook/rp/runtime/consistency/latest.json`；提交、长跳、恢复和项目打开接入对应档位。只自动重建 timeline children 索引；剧情、资源、关系、认知和正文冲突要求玩家确认。切片损坏写 `.nbook/rp/branches/problems/` 并只建议最近可验证祖先，不自动回退。骰子、候选抽取和概率风险支持 test-only AsyncLocalStorage 固定种子。100 Tick 硬验收覆盖 20 角色、30 地点、10 周期资源、三档强度、四次长跳与三条时间线，最终 deep healthy；P1-P9 全量回归 28 文件、121/121 通过，浏览器交互验收仍由用户决定。
- **[Task 100] RP Provider schema 实测故障已修复**（2026-07-29）：九个多操作工具不再把根级 `Type.Union` 直接发送给模型服务；模型可见 parameters 统一投影为 object，原 union 保留为执行期 `validationSchema`。工具注册表新增根类型 fail-fast，全内置工具 schema 契约与 RP Profile 联合回归 32/32 通过；用户需重启服务后重新发送 RP 消息完成真实 Provider 验收。
- **[Task 100] RP 重进页面后 session 暂时消失已修复**（2026-07-29）：`RpModeSurface` 的 `v-if` 重建会让内部对话面以 active=true 初始挂载，原非 immediate watcher 因没有状态变化而不执行恢复；新建 session 附带 refresh 才令旧列表重新出现。通用 `AgentChatSurface` 现于挂载时补走幂等 `ensureSessionReady()`，保留 watcher 处理后续切换；会话生命周期专项 4 文件、40/40 通过，浏览器交互仍由用户验收。
- **[Task 100] 开团确认已迁移到 RP 左侧状态页**（2026-07-29）：实测 session 证明 `rp.leader` 在 `review` 后漏掉旧 `confirm` 并直接触发 Bootstrap 门禁，因而没有产生任何 pending UI；通用 formSpec 的 `layout=dialog` 也从未被前端渲染。当前 `reviewing + version` 持久状态直接驱动左侧“确认并开团”按钮，玩家 API 按可见版本确认，Agent schema 删除 confirm；主持回合结束自动刷新侧栏，确认后在主持空闲时自动续跑，页面切换/重启不再隐藏入口。联合回归 5 文件、27/27 通过，内置 Profile 已重编译并同步真实 user runtime，浏览器交互由用户验收。
- **[Task 100] Bootstrap 阶段状态机与正文发布门禁已落地**（2026-07-29）：intake 升级为 schema v2，服务端按 config/world/map/characters/opening_event/narrative 六阶段校验真实数据；Agent 只能 checkpoint 当前阶段，激活前重新全量验收。开场正文改写 staging，active 后才原子发布到 Tick 000000，正文 API 在非 active 时固定为空；World status 改为真实加载 Schema/Calendar，侧栏显示 `n/6`。后续 config 加固已把失败语义改为停留原阶段直接修正，不再回到 confirmed。
- **[Task 100] Bootstrap config 标准初始化已落地**（2026-07-29）：新增 `rp_intake initialize_config`，由服务端生成并立即加载真实 Zod Schema 与 Gregorian/Simple Calendar；Schema loader 拒绝普通 `{shape:{}}` 伪对象，config checkpoint 校验核心属性，Bootstrap 通用文件工具禁止手写 Schema/Calendar。失败保留 `bootstrapping/failed`、原 stage 与已完成阶段，状态页直接展示修正原因。当前测试 Project Workspace 已通过生产 config checkpoint 并推进到 world；完整 `server/rp` 回归 22 文件、95/95 通过，系统 Profile 与两项 RP Skill 已同步真实 runtime；全仓 tsc 只剩既有 Vue/Profile fixture 错误。
- **[Task 100] Bootstrap map 提案权限已修复**（2026-07-29）：设计职责保持 screenwriter 提案、world review/固化、leader 编排；工具层新增唯一特例，只允许 `rp.screenwriter` 在服务端确认的 `bootstrapping/map` 阶段逐地点使用 `origin=bootstrap`，active、其他阶段与其他 Agent 均拒绝。`propose` 明确为单地点根字段协议，`candidates[]` 仅用于 `stage_import`。定向回归 21/21、完整 RP 回归 95/95 通过，3 个 Profile 与两项 Skill 已同步真实 runtime；当前项目保留 map failed 供主持直接重试。
- **[Task 100] Bootstrap characters 权限与空 roster 语义已修复**（2026-07-29）：子 Agent 门禁允许 rp.world 在 characters 阶段登记企划中已确认具名的 NPC，leader 仍无 register_named 权限；characters checkpoint 不再无条件要求 roster 非空，未具名群演不得用占位描述伪造姓名，独角开场合法。定向联合回归 24/24、完整 RP 回归 97/97 通过，2 个 Profile 与两项 Skill 已同步真实 runtime；当前项目保留 characters failed 和空 roster，供主持直接重试进入 opening_event。
- **[Task 100] 首回合 world_snapshot 隐藏等待已系统修复**（2026-07-30）：工具协议新增审批前只读预授权，五个带角色权限和用户确认的 RP 工具全部接入；越权操作现在直接返回错误，不再生成隐藏 pending。`get_agent` 改为投影真实 waiting/running/idle。rp.world 合同固定 `rp_focus get → execute_world state_read → rp_focus rebalance → rp_focus plan_runtime → rp_pipeline capture_snapshot`，禁止 world 使用 leader 专属 `set_focus`，并明确 rebalance 根节点没有 reason。相关 RP 回归 43/43 通过；真实 session 49 的旧 waiting 已通过 Harness 正式 abort 清理，同一 turn 已写入 `snapshot-669d9f1268f7e130a0a4` 并解除旧 failure，仍停在 world_snapshot 等待主持继续。
- **[Task 100] RP 运行计划、地图恢复与图谱交互已系统修复**（2026-07-30）：九个多操作 RP 工具的模型可见 object Schema 现保留精确 op 分支，Harness 按 op 做同分支执行校验；`plan_runtime` 只接受 bigint 十进制 Instant，`execute_world` 固定返回原始 `worldInstant`，消除 Provider 接受而运行时拒绝的同类卡住路径。`rp_map replace_proposal` 以 superseded 审计链修订未落地冲突；角色页合并化身、主要/常驻/具名/非活跃与其他角色，移除 aliases/完整 World attrs 泄密面；地图和正式关系图可放大、点击节点查看详情。联合回归 26 文件、139/139 通过。`workspace/xin-xiao-shuo` 地图三级目录已恢复，`turn-000002-4a8cf3c2` 已生成 Instant `42600→42660` 计划和 `snapshot-71b36f3fbe55ffc29956`，仍停在 world_snapshot，未推进剧情。
- Project list 已有 5s 短缓存、启动预热与 `Server-Timing` 分段观测；后续如果 Project 数增长到上百且短缓存仍不足，再考虑持久化 Project stats 索引。
- Agent command 轻控制路径已移出完整 snapshot，relation/profile 热路径一致性已补齐；后续若完整 snapshot 首屏仍慢，优先单独治理 `snapshotSystemPrompt` 冷路径成本。
- Profile 编译系统底层重构（Task 79）已完成 Phase 0–3 收口：读路径硬切、内容寻址 artifact、原子 manifest、`compile_failed` 记账、严格无 stale、主线程 in-process Publisher、Registry 翻转、自动编译 Coordinator、boot sweep、profile assets sync Publisher 化、artifact grace GC、独立 ArtifactStore/FreshnessChecker/Registry/SourceWatcher 组件、全量 worker 池 fan-out/fan-in、发布前 source file set + entry source hash/bytes 重校验、磁盘发布成功后 source 不回滚、Registry 半成功 committed error、profile/workspace 测试 root 隔离与嵌套恢复均已落地；`.compiled` 新格式稳定契约见 `reference/agent/profile-compiled-artifacts.md`。
