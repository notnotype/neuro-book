---
标签: [state:local]
别名: ["标签页", "Tab 栏"]
---

# Tabs

页面或区块级的线性标签栏：把互斥的几个去处排成一行，当前所在的那一项用底部指示线标出。它只画标签栏本身，**不渲染内容面板**——换了选中项要显示什么，由消费方按 `modelValue` 自己决定。

和同类零件的分工：紧凑地切换「模式」（选项一般等分、带滑动选中块）用 `SegmentedControl`；需要一组可以多选、也可以全不选的开关按钮用 `ToggleGroup`；`Tabs` 用在那些是「去处」而不是「模式」的地方。

## 布局

只有一块：一行标签，外加底边线。标签栏**不换行**，标签变多或容器变窄时在标签栏内部横向滚动，不会撑破父容器，也不会出现第二行。选中指示线贴在底边线**上方**，不覆盖边线——标签栏是横向滚动容器，指示线不能越出盒子。

`size` 为 `sm` 时高度与字号都收一档，`md`（默认）为常规控件高度。每个标签内从左到右是可选图标、文字、可选计数徽标；计数徽标用等宽字体，`0` 也会照常显示。三项都排在同一行，不因窄屏折行。

`390×844` 下结构不变：标签栏仍是一行，超出部分横向滚动，指示线与徽标随滚动移动。

## 交互

- 点击一个启用项发出 `update:modelValue`。组件不自己改选中态：**这是受控组件**，父组件不处理该事件时点击没有任何视觉变化。
- 禁用项不可点击、不参与键盘移动，视觉上透明度降低。
- 键盘（焦点在标签栏内即可，事件挂在标签栏容器上）：
  - `ArrowRight` / `ArrowLeft` 在**启用项**之间循环移动，并**同时改变选中项**（自动激活，不是只移动焦点）；移动后焦点跟到新选中的标签上。两端绕回另一端。
  - `Home` 跳到第一个启用项，`End` 跳到最后一个启用项。
  - 当前值不落在任何启用项内时（例如它指向一个禁用项，或指向已被移除的值），移动起点按第一个启用项算：`ArrowRight` 会落到**第二个**启用项，`ArrowLeft` 落到最后一个。
  - 全部标签都禁用时，方向键与 `Home` / `End` 不做任何事。
- 焦点模型是 roving tabindex：只有选中项 `tabindex="0"`，其余（含禁用项）都是 `-1`，整条标签栏只有一个 Tab 停靠点。

无障碍：容器是 `role="tablist"`，`ariaLabel` 非空时同时设 `aria-label`（默认为空，此时标签栏没有可访问名）；每个标签是 `role="tab"` 并带 `aria-selected`。组件不生成 `id`、不设 `aria-controls`，也不渲染 `role="tabpanel"` 的内容区——面板与标签的程序化关联由消费方自己建立，内容切换的显隐也由消费方负责。

## 数据

```ts
/** 一个标签项。value 必须唯一。 */
export type TabsItem = {
    /** 标签身份；与 modelValue 相等即为选中项 */
    value: string;
    /** 标签文字；必填，没有纯图标形态 */
    label: string;
    /** 图标类名（如 i-lucide-star）；默认不显示图标 */
    iconClass?: string;
    /** 尾部计数徽标；可选，传 0 也会显示 */
    count?: number | string;
    /** 禁用：不可点、不可聚焦、键盘跳过；默认 false */
    disabled?: boolean;
};

export type TabsSize = "sm" | "md";

type TabsProps = {
    /** 当前选中项的 value；必填、**受控**——组件不修改它，只在交互时发出 update:modelValue */
    modelValue: string;
    /** 标签列表；必填 */
    items: TabsItem[];
    /** 标签栏的可访问名；默认空字符串，为空时不设 aria-label */
    ariaLabel?: string;
    /** 尺寸；默认 "md" */
    size?: TabsSize;
};

type TabsEmits = {
    /** 点击启用项，或方向键 / Home / End 移动选中项时发出；参数是新的 value */
    (event: "update:modelValue", value: string): void;
};
```

扩展面：组件不提供任何 slot；不 expose 任何方法或属性；未声明的 attribute、`class`、`style` 按 Vue 默认行为落到**标签栏根节点**上，不是落到单个标签上。

## 状态

- 选中：完全由 `modelValue` 决定，组件不做默认选中，也不缓存上一次的值。
- 禁用：该项不可点、不可聚焦、键盘跳过。
- 空列表：渲染一条空的底边线，没有任何可聚焦项。
- 组件自持的唯一内部状态是标签栏容器的元素引用，用于键盘改变选中项后把焦点跟过去；它不伸出组件之外，随组件销毁释放。

## 不支持

- 不支持内容面板：不渲染 `tabpanel`，也不提供面板关联的 `id` / `aria-controls`。
- 不支持换行：标签过多时横向滚动，不做多行排列。
- 不支持关闭按钮、拖拽重排、溢出菜单（没有 `closable` / `draggable` / 「更多」入口）。
- 不支持纯图标标签（`label` 必填）与垂直朝向（没有 `orientation`）。

## 注意事项

- **受控**：不在父组件里处理 `update:modelValue` 时，点击与方向键都不会改变选中态。
- **不要把一个禁用项的 `value` 传给 `modelValue`**：那会让所有标签的 `tabindex` 都变成 `-1`，整条标签栏无法用 Tab 键进入。
- **`items` 里的 `value` 必须唯一**：重复值会让多个标签同时呈选中态，键盘移动也会定位到第一个匹配项。
- `count` 的可见性由 `count !== undefined` 判定，传 `0` 会显示「0」；不想要徽标就不要传这个字段。
