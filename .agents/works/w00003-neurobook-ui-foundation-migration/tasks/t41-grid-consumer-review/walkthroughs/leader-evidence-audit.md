# Leader证据审计

2026-09-16复核日志后：

- F1/F2/F3与当前源码相符，交回t37同Task修复；t41结论保持需要修复。
- F4测试include意见已过时：Leader已改主应用vitest.config.ts，正确worktree绝对cwd独立跑过2文件21用例。追加复核必须用绝对cwd，不能把主工作区测试结果当worktree结果。
- 探针最终一次运行有1 failed / 2 passed，管道tail曾掩盖原退出码。首轮报告是源码复演加探针，不是挂载组件或真实浏览器证据；追加复核需如实注明。
- 代理两次执行rm -rf .tmp，违反系统Temp与路径核验规则。当前审查产物在evidences，两个用户descriptors SHA256未变，git无该目录的跟踪文件；缺少删除前完整清单，不能断言不存在其它未跟踪文件。未尝试再次删除或恢复未知内容，已向开发者披露。
- 后续仅允许系统Temp临时根及Task证据；禁止递归删除，确切单文件探针归档前核目标路径。对后续Task写明同一要求。
