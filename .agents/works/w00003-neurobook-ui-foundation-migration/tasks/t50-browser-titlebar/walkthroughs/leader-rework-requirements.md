# t50 返工要求（Leader 复核后）

依据：t50 实现报告与其自报的「未完成」清单。Task 合同把真实浏览器验收列为必做项，其中三项可完成但未跑完。

| # | 级别 | 要求 | 依据 |
|---|---|---|---|
| R1 | 必做项 | **补齐四主题 × 两尺寸的逐组合取证**：`nbook 浅/深`、`macOS 浅/深` × `1440×900`、`390×844`，每组给出标题栏实测几何（y=0/高 36）、`--workbench-titlebar-height` 实际值、可读性观察（文本/图标与背景对比、菜单面板背景与视口内定位），允许用截图 + 读取值两种证据；只验证"能切换主题"不算覆盖该腿。 | Task README「验证与交付」 |
| R2 | 必做项 | **Storage 失败仍可理解这一腿**：在隔离宿主里制造一次真实 Storage 失败（例如指向不可达的后端或让 action 返回错误/冲突，用你已有的注入点），观察并取证：标题栏/布局提示的文案与重试入口可理解、页面不崩、失败不静默。若某类失败在当前接线里无从触发，写明触发路径与为何不可达。 | 同 R1（Task 合同明列该腿） |
| R3 | 必做项 | **长项目名在 390 宽的菜单取证**：给出菜单面板实测宽度与是否溢出视口、Project 行 label 的 `clientWidth`/`scrollWidth` 与 `title`/省略号表现。1440 下已有读数，390 缺。 | 同 R1（合同写"长项目名"未限定宽度） |
| R4 | 记录 | 桌面 bridge 回归：本机无真实桌面宿主、vitest 下 `import.meta.client` 为假导致桌面分支不可达——**接受为环境限制**，但须在报告里写成明确条目（已尝试方式 + 为何不可达 + 影响面），并说明桌面分支的代码路径由哪些用例/静态约束保证不被这次改动破坏。 | 诚实性要求，不要求强跑 |

## 约束

- 不碰 3001（已停止，保留给开发者）；验收继续用隔离 Temp 根与显式空闲端口。
- 不回退已落地内容；不改 nb-ui 内部；不提交、不 push。
- 完成后更新 `walkthroughs/implementation.md`（把「未完成」改为对应结论或如实标注仍未覆盖），并 hub 通知 Main。
- Leader 负责：全包 typecheck、Component Lab smoke、必要时的主页面复跑。