# 追加审查裁定

先读 [t39追加审查](../../t39-splitter-review/walkthroughs/review-followup.md) 及其探针。本轮是明确且有限的同合同修复，不要重新调查首稿。

## 裁定

1. F1必须修：禁用/0px边界的Enter要截断上游有缺陷的默认动作，保持几何和事件均不变化。不能改文档允许禁用仍折叠。
2. F2必须修：只有panel身份或约束的值变化才取消。新数组但字段相同应继续拖动；同时覆盖真正变化仍取消。
3. F3必须修：键盘测试用真实可取消事件，并断言DOM面板尺寸与layout/gesture值一致。补真实Chromium Enter repeat及多键序列；无需重写所有测试。
4. F4不成立于当前文件：Leader直接读取implementation.md已是30 tests、8 E2E、lab.css已变化。不要按旧快照改回28/4/CSS未变化；收口后按实际最后结果更新即可。
5. F5/F6文档数字与失焦语义一并澄清，局部编辑。
6. 0px边界的伪元素是否吞点击需要真实浏览器核对；零尺寸应不截获下面内容点击。如果修复，保留其它sash正确相邻panel定位的回归。
7. 新sashSizes几何在手势中改变，与容器/约束变化同属程序布局：取消当前旧基线，不提交程序变化。相同值的新数组不要取消。给有意义的用例。

## 验证补齐

Leader已独立跑过当前2文件30用例，正确worktree绝对cwd；这只覆盖当前测试。
桌面与390px的四主题组合应明确形成8组合矩阵，当前4主题只在桌面跑；同一E2E循环可完成，不用人工重复脚本。
不要强行将默认fixture常规分隔条改粗来证明可配置：默认保持原1px，可为7px/0px提供明确场景或控制，并由对应测试选择；避免把验收参数变成默认视觉回归。
仅聚焦测试和本次3138或另一空闲端口的纯playground；不访问3001，不跑全包typecheck/build。不得递归删除目录，不覆盖用户改动。
最终报告列真实命令、退出码、测试数量及未运行项；读写后核文件真实内容，历史报告可过时，不能用历史报告替代当前源码。
