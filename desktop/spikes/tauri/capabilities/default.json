{
    "$schema": "../gen/schemas/desktop-schema.json",
    "identifier": "default",
    "description": "Task 140 loopback window only; no shell, filesystem, or arbitrary process capability.",
    "windows": ["main"],
    "permissions": [
        "core:window:default",
        "core:event:default"
    ]
}
